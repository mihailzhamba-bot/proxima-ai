---
phase: 03-postgresql-quality-atomic-releases
plan: 04
type: execute
requirements: [DATA-09, DATA-10]
depends_on: [03-03]
wave: 4
must_haves:
  truths:
    - "Cross-tenant access under any runtime role except migration owner fails closed and is proven by tests, not by convention."
    - "Every forbidden write (wrong role to wrong object) raises a database permission error and is asserted in the suite."
    - "The full adversarial matrix - roles x objects x tenants x crash points - runs on real PostgreSQL inside the verify cycle."
  artifacts:
    - "services/control-plane/tests/test_runtime_roles.py: cross-tenant and forbidden-write suite"
    - "services/control-plane/tests/test_crash_recovery.py: end-to-end crash matrix"
    - "docs/operations/runtime-roles.md: role/grant matrix for operators"
    - "docs/architecture/data-flow.mmd: updated with facts/release/lineage path"
  key_links:
    - "test_runtime_roles.py -> 009 roles: has_table_privilege assertions for all four roles against every Phase 3 table"
    - "test_crash_recovery.py -> 03-02/03-03: promotion and release injection matrix in one suite"
    - "runtime-roles.md -> VPS runbook family: how roles map to connection secrets (passwords live on the VPS only)"
files_modified:
  - services/control-plane/tests/test_runtime_roles.py
  - services/control-plane/tests/test_crash_recovery.py
  - docs/operations/runtime-roles.md
  - docs/architecture/*
autonomous: true
---

# Plan 03-04 - Runtime roles and the adversarial test matrix

## Objective

Prove the four-role separation with an adversarial suite on real PostgreSQL: cross-tenant isolation, forbidden-write denial for every role x object pair, and the consolidated crash-recovery matrix over promotion and release boundaries - then document the role model for operators.

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-tenant and forbidden-write suite</name>
  <files>services/control-plane/tests/test_runtime_roles.py</files>
  <action>For each of the four roles assert the full grant matrix against every Phase 3 table (SELECT/INSERT/UPDATE/DELETE per has_table_privilege), then execute real cross-tenant attempts (role session of tenant A touching tenant B rows must fail via query-level tenant constraints or permission denial) and real forbidden writes (source publisher updating pointers, release publisher inserting facts, Data Health writing anything, anyone but migration owner touching schema_migrations). Every denial asserted as the exact PostgreSQL permission error.</action>
  <verify><automated>pytest on disposable PostgreSQL: matrix green, all forbidden paths raise; suite fails if any grant is widened</automated></verify>
  <done>DATA-09 proven fail-closed by execution, not by documentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consolidated crash-recovery matrix + docs</name>
  <files>services/control-plane/tests/test_crash_recovery.py, docs/operations/runtime-roles.md, docs/architecture/data-flow.mmd</files>
  <action>Consolidate the 03-02 promotion and 03-03 release injection points into one ordered matrix suite (attempt/quarantine/fact/lineage/quality/success + release/promoted/visibility/pointer/success), each asserting full rollback, LKG intact and clean retry. Write the operator doc: role inventory, grant matrix, which CLI runs under which role, where role passwords live (VPS secrets dir only), how to verify roles after a migration. Update the data-flow diagram with the facts/release/lineage path.</action>
  <verify><automated>matrix suite green in the roundtrip runner; architecture render green in make verify</automated></verify>
  <done>DATA-10 complete: roundtrip + full crash matrix on real PostgreSQL, documented for operators.</done>
</task>

</tasks>

## Must haves

- Suite is additive to `make verify` via the roundtrip runner - the adversarial matrix is not optional.
- No role passwords in Git; local tests generate throwaway role credentials inside the disposable cluster.
- Phase closure: after this plan, EVIDENCE.md records all four plans' verify + CI + cross-model 0/0 verdicts, and STATE moves to Phase 4 planning.
