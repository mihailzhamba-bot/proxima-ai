# Phase 3 Evidence - PostgreSQL Quality & Atomic Releases

## Plan 03-01 - Schema foundation: migrations 007-009, enforced additive-only, disposable pg16 roundtrip

- **Implementation commits:** `3a470e0` -> review-fix chain `64becfc` (R1), `e82987a` (CI bind), `ef92c85` (R2), `79ba719` (R3), `4ae8a81` (R4), `e7f2c7a` (R5), `b59b615` (R6), `e92c604` (R7), `f9a4428` (R8), `10ba90c` (R9), `92b935c` (R10), `dc2d3eb` (R10 fixtures) + evidence commits `046e8bf`/`e6e3081`/`52fad8d`/`c0effe0`/`05bb3a4`/`7e62f6f`/`2af5d74`. PR #21.
- **Root verification:** `make verify` PASS on `ef92c85` (2026-08-26, dev Mac): 54 pytest / 4 PG-skip in main run + TS suites + build + contracts + migrations (additive-only enforcement active) + **pg-roundtrip on real disposable PostgreSQL 16.14**: migrations 001-009 applied through the canonical path, real-PG pytest subset 4/4 (apply-all, grant matrix incl. denials, 23 policies / 11 RLS tables recount, live two-tenant RLS isolation incl. security_invoker view + three cross-tenant FK rejections), CLI idempotence ("already current"), ledger 9/9, cluster destroyed.
- **Hosted CI:** GitHub Actions `verify` PASS on exact SHAs: `ef92c85` run `32933122869`, `79ba719` run `32933718254`, `4ae8a81` run `32934435419`, `e7f2c7a` run `32935660329`, `b59b615` run `32936767314`, `e92c604` run `32937881260`, `f9a4428` run `32971931988`, `10ba90c` run `32973531112`, `92b935c` run `32974989904`, `dc2d3eb` run `32975241804` (2026-08-26), each including pg-roundtrip executed on the runner's own PostgreSQL 16 (`/usr/lib/postgresql/16/bin`); the run on `64becfc` failed exactly at pg-roundtrip server start (IPv6 bind) - fixed by `e82987a`.
- **Independent cross-model review (critical-phase gate):** codex `gpt-5.6-sol`, read-only sandbox. Round 1 on `3a470e0`: **7 blockers / 2 warnings / 1 NIT** (quarantine FK, tenant graph identity, view owner-bypass, evidence RLS, default-deny grants, verifier bypasses, verify-evidence UNKNOWN; typed failure codes, NOLOGIN coverage, plan drift) - all closed by `64becfc`. Round 2 on `e82987a`: **3 blockers / 2 warnings** (release-edge FK gap + single-column attempt FKs; EXECUTE dynamic-SQL bypass; CI locator not in repo; fixed port; discovery order) - closed by `ef92c85` (schema) + `046e8bf` (CI locator in repo). Round 3 on `046e8bf`: **1 blocker / 1 warning** (`ALTER TABLE ... DISABLE ROW LEVEL SECURITY` passed the denylist; evidence fixture count) - closed by `79ba719` + CI `32933718254`. Round 4 on `79ba719`: **1 blocker / 1 warning** (denylist model itself fail-open: SET UNLOGGED / SET SCHEMA / DETACH PARTITION / ALTER POLICY / ALTER VIEW / ALTER ROLE BYPASSRLS / REVOKE passed; stale evidence tail) - closed by `e7f2c7a` (round-5 form constraints), `b59b615` (round-6: combined-ALTER lookahead ban, identity SET/RESET variants, CREATE alias heads, GRANT privilege pinning, embedded CREATE SCHEMA), `4ae8a81` (fail-closed allowlist: BEGIN/COMMIT, CREATE sans OR REPLACE/RULE/EVENT TRIGGER, GRANT, INSERT, SELECT, SET, RESET + ALTER TABLE limited to ADD COLUMN|ADD CONSTRAINT|ENABLE ROW LEVEL SECURITY; 27 banned + 11 allowed fixtures) and the evidence tail. Round 5 on `e6e3081`: **4 blockers / 3 warnings** (CREATE FUNCTION/EXTENSION-family heads admitted; GRANT unrestricted incl. TO PUBLIC/role membership; CREATE ROLE LOGIN/SUPERUSER/BYPASSRLS attributes; INSERT ON CONFLICT DO UPDATE rewrite; SET ROLE/SESSION AUTHORIZATION; round-2 attribution; subset-not-equal RLS set) - all closed by `e7f2c7a` (CI run 32935660329): FUNCTION/PROCEDURE/EXTENSION/SUBSCRIPTION/PUBLICATION/FDW/DATABASE heads banned, GRANT pinned to `GRANT <privs> ON [SEQUENCE] <obj> TO proxima_*`, CREATE ROLE pinned to `CREATE ROLE proxima_* NOLOGIN`, DO UPDATE banned, identity-changing SET banned, exact 11-table RLS equality; fixtures 35 banned + 12 allowed. Round 6 on `52fad8d`: **5 blockers / 1 warning** (multi-action ALTER TABLE passes the `.*` tail; SET LOCAL/SESSION ROLE and RESET ROLE/AUTHORIZATION identity variants; CREATE USER/GROUP/LANGUAGE/ACCESS METHOD/AGGREGATE/CAST/SERVER/FOREIGN TABLE heads; GRANT ALL PRIVILEGES/DELETE/TRIGGER/REFERENCES and embedded CREATE SCHEMA sub-commands; CI externally unreachable from the review sandbox; evidence summary missing e7f2c7a/32935660329) - code items closed by `b59b615` (CI run 32936767314; fixtures 57 banned + 12 allowed), evidence completed by this update. Round 7 on `c0effe0`: **5 blockers / 0 warnings** (multi-action ALTER with NO FORCE/ATTACH/RESET tails; SET LOCAL session_replication_role, quoted "role" GUC, set_config probes; CREATE VIEW without security_invoker; fixture count 56 not 57; implementation list incomplete) - closed by `e92c604` (CI run 32937881260): action-list parsing (every ALTER action must be additive; ENABLE RLS standalone-only), SET/RESET/SELECT heads removed, SET_CONFIG blanket-banned, CREATE VIEW pinned to `WITH (security_invoker = true)` with a live pg_class.reloptions assertion that NO public view lacks invoker semantics; fixtures recounted to 66 banned + 12 allowed; evidence list rewritten. Round 8 on `05bb3a4`: **5 blockers / 0 warnings** (multi-transaction migration files; quoted pg_catalog."set_config" bypass of the identifier-stripping; GRANT matrix not semantic - data_health could receive writes, ledger grants unrestricted; CTAS/MATERIALIZED VIEW data copies and permissive USING(true) policies admitted; ENABLE RLS combinable despite the documented standalone-only contract) - closed by `f9a4428` (CI run 32971931988; erratum: the fixture count recorded for b59b615 here was written as 57 before R7's AST recount established the true count 56 - the 57 figure is superseded): exactly-one-BEGIN/COMMIT transaction check on the raw statement list; raw-sql quoted set_config ban; semantic grant matrix (data_health SELECT-only, schema_migrations migration-owner-only); CREATE TABLE AS and MATERIALIZED VIEW banned, CREATE TABLE pinned to the declarative form, every CREATE POLICY must reference the tenant current_setting guard; ENABLE RLS standalone-only enforced; INSERT restricted to plain VALUES lists (no INSERT..SELECT); fixtures 77 banned + 14 allowed. Round 9 on `7e62f6f`: **4 blockers / 0 warnings** (policy check merely lexical - token in name/string or WITH CHECK without USING passed; INSERT 'plain VALUES' bypassed by parenthesised scalar subquery and side-effecting function calls; evidence inventory stale; R6 fixture-count contradiction) - closed by `10ba90c` (CI run 32973531112): CREATE POLICY pinned to the canonical tenant-guard templates (USING/WITH CHECK exact forms + the 009 EXISTS variant), INSERT pinned to the literal-VALUES grammar (subqueries and any function call rejected); evidence inventory completed and the R6 count corrected by erratum; fixtures 84 banned + 15 allowed. Round 10 on `2af5d74`: **3 blockers / 1 warning** (policy template validated the stripped '?' placeholder instead of the exact GUC literal - current_setting('evil.tenant_id', ...) accepted; grant matrix compared only the bare object name - public.schema_migrations bypassed; column-list CTAS `CREATE TABLE (cols) AS SELECT` admitted; CREATE INDEX CONCURRENTLY admitted) - closed by `92b935c` + `dc2d3eb` (CI runs 32974989904/32975241804): the GUC literal 'proxima.tenant_id' is marked in raw SQL before string-stripping and the policy templates pin current_setting(<GUC>, true); the grant-matrix ledger check compares the last name component (any qualification); `) AS SELECT` data-copy form banned (GENERATED ALWAYS AS IDENTITY stays legal); CONCURRENTLY banned (invalid inside the mandatory single transaction). Fixtures 88 banned + 15 allowed (R10 probes initially lost to a silent anchor no-op - caught by AST recount, restored; the verifier code itself was already correct). Final round verdict appended below on merge.
- **Migration checksums:** 007 `df2f563a…`, 008 `02508559…`, 009 `c2abd444…` (normalized-self-v1; re-derived by reviewer round 2). Migrations 007-009 were applied only to disposable clusters before merge - no persistent ledger carries an earlier checksum, so in-PR amendment was safe.
- **Self-review:** PASS. Live staging data untouched (runner uses its own cluster; VPS untouched); no secrets (runner creds are throwaway in a temp dir, 0600, destroyed); roles are NOLOGIN groups (login/password provisioning is operator-side, VPS-only); the additive-only verifier self-tests 88 banned + 15 allowed fixtures including stripping-order, dynamic-SQL and ALTER-rewrite bypasses.
