# Phase 3 Evidence - PostgreSQL Quality & Atomic Releases

## Plan 03-01 - Schema foundation: migrations 007-009, enforced additive-only, disposable pg16 roundtrip

- **Implementation commits:** `3a470e0` (schema + verifier + runner + tests), `64becfc` (codex round-1 fixes), `e82987a` (CI IPv4 bind fix), `ef92c85` (codex round-2 fixes: tenant-carrying FKs across the graph, EXECUTE ban, ephemeral port, discovery order). PR #21.
- **Root verification:** `make verify` PASS on `ef92c85` (2026-08-26, dev Mac): 54 pytest / 4 PG-skip in main run + TS suites + build + contracts + migrations (additive-only enforcement active) + **pg-roundtrip on real disposable PostgreSQL 16.14**: migrations 001-009 applied through the canonical path, real-PG pytest subset 4/4 (apply-all, grant matrix incl. denials, 23 policies / 11 RLS tables recount, live two-tenant RLS isolation incl. security_invoker view + three cross-tenant FK rejections), CLI idempotence ("already current"), ledger 9/9, cluster destroyed.
- **Hosted CI:** GitHub Actions `verify` run `32933122869` PASS on exact SHA `ef92c85` (2026-08-26), including pg-roundtrip executed on the runner's own PostgreSQL 16 (`/usr/lib/postgresql/16/bin`); earlier run on `64becfc` failed exactly at pg-roundtrip server start (IPv6 bind) - fixed by `e82987a`.
- **Independent cross-model review (critical-phase gate):** codex `gpt-5.6-sol`, read-only sandbox. Round 1 on `3a470e0`: **7 blockers / 2 warnings / 1 NIT** (quarantine FK, tenant graph identity, view owner-bypass, evidence RLS, default-deny grants, verifier bypasses, verify-evidence UNKNOWN; typed failure codes, NOLOGIN coverage, plan drift) - all closed by `64becfc`. Round 2 on `e82987a`: **3 blockers / 2 warnings** (release-edge FK gap + single-column attempt FKs; EXECUTE dynamic-SQL bypass; CI locator not in repo; fixed port; discovery order) - all closed by `ef92c85`. Round 3 verdict recorded below on merge.
- **Migration checksums:** 007 `df2f563a…`, 008 `02508559…`, 009 `c2abd444…` (normalized-self-v1; re-derived by reviewer round 2). Migrations 007-009 were applied only to disposable clusters before merge - no persistent ledger carries an earlier checksum, so in-PR amendment was safe.
- **Self-review:** PASS. Live staging data untouched (runner uses its own cluster; VPS untouched); no secrets (runner creds are throwaway in a temp dir, 0600, destroyed); roles are NOLOGIN groups (login/password provisioning is operator-side, VPS-only); the additive-only verifier self-tests 16 banned + 7 allowed fixtures including stripping-order and dynamic-SQL bypasses.
