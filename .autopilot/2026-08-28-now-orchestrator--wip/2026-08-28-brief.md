# Изначальная задача

> Записано 2026-08-28. Текст задачи не редактируется - он эталон, с которым
> сверяется готовый результат. Всё, что сказано позже, дописывается
> в «Дополнения» ниже.

$grill-me Смотри, мне необходим некий оркестратор в проекте, да? Чтобы я к нему приходил, говорил: «Слушай, братан, а какую сейчас приоритет на задачу делать по проекту? Чтобы это было целостно, безпроблем на в плане последовательности, то есть мы сейчас делаем вот-вот раз-два-три. Пока не закончим, не касаемся. То есть, и он контролирует эти процессы у меня. После, то есть проверяет целостность того, что мы сделали, закрепляет, и мы потом идём дальше. мне нужно максимальное погружение этот вопрос.сё, гипп обязательно в изучении того, что уже есть готовое. Ну и решение обязательно в конце, то есть полностью автономны агент, которому я прихожу, говорю, Что делаем сейчас. Он говорит, вот это задачу всё. И после этого, если он видит чего-то не хватает, он также добавляет это всё работа с жирой ксмально развёрнуто всё пишет, подписывается задача.

## Дополнения

- 2026-08-28 - «после отдай в $autopilot»
- 2026-08-28 - «Implement the plan.»

## Приложение: referent для «the plan»

Это не дословная речь Mike. Это утверждённый им после grill план, на который ссылается команда «Implement the plan.». Канонический Scope lock: Gate `RG-20260828-now-orchestrator`.

- Общий `/now` facade для Codex, Claude Code и opencode; один deterministic result во всех клиентах.
- `/now` восстанавливает и сверяет live Jira, Git/PR/worktrees, Orca, HANDOFF, TASKS и active ExecPlans.
- Выводит по одной карточке Track A/B/C, Track D всегда FROZEN; рекомендуемый порядок B > C > A.
- До старта Mike подтверждает точный key командой `/now go <KEY>`; дальше автономный цикл до merge gate и после одобренного merge до Done.
- Safe Jira create/edit/comment/link/assign/transition внутри PA/PMM выполняются автоматически и audit-before-write; delete/archive/bulk требуют approval; assignee - Mike.
- Одновременно допустима одна write-задача на каждый Track A/B/C в изолированном worktree; обязательны declared file zones, overlap блокирует старт, integration сериализован одним global lock.
- Repo state синхронизируется через отдельный ops worktree и PR; parent/sibling dirty trees не мутируются.
- Done только после merge, verify/review evidence и convergence Git/Jira/Orca/HANDOFF/TASKS/ExecPlan.
- Обнаруженный gap linked к current task, если он in-scope; иначе создаётся или обновляется backlog issue. Ambiguous, destructive или scope-changing gap остаётся blocked proposal до approval.
- Переиспользуются Orca, Autopilot, Release Gate, reviewer, Jira MCP и Git worktrees; второй orchestration engine запрещён.
- Merge, deploy, production/config destructive actions остаются explicit gates Mike; WB writes запрещены.
