# 02 — Shell: сайдбар с секциями + метрическая полоса

**Требования:** R04, R05, R05.1, R05.2, R14i
**Blocked by:** 01
**Зона:** `services/webapp/src/components/shell/`, `src/components/metrics/`, `src/lib/fixtures/`, `src/app/(app)/layout.tsx`
**Волна:** 2
**Status:** ready

## Что должно заработать

Шелл в каноне: сайдбар 256px с uppercase-секциями («Ежедневное»: Бриф, Inbox; «Аналитика»: Дашборд; «Система»: Админ, Стайлгайд), пункты 36px, активный = заливка 8% violet + левый бордер 3px. Вместо топбара-счётчика - метрическая полоса из 5 карточек: Сигналы GYR, Выручка/день, Заказы/день, OOS-риски, Свежесть данных. Каждая: число 28-32px mono + бейдж FX + дельта vs 7 дней + статичный SVG-спарклайн (без библиотек). Кабинет-свитчер - dropdown. Unreleased-плашка компактнее: «unreleased · fixtures (DEC-006)», одна строка.

## Из брифа, дословно

> «5: GYR+выручка+заказы+OOS+свежесть» — метрическая полоса из пяти карточек
> «Статичные» — спарклайны без анимации
> «Явный бейдж на каждой»

## Разделы спецификации

Истории 4, 5, 6, 18, 19; Решения «Метрическая полоса», «Провайдер данных», «FX-бейдж»; Границы: `shell`, `components/metrics`, `lib/fixtures`.

## Критерии приёмки

- [ ] Сайдбар: 256px, секции uppercase micro-labels, пункты 36px, активное состояние 8% заливка + 3px бордер; навигация работает на всех роутах
- [ ] Метрическая полоса: 5 карточек из fixtures-провайдера (getMetrics()), число mono 28-32px tabular-nums, дельта со стрелкой/знаком, спарклайн inline-SVG статичный
- [ ] FxBadge рядом с каждым числом карточек
- [ ] Карточка без данных: «нет данных» в muted-тонах (R05.1); спарклайн скрывается <380px, число+дельта остаются (R05.2)
- [ ] Кабинет-свитчер: dropdown (1 элемент fixtures)
- [ ] Компактная unreleased-плашка на всех экранах (app)-группы
- [ ] vitest: getMetrics() форма данных стабильна (шов lib/fixtures)
- [ ] typecheck + lint + build зелёные
