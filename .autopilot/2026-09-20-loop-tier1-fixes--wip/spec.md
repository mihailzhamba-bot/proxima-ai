# Spec — LOOP tier1 fixes — СЛУЖЕБНЫЙ АРТЕФАКТ AUTOPILOT. Источник: brief (протокол комнаты) + manifest R01-R08. Целевое дерево: 8d23519 (HEAD рабочей ветки), НЕ PR-состояние 1dfb971. ## R01 — /brief guard сессии brief/page.tsx: const queue = provider.mode === " 	,	"  ... getQueueService().list(await currentPrincipal()) — без обработки QueueError. Спека: обернуть чтение очереди в try/catch; QueueError с 401/неавторизацией → redirect(  /login  ); остальные QueueError → render честной страницы-заглушки с текстом ошибки (по образцу /inbox). getBrief/getSummary уже обёрнуты authorize() — не трогаем. ## R02 — ISO таймстемпы service.ts taskSelect: t.due_at::text и t.created_at::text → to_json(t.due_at)#>>'{}' и to_json(t.created_at)#>>'{}' (ISO 8601 с T и +00:00, JSC-совместимо). queue.tsx:73 не меняется (new Date(ISO) корректен везде); dateTime= получает валидное значение. Тест: unit-тест на SQL-выход (format ^\\d{4}-\\d{2}-\\d{2}T). db-тест — CI. ## R03 — 409 на отменённый сигнал service.ts accept: после existing.rows[0] — добрать статус задачи; если status = 'cancelled' → throw QueueError(409, «Сигнал уже решён, отмена финальна») — точный текст может быть отшлифован по UX-стилю страниц, суть: честный отказ, не success. Иначе — прежний replay (та же задача). queue.tsx: обработка ошибки уже идёт через request.send(..., successMsg) — проверить путь отображения 409 (alert/error в UI), при необходимости — отображение текста ошибки. ## R04 — observe pinned к end service.ts observe (~строки 186-233): end = MSK-день (completed_at + horizon_days) через lastFullMoscowDay/календарную арифметику calendar.ts (reuse вместо инлайна). Логика: row.brief_day < end → прежний «горизонт ещё не прошёл» unknown; row.brief_day > end → unknown с честной причиной (бриф за пределами окна наблюдения, замер невозможен); row.brief_day == end → вердикт по данным дня end. ## R05 — стоп-клин (bridge.py, текущее дерево) cancel(): для paperclip-операции без child stop_ok ставится только через ветки hermes/openhands; добавить подтверждение по upstream: после stop-запроса (или если upstream уже TERMINAL по reconcile) — stop_ok = True. Повторный /stop при state='cancelled' && stop_confirmed — идемпотентный успех без перезаписи состояния (нет регресса cancelled → cancelling). ## R06 — 500 + traceback + логи bridge.py HTTP-handler: BridgeError → свой status (как сейчас); любое другое исключение → traceback в stderr + send_json(500, {error: internal, uncertain: true}); silencer log_message (строка ~867) удалить. runner.py serve-цикл (~411): except-ветка печатает исключение в stderr перед sleep(10). Клиентская семантика JsonHTTP (bridge.py:67-80) не меняется: 5xx → uncertain (проверить, что так и есть). ## R07 — единая статус-мэпа Модульная константа PAPERCLIP_STATUS_MAP в bridge.py, используется в reconcile (~301) и fence (~566). PR-listing дедуп (GitHubPublisher) — nit, в scope не входит. ## R08 — bind по умолчанию bridge.config.example.json: bind 0.0.0.0 → 127.0.0.1. OPERATIONS.txt: абзац «Сетевой доступ моста» — compose публикует 127.0.0.1:18770; не-loopback bind допустим только с TLS-фронтом; runner на другом хосте ходит по https (JsonHTTP http разрешает только loopback/control-имена). Проверить test_loop_config.py — если ассертит bind, обновить. ## Тесты - tools/tests/test_loop_bridge.py: новые кейсы R05 (childless stop, идемпотентный повтор, нет регресса), R06 (500+uncertain на мусорном handler-исключении), R07 (мэпа одна — smoke на использование). - services/webapp/src/tests/loop.test.ts: R02 формат, R03 unit-ветка (если чистая функция), R04 observationReadiness/observe-ветки. - db-тесты (loop.db.test.ts) для R02/R03 — только CI (pg-roundtrip SKIP локально). ## Верификация Локально: pytest tools/tests (через uv), vitest webapp, typecheck, lint, npm test (collector), затем make verify (ожидаемо один SKIP pg-roundtrip). Далее reviewer-субагент по diff, push, PR.