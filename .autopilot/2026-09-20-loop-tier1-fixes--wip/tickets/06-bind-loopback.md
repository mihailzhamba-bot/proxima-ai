# T06 — R08: bind по умолчанию loopback + OPERATIONS.txt ## Требование R08: bridge.config.example.json bind → 127.0.0.1; OPERATIONS.txt — абзац: не-loopback bind требует TLS-фронт; JsonHTTP http-allowlist (loopback + control-имена) — как есть. ## Файлы - infra/loop-control/bridge.config.example.json - infra/loop-control/OPERATIONS.txt - tools/tests/test_loop_config.py (если ассертит bind) ## Приёмка - [ ] example bind 127.0.0.1 - [ ] OPERATIONS.txt абзац добавлен (правка существующего файла, не новый .md) - [ ] pytest tools/tests зелёный ## Прогон - [ ] реализовано - [ ] тест - [ ] ревью