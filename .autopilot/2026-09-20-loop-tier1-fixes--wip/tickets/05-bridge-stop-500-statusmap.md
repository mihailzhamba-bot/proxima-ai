# T05 — R05+R06+R07: bridge stop-wedge + 500/traceback + единая статус-мэпа ## Требование (один коммит — tier 1.5 привязан к R05) R05: stop_ok достижим для childless paperclip; повторный /stop идемпотентен; нет регресса cancelled→cancelling. R06: unexpected → 500 + traceback + uncertain; silencer снят; runner serve-loop логирует исключения. R07: PAPERCLIP_STATUS_MAP одна на reconcile и fence. ## Файлы - tools/loop/bridge.py (cancel ~322-386, reconcile ~301, fence ~566, handler ~960, silencer ~867) - tools/loop/runner.py (serve-цикл ~400-415) - tools/tests/test_loop_bridge.py ## Приёмка - [ ] childless paperclip: upstream TERMINAL → stop_ok=True → state=cancelled - [ ] повторный /stop по cancelled — тот же ответ, состояние не перезаписывается - [ ] BridgeError ведёт себя как раньше; неожиданное исключение → 500 {uncertain:true} + traceback в stderr - [ ] serve-loop печатает исключение в stderr - [ ] reconcile и fence читают одну константу - [ ] pytest tools/tests зелёный (uv) ## Прогон - [ ] реализовано - [ ] тест - [ ] ревью