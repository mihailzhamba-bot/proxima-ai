# Interfaces — LOOP tier1 fixes — СЛУЖЕБНЫЙ АРТЕФАКТ AUTOPILOT. ## Из spec (границы, которые нельзя ломать) - services/webapp/src/lib/loop/service.ts: публичные функции list/accept/cancel/complete/observe — сигнатуры не меняются; меняется только поведение веток (R03 409, R04 честный unknown, R02 формат строк). - tools/loop/bridge.py: HTTP API моста (эндпоинты/пути) не меняется; меняются статусы ответов только для unexpected-исключений (400→500+uncertain) и семантика подтверждения стопа (R05). JsonHTTP-клиент (bridge.py:67-80) не меняется. - db/migrations/*, contracts/*.schema.json, codegen-выход, .github/ — вне изменений. ## Построено тикетками (заполняется по ходу) - T01: brief/page.tsx — локальный helper loopQueueErrorView + redirect /login на 401. - T02: SQL taskSelect — to_json(t.due_at)#>>'{}', to_json(t.created_at)#>>'{}'. - T03: service.ts accept — SELECT статуса существующей задачи; cancelled → QueueError 409. - T04: service.ts observe — mskDayFromCompleted + сверка row.brief_day с end; reuse calendar.ts. - T05: bridge.py — upstream-подтверждение стопа для childless paperclip; идемпотентный /stop; _internal_error() 500+uncertain+traceback; PAPERCLIP_STATUS_MAP. - T06: bridge.config.example.json bind=127.0.0.1; OPERATIONS.txt раздел про bind/TLS.