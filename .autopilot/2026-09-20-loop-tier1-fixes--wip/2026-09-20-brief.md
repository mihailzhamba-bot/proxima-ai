# Brief — 2026-09-20 — фикс tier 1 + 1.5 из Code Review Crew (PR #140 LOOP pilot) — СЛУЖЕБНЫЙ АРТЕФАКТ AUTOPILOT, не документ репозитория. Не коммитить отдельно от .autopilot/ ритуала; в противном случае удалить до merge, если Mike скажет. Правило репо «не создавать .md» Mike одобрением плана отменил для этого рана (2026-09-20, сессия «code-review-crew → /autopilot» — PLAN.md в чате, решения: полный ритуал, ставить uv, ветка + push + PR). Аналогично помечены остальные файлы этой папки. При merge: сводка единицы — в PR-тело; этот каталог остаётся в репо как протокол рана. # Brief — код-ревью Code Review Crew (party-режим bmad, 3 раунда перекрёстно верифицированных находок) по PR #140 «LOOP pilot», голова 1dfb971, прогнано по дереву 8d23519. Слова Mike: «гоу в план /autopilot» — т.е. автопилотом реализовать согласованные комнатой фиксы. Источник требований — финальный протокол комнаты (ниже, заморожен). ## Протокол комнаты (источник, заморожен) Tier 1: 1. brief/page.tsx:26 — crash на истёкшей сессии вместо redirect /login. 2. service.ts:27 + queue.tsx:73 — timestamptz::text в new Date(): Invalid Date в JSC/iOS. 3. service.ts:152-157 + 019_loop_queue.sql:62 + queue.tsx:44 — ре-подтверждение после отмены врёт в success-сообщении. 4. service.ts:199-218 — день наблюдения замеряет последний бриф вместо конца горизонта. 5. bridge.py:216-219 (PR) — стоп-клин: stop_ok недостижим для childless paperclip; /stop регрессирует cancelled → cancelling. 6. bridge.config.example.json:2 + bridge.py:51 — пара: bind 0.0.0.0 в примере + plaintext allowlist. Tier 1.5 (тем же коммитом с п.5): traceback вместо catch-all-400 (bridge.py:466,529 PR), вынос дублированной статус-мэпы (bridge.py:184,280 PR). Границы (утверждены Mike в плане): миграции не трогаем; contracts/*.schema.json и codegen не трогаем; .github/ не трогаем; tier 2/ниты — только строкой follow-ups в HANDOFF; merge и deploy не выполняются — за Mike. Решения Mike (план от 2026-09-20): полный ритуал autopilot (.autopilot/ в репо); ставить uv для pytest; ветка fix/loop-pilot-review-tier1 от 8d23519, push + PR без merge. ## Готовые решения (не открывать заново) R04: brief_day < end → «ещё не прошло»; brief_day > end → честный unknown; brief_day == end → вердикт. Reuse lastFullMoscowDay из calendar.ts. R03: replay немCancelled задачи сохраняется (идемпотентность); отменённая → 409 с честным текстом. R06: unexpected → 500 + {":"uncertain" } 	,		" style="	 ,	" — см. spec.md; silencer снять, serve-loop логирует исключения. R05: точную семантику cancel() брать из ТЕКУЩЕГО дерева (8d23519), не из PR-строк; протокол — источник требования, не старые строки.