# Brief — фикс tier 1 + 1.5 из Code Review Crew (PR #140 LOOP pilot)

> Записано 2026-09-20. СЛУЖЕБНЫЙ АРТЕФАКТ AUTOPILOT (протокол рана). В репо — с одобрения Mike (план от 2026-09-20: полный ритуал скилла, ставить uv, ветка fix/loop-pilot-review-tier1 + push + PR без merge).

Слова Mike: «гоу в план /autopilot» после сессии code-review-crew — автопилотом реализовать согласованные комнатой фиксы по PR #140 «LOOP pilot» (голова 1dfb971), находки верифицированы по дереву 8d23519.

## Протокол комнаты (источник, заморожен)

Tier 1:

1. `brief/page.tsx:26` — crash на истёкшей сессии вместо redirect `/login`.
2. `service.ts:27` + `queue.tsx:73` — `timestamptz::text` в `new Date()`: Invalid Date в JSC/iOS.
3. `service.ts:152-157` + `019_loop_queue.sql:62` + `queue.tsx:44` — ре-подтверждение после отмены врёт в success-сообщении.
4. `service.ts:199-218` — день наблюдения замеряет последний бриф вместо конца горизонта.
5. `bridge.py` (PR 216-219) — стоп-клин: stop_ok недостижим для childless paperclip; `/stop` регрессирует cancelled → cancelling.
6. `bridge.config.example.json:2` + `bridge.py:70` (в дереве 8d23519) — пара: bind 0.0.0.0 в примере + plaintext allowlist.

Tier 1.5 (тем же коммитом с п.5): traceback вместо catch-all-400; нулевая диагностика bridge (log_message заглушён, serve-цикл runner'а глотает исключения); вынос дублированной статус-мэпы.

## Границы (утверждены Mike в плане)

- Миграции не трогаем; `contracts/*.schema.json` и codegen-выход не трогаем; `.github/` не трогаем.
- Tier 2/ниты — только строкой follow-ups в HANDOFF.
- Merge и deploy не выполняются — за Mike.

## Готовые решения (не открывать заново)

- R04: `brief_day < end` → «ещё не прошло»; `brief_day > end` → честный unknown; `brief_day == end` → вердикт. Reuse `lastFullMoscowDay`/календарь из calendar.ts.
- R03: replay не-отменённой задачи сохраняется (идемпотентность); отменённая → 409 с честным текстом.
- R06: unexpected → 500 + traceback + uncertain; клиентская семантика JsonHTTP не меняется.
- R05: точную семантику `cancel()` брать из ТЕКУЩЕГО дерева (8d23519), не из PR-строк; протокол — источник требования.

## Дополнения

- 2026-09-20 — сбои вывода при старте рана; артефакты переписаны начисто, код не тронут. Требования не менялись.
