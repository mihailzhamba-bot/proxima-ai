FROM python:3.12.12-slim-bookworm
RUN useradd --uid 10001 --create-home loop && mkdir -p /var/lib/loop/bridge && chown loop:loop /var/lib/loop/bridge
COPY tools/loop/bridge.py /opt/loop/bridge.py
USER 10001:10001
CMD ["python3", "/opt/loop/bridge.py", "--config", "/etc/loop/bridge.json"]
