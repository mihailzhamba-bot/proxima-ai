[Service]
# Admission requires 3 GiB free before any batch process starts.
ExecStartPre=/usr/bin/python3 -I -c "import shutil,sys;sys.exit(0 if shutil.disk_usage('/srv/loop-runner/work').free >= 3221225472 else 1)"
