# Ворота 1 - сводка на одной странице

**Дата:** 30.08.2026. **Статус:** ждёт подтверждения Mike. Бэклог Jira (PA + PMM) - в работе в параллельном окне, строка появится в `BACKLOG-REVIEW.md`.
**Источники:** `INVENTORY.md`, `WEB-STATE.md`, `API-FACTS.md`, `MIGRATION-GAPS.md`, `WORKS-TODAY.md` (все в `docs/state/`), решения `DECISIONS.md`.

## Красные флаги (по убыванию влияния на 30.09)

1. **Данные не собираются.** Последний WB-ответ 25.08, расписания сбора нет и никогда не было. Без ежедневного сбора нет ряда для нормы - M-01 первая после разведки. История в WB не копится: окно Statistics ~6 месяцев скользящее.
2. **~1.5k строк кода лежат в 7 незамерженных ветках.** Среди них детектор нормы 7/14/28 (`pmm-20-scn-001`, прямое попадание в M-02/M-03), единственный писатель дневного ряда `fact_order_counts` (`PA-03-02` / `pa41`), контракты сигнала (`pmm29-contracts`), провайдер данных веб-морды (`ai/pa-50`, спасена сегодня). Три разных `010_*.sql` - коллизия нумерации при мерже.
3. **Схема БД отстаёт от кода:** в БД миграции 1-6, в main 1-10; compose пилота монтирует миграции из чекаута 25.08. 11 из 23 таблиц никто не пишет.
4. **Воронка WB v2 снята (404).** Замена `v3/sales-funnel` не проверена - не входила в allowlist. Продажи/заказы (Statistics) есть с 01.03.2026, ~26 недель.
5. **Пять копий кода разъехались; веб-морда поднята вручную** из ветки 26.08 (≠ main), auth выключен, Caddy из main не развёрнут.

Зелёное: секретов в истории git нет (276 коммитов, 12 паттернов); потерь при переезде 0; `make verify` зелёный на маке (52 TS + 202 py); 33/33 пунктов регрессии проходят; кабинет подтверждён (ИП Амирова О. Ф.).
Жёлтое (не блокирует): `~/.git-credentials` на сервере вопреки README; analytics-токен read-write; кто-то извне ежедневно ~00:51 UTC создаёт `detail_history_report` этим токеном - источник не найден.

## Картина в одном абзаце

Один VPS (Selectel, только tcp/22). Живут: Postgres пилота (13 таблиц, данные заморожены 25.08), веб-морда Next.js на fixtures, GlitchTip, OpenHands с rootless Docker и egress через SSH-туннель на 153.56.134.240, host-monitor, ночной бэкап в S3. Код на GitHub main цел и собирается; продуктовой ценности пока нет: нет сбора, нет нормы, нет сводки. Из промта «непонятно, что готово» - теперь понятно: фундамент M1 готов на 60%, лестница M-00..M-05 начата на M-00 (разведка API сделана сегодня).

## Условие 1. Подтверждение картины

Вопрос Mike: картина совпадает с реальностью? Если что-то из красных флагов - не новость или не так, назвать номер.

## Условие 2. План Б по глубине истории API

| Вариант | Что берём за базу нормы | Глубина сейчас | Срок до базы нормы (8 нед) | Последствие для 30.09 |
|---|---|---|---|---|
| A (рекомендован) | Дневной ряд продаж и заказов из Statistics `sales`/`orders` | 26 недель (с 01.03.2026) | есть сегодня | M-03 «минус 23% против обычного вторника» по выручке/заказам реалистичен |
| B (параллельно) | Воронка (показы → корзина → заказ) через async CSV `nm-report/downloads`, старт сбора 01.09 | 0 (v2 снят) | 27.10 | воронка в сводке - октябрь; либо проверить `v3/sales-funnel` (2 вызова, нужно разрешение) |
| C (запас) | Недельный разрез из финотчётов `reportDetailByPeriod` | 31 месяц | есть | норма «неделя к неделе», без дневного разреза - хуже для утренней сводки |

Рекомендация: A сейчас + B фоном с 01.09 + разрешение на 2 пробных вызова v3. Решение - Mike.

## Условие 3. Спасать или переписывать

Живого больше половины: сервер 15/24 работает, код собирается и проходит verify, потерь 0. Вопрос «переписывать всё» не встаёт. Две оговорки: веб-морду доделывать, не переписывать (WEB-STATE, ~3 единицы работы против 3.5-4); по каждой из 7 веток - отдельно «мерж через ревью / списать» (таблица в MIGRATION-GAPS §4).

## Решения, которые нужны от Mike до Сессии 2

1. Картина подтверждена? (условие 1)
2. План Б: A + B фоном? (условие 2)
3. Ветки: какие мержить через ревью, какие списать - минимум по `pmm-20-scn-001`, `PA-03-02`/`pa41`, `pmm29-contracts`, `ai/pa-50`.
4. Фикстуры 436 МБ (два файла > 200 МБ): оставить на сервере + копия в S3 к бэкапам, или git-lfs.
5. Разрешение на 2 read-вызова `v3/sales-funnel/products/history` с сервера.
6. Внешний создатель `detail_history_report` ~00:51 UTC - твой скилл `wb-analytics` на маке / autopilot / другое?

После ответов: Сессия 2 - `bmad-project-context` (brownfield) → AGENTS.md, брифинг Grill Me → DECISIONS.md → Ворота 2.
