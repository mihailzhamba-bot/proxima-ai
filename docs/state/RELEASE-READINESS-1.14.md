# Готовность к релизу: Story 1.14 «Первый релиз M-01 на сервере»

## Ревизия v2 (08.09.2026, вторая редакция)

**Дата проверки:** 08.09.2026, 13:09-13:14 UTC; §4 дополнен результатом репетиции (13:10:14-13:11:55 UTC, `main` `1c5e256`, по отчёту оркестратора) и находками B9/B10. **Кто:** docs-воркер конвейера (единица U-A3 по D35), сервер только чтение. **База кода:** `origin/main` `ad2b3f4` (PR #110, 08.09 13:07 UTC; CI `verify` + `images` success). PR #111 (репетиция) смержен в `main` 13:09 UTC (`1c5e256`), CI на нём в момент записи `in_progress`; на PR все проверки (`verify`, `build-images`, `systemd-verify`, `apply-migrations-in-container`) были зелёные. **Первая редакция:** `a1504ab` (PR #93, 08.09 06:32/06:37 UTC) - проверка 06:21-06:40 UTC, блокеры B1-B6, два вопроса к Mike в §4. **Сервер:** `claudette` (135.106.186.210) под `proxima-admin`, те же формы доступа, что в v1: `systemctl`, `sudo -n git … rev-parse/status/remote/tag`, `sudo -n ls`, `sudo -n docker ps`, `sudo -n docker compose ls`, SELECT через `/usr/local/sbin/proxima-psql-readonly` (один запрос). Ничего не запускалось, не останавливалось и не менялось; значения секретов не читались - только имена, владельцы и права.

**Источник критериев:** `_bmad-output/planning-artifacts/epics.md`, Story 1.14 (строки 338-350); формулировки AC ниже те же, что в v1.

**Статусы:** `готово` - подтверждено файлом или выводом команды; `блокер` - релиз не может начаться, пока пункт не закрыт; `частично` - есть заготовка, но условие AC не выполнено; `UNKNOWN (до деплоя)` - проверяется только исполнением runbook (или репетицией, §4). Владелец: `конвейер` (оркестратор/воркеры, без деплоя), `Mike`, `Владислав`.

### Что изменилось с первой редакции

| # | Что | Было (v1, 06:40 UTC) | Стало (v2) | Источник |
|---|---|---|---|---|
| 1 | Решение по CP-12 и дате (§4 v1) | два открытых вопроса к Mike | **D35** (`DECISIONS.md`, PR #108, 13:06 UTC): CP-12 не снимается - Story 6.1 в `main` до пт 11.09, релиз 1.14 вт 15.09 по D33 (PR #89, открыт); октябрьский трек (4.4, Epic 5) заморожен до трёх утр SUCCEEDED; репетиция на VPS с живым хвостом из 2 read-вызовов; новые источники данных - отдельный эпик позже | `DECISIONS.md` D35, п. 1-4 |
| 2 | B3 - runbook отстал от `main` и сервера | блокер, пять расхождений (a)-(e) | **закрыт** PR #99 (08:36 UTC): §0 через `proxima-psql-readonly`, §1.1 правка `.env` (`COMPOSE_FILE`, `PROXIMA_SECRETS_DIR`, `WEBAPP_*`), §2 «007…018» и ожидание `18\|18`, §3 `--date-from 2026-08-27` и импорт от root, §7 условный `sed`; раздел «Сверка 08.09.2026» в runbook перечисляет 15 правок | `docs/operations/release-m01.md` строки 44, 160-165, 244, 404-422 |
| 3 | B4 - схема 6 против кода | блокер только вместе с B3 | **закрыт вместе с B3**: runbook §2 ждёт `18\|18`, §0 проверяет последнюю миграцию тега (`ls … \| tail -1`); на сервере по-прежнему 6 (13:09 UTC) - ожидаемое состояние до §2 | runbook строки 36-38, 165; §5 ниже |
| 4 | Контракт монтирования коллектора | в v1 не проверялся | **PR #103** (10:00 UTC): tenant-prefixed WB-токены и `${PROXIMA_RAW_DIR}` через compose `secrets:`/`volumes:` сервиса `collector`, контейнерные пути в `morning_run.sh`/`funnel_*_run.sh`, тест `tools/tests/test_compose_collector_mounts.py`, проверка в CI `images.yml`; ручные `-v` из runbook §3 убраны, в §5 добавлена проверка `compose config \| grep` | `infra/compose.yaml` 43-52; runbook строки 283-287, 420 |
| 5 | **Новый блокер B7** - живая сеть WB | не найден | `WB_ALLOW_LIVE_NETWORK=1` (AD-4, fail-closed) не выставлялся нигде в контуре деплоя - первый `collect` на сервере упал бы на первом HTTP-вызове с `WB_NETWORK_FORBIDDEN`. **Закрыт в `main`** PR #110 (13:07 UTC): флаг в `environment:` сервиса `collector`, гейт `tools/verify_live_network.py` (`make live-network`, в `make verify`), проверка в runbook §3. На сервере подтверждается в §3 в день релиза | `infra/compose.yaml` 38-42; runbook 234-239, 289, 422; §2 B7 |
| 6 | **Новый пункт B8** - репетиция на VPS (D35 п. 2) | - | `infra/compose.rehearsal.yaml` + `tools/rehearsal_run.sh` + раздел runbook «Репетиция на VPS (D35, не деплой)» (PR #111, `feat/rehearsal-stack`, смержен 13:09 UTC). Проект `proxima-rehearsal` поднят оркестратором 13:10 UTC, репетиция прошла 13:10:14-13:11:55 UTC - результат §4: цепочка `backfill → collect → norm → brief` SUCCEEDED, боевой statistics-токен прошёл проверку scope, W10 сошёлся точно, W35 разошёлся по объяснимой причине (правило гейта - решение Mike), два новых блокера 2.6 - B9/B10 | §4; `docker compose ls` |
| 7 | Story 1.11 | `backlog`, «влита в 2.5» | **`done`** PR #109 (13:07 UTC): объём отгружен в 2.5 (PR #69) по CP-5. Оговорка для экрана приёмки - §6 п. 1 | `sprint-status.yaml:52` |
| 8 | Исполнитель runbook | строка 3: «Исполняет Mike на VPS» | по D7 исполняет Claude (сессия оркестратора на VPS) после явного «деплой» от Mike; за Mike - решения раздела 1, если хочет сделать их сам. Правка в этой же ветке | `DECISIONS.md` D7; runbook строка 3 |
| 9 | `WORKS-TODAY.md` | пунктов конвейера нет (T6, A2) | блок «Конвейер сбора (ожидаемо после релиза 1.14)», WT-34…WT-40, обновлены WT-03, NW-2, NW-3 - в этой же ветке | `docs/state/WORKS-TODAY.md` |

## 1. Пункты AC Story 1.14

| # | Пункт AC (дословно) | Статус v2 | Evidence (файл:строка, команда → вывод) | Владелец |
|---|---|---|---|---|
| G1 | **Given** «Stories 1.0-1.7, 1.9, 1.10, 1.12, 1.13 смержены (1.8 - октябрь, 1.11 - в составе 2.5: правило сжатия)» | готово | `sprint-status.yaml` (last_updated 08.09 12:49): 1.0-1.13 все `done`, включая 1.11 (PR #109, 13:07 UTC - объём в 2.5, PR #69) и 1.8 (PR #73, 07.09). Мержи - как в v1 | конвейер |
| G2 | «в том числе 1.5 с реальным бэкфиллом (без синтетических строк)» | готово (код); первый прогон на артефактах - репетиция | PR #64 (03.09), `services/collector/src/jobs/backfill.ts:22` - источник строго `artifact:<sha256>,<sha256>` из CAS. До 08.09 цепочка на реальных артефактах 31.08 не прогонялась ни разу (D35, «Диагноз»); репетиция `rehearsal_run.sh backfill` делает это первой - результат §4 | конвейер |
| G3 | «теневой пересчёт шагов 1-4 и 6 (Story 6.1) выполнен - расхождений нет либо каждое объяснено записью в `docs/state/SHADOW-RECONCILIATION.md` (CP-12)» | **блокер** | Без изменений: `SHADOW-RECONCILIATION.md:17` пусто, `verification/` и `tools/verify_shadow.py` отсутствуют, `sprint-status.yaml:91` - 6.1 `backlog` (проверено 13:12 UTC на `ad2b3f4`). **D35 п. 3:** ждать Владислава, Story 6.1 в `main` до пт 11.09, waiver D26 не пишется. Вариант B из v1 §4.1 отклонён | Владислав (срок 11.09) |
| G4 | «тег `v2026.09.NN-2`» | частично | Дата известна: `v2026.09.15-2` (runbook строка 38, по D33/D35). Тега нет: `git ls-remote --tags origin` → пусто; `sudo -n git -C /srv/proxima-ai/repo tag --list` → пусто (13:09 UTC). Ставится на `main` за сутки до релиза - пн 14.09 - и записывается в runbook §0 и `CHANGELOG.md` (файла нет) | конвейер (14.09) |
| G5 | «runbook по CP-1» | готово (документ) | `docs/operations/release-m01.md` разделы 0-8 в порядке CP-1; B3 закрыт PR #99, форма вызова заданий и монтирование - PR #103, проверка флага сети - PR #110; исполнитель по D7 - эта ветка. Хвост вне `main`: обёртка `proxima-psql-owner` (§1.4, строка 117) создаётся в день релиза руками, кандидат в `infra/bootstrap/` (раздел «Октябрь», строка 402) | конвейер |
| G6 | «явное «деплой» от Mike (D7)» | **блокер** | Не получено; по D35 - «деплой только по слову «деплой» от Mike»; ожидается вт 15.09 | Mike (15.09) |
| G7 | «дата деплоя стоит в календаре Mike - не позже 20.09.2026 при выполненном бэкфилле (13.09 без него) `[ожидает календаря Mike]`» | готово | D35 (в `main`, PR #108): «релиз 1.14 - вт 15.09 по D33 (PR #89)». D33 в `origin/docs/analyst-week2-2026-09-08` (PR #89, открыт): 6.1 до пт 11.09, 1.14 вт 15.09 с LOGIN-ролью аналитика, пересчёт 6.4 ср 16.09, три утра 16-18.09, 2.6 вт 22.09. 15.09 ≤ 20.09 - в окне AC. Пока #89 не смержен, дата в `main` держится только ссылкой из D35 (§6 п. 9) | Mike (сделано) |
| W1 | **When** «Claude выполняет runbook шаг за шагом, фиксируя вывод в `docs/operations/releases/2026-09-NN-m01.md`» | UNKNOWN (до деплоя) | `ls docs/operations/` (13:12 UTC) → каталога `releases/` нет; runbook §8 (строка 391) называет файл `2026-09-15-m01.md`; заготовки нет. Логи репетиции - `<root>/logs/*.log`, не журнал релиза | конвейер |
| T1 | **Then** «миграции применены полностью» | UNKNOWN (до деплоя) | Боевая база 13:09 UTC: `SELECT max(version) FROM schema_migrations` → `6`; чекаут `/srv/proxima-ai/repo/db/migrations/` → последняя `006_raw_artifact_headers.sql`; `main` → `018_nm_daily.sql`. Runbook §2 ждёт список из 12 имён `007…018` и `18\|18`. Репетиция и CI стартуют с пустого тома (initdb применяет 001-018 сам, `apply-migrations` отвечает `already current`) - инкрементальный накат 007-018 поверх живой схемы 6 с данными пилота до 15.09 не прогоняется нигде (§6 п. 4) | конвейер |
| T2 | «provision идемпотентен» | UNKNOWN (до деплоя); репетиция - два прогона | CI `pg-roundtrip` (`provision: idempotent (2 runs)`, WT-30); runbook §1.4 + §2 - два прогона до и после миграций; `rehearsal_run.sh up` гоняет оба - результат §4 | конвейер |
| T3 | «бэкфилл из артефактов 30.08 + живой хвост SUCCEEDED» | UNKNOWN (до деплоя); репетиция - первый прогон | Артефакты на месте (`ls ~/signal-inputs/fixtures/wb-api/statistics/…` 13:12 UTC: пары 30.08 и 31.08), sha256 сверены в v1 (4/4). Репетиция: `backfill` (пара 31.08) → `tail --live` (2 read-вызова, D35 п. 2) - §4. Копия в S3 - `UNKNOWN` (B6) | конвейер / Mike (S3) |
| T4 | «`fact_cabinet_daily_current` содержит дни с 02.03.2026 по вчера» | UNKNOWN (до деплоя) | На боевой view нет (v1: `to_regclass` → NULL; схема не менялась). `rehearsal_run.sh check` печатает ledger и W10/W35 - §4 | конвейер |
| T5 | «W10 и W35 совпадают с API-FACTS» | готово (доки); сверка - репетиция / релиз | `docs/state/API-FACTS.md:144-151` «Эталоны недельных сумм W10/W35» (PR #99): W10 = 649 / 700 860.50 ₽ (копейки - PR #112), W35 - по дням против пары 31.08 (правило Mike 08.09, D35 дополнение, PR #116: 24-26.08 = `55|6|62146.87`, `40|7|29247.90`, `28|4|44956.00`; 27-30.08 = последние наблюдения), ISO-недели; старое «225 / 263 089» - снимок 30.08, не сверяется. Runbook §4 (строки 255-265) и `rehearsal_run.sh` (`W10_EXPECTED`/`W35_EXPECTED`) сверяют с этими числами - §4 | конвейер |
| T6 | «`WORKS-TODAY.md` прогнан и дополнен (таймеры активны, `data_status_current`, `restore-check`)» | частично → пункты заведены | В этой ветке: блок WT-34…WT-40 (таймер 05:30 МСК, `collector_runs`, `data_status_current`, `norm_daily_current`/`brief_current`, `restore-check` пн, `make live-network`, `/brief`), все `NOT APPLICABLE` до релиза; WT-03 ждёт `18\|18` после 1.14; NW-2/NW-3 датированы 08.09 с D35. Полный прогон - после релиза (runbook §4, строка 275) | конвейер |
| T7 | «план отката записан до первого шага» | готово (документ) | Runbook §7 (строки 336-378) после PR #99: таймеры → `delete_run.py --dry-run` в контейнере `control-plane` → условный `sed WEBAPP_DATA_MODE` (переменная появляется в §1.1) → `checkout v2026.09.0-baseline` + возврат `.env` из копии. Точка возврата создаётся в §0 в день релиза (тегов на сервере нет, 13:09 UTC) | конвейер |
| T8 | «порядок исполнения - бэкфилл → первый `collect` → включение таймеров (CP-1)» | готово (документ) | Runbook §3 → §4 → §5 («только после SUCCEEDED бэкфилла и сошедшихся цифр», строка 281); репетиция повторяет §2-§4 в том же порядке | конвейер |
| T9 | «утренний прогон `collect` завершается SUCCEEDED и `data_status_current` обновлён не позже 06:30 МСК (PRD FR-22; `brief_current` появляется в релизе 2.6), превышение видно как алерт через `TimeoutStartSec` юнита `proxima-morning@` с `OnFailure` (задача PA-65 sync-brief 03.09, до релиза 1.14)» | UNKNOWN (до деплоя); код готово, B7 закрыт | PA-65 в `main` (PR #80); `proxima-morning@.timer:5` `OnCalendar=*-*-* 05:30:00 Europe/Moscow`, `.service` `TimeoutStartSec=55min`, `OnFailure=proxima-alert@%n.service`. Флаг живой сети теперь в compose (PR #110; `sudo -n docker compose … config \| grep -c 'WB_ALLOW_LIVE_NETWORK: "1"'` на чекауте `ad2b3f4` → `1`, 13:12 UTC). На сервере юнитов `proxima-morning@`/`proxima-restore-check@`/`proxima-alert@` по-прежнему нет (13:09 UTC) | конвейер |
| A1 | **And** «три утра подряд `collector_runs` содержит SUCCEEDED `collect` (CAP-1)» | UNKNOWN (до деплоя + 3 дня) | Календарь D33: утра ср-пт 16-18.09, релиз 2.6 вт 22.09 - запас три дня (в v1 при D = 20.09 запаса не было) | конвейер |
| A2 | «`data_status_current` отдаёт `last_full_day` = вчера и `stale = false` (проверка SQL под `proxima_webapp_readonly` через `ssh -N proxima-db`, пункт `WORKS-TODAY.md`)» | UNKNOWN (до деплоя); пункт заведён | `WORKS-TODAY.md` WT-36 (эта ветка): SQL и ожидание `<вчера по МСК>\|<сегодня ≤ 06:30 МСК>\|f`; форма для Mike с мака через `ssh -N proxima-db` и URI роли `proxima_webapp` описана там же. Роль и view появляются в §1.4/§2 runbook | конвейер |
| A3 | «строка статуса на `/brief` появляется в релизе 2.6 (Story 2.5 с объёмом 1.11)» | готово (вне объёма 1.14) | 1.11 `done` (PR #109), 2.5 `done` (PR #69). В 1.14 витрина остаётся на фикстурах (`WEBAPP_DATA_MODE=fixtures`, runbook §1.1 строка 76). Оговорка про режим «статус без сводки» - §6 п. 1 | конвейер |
| A4 | «не пришёл - откат по плану» | готово (план) | Runbook §7; см. T7 | конвейер |
| A5 | «Владислав в течение суток после релиза повторяет шаги 1-4 и 6 теневого пересчёта против боевых данных (Story 6.4) и заполняет первую строку журнала сверки с кабинетом (`docs/state/CABINET-RECONCILIATION.md`) по дню, закрытому не менее 3 суток назад» | **блокер** (предпосылки) | `sprint-status.yaml:94` - 6.4 `backlog`; `CABINET-RECONCILIATION.md` - эталон `UNKNOWN` (без изменений с v1). D33: LOGIN-роль аналитика выдаётся вместе с релизом 15.09, пересчёт ср 16.09; состав грантов не предложен (B5). На боевой ролей спайна нет (схема 6) | Владислав / Mike (роль) |
| A6 | «Mike выполняет одно действие: по пункту `WORKS-TODAY.md` одной командой через `ssh -N proxima-db` видит `last_full_day` = вчера и `stale = false`; экран `/brief` со строкой статуса - в релизе 2.6» | UNKNOWN (до деплоя) | WT-36 заведён; LOGIN-роль `proxima_webapp` и файл `proxima_webapp_uri` создаёт `provision-runtime-roles.sh` в §1.4 | Mike (после релиза) |

## 2. Жёсткие блокеры (v2)

Порядок - как в v1; статус на 08.09 13:14 UTC.

| # | Блокер | Статус v2 | Факт (источник, дата) | Кто закрывает |
|---|---|---|---|---|
| B1 | **Story 6.1 - теневой пересчёт не начат.** CP-12 требует шаги 1-4 и 6 в журнале | **открыт**, срок пт 11.09 (D33/D35) | `SHADOW-RECONCILIATION.md:17` пусто; `verification/` и `tools/verify_shadow.py` отсутствуют; 6.1 `backlog` (13:12 UTC, `ad2b3f4`). D35 п. 3: waiver D26 не пишется. Ни одного коммита Владислава в `origin/main` | Владислав |
| B2 | **Дата деплоя и слово «деплой»** | **частично закрыт**: дата есть, слова нет | Дата - вт 15.09 (D35 в `main`; D33 в PR #89). «Деплой» - в чате в день релиза (D7, D35 «Не меняется») | Mike (15.09) |
| B3 | **Runbook отстал от `main` и от сервера** | **закрыт** | PR #99 (08:36 UTC): (a) §0 через `proxima-psql-readonly`; (b) §2 `007…018`, `18\|18`; (c) §3 `--date-from 2026-08-27`; (d) §3 импорт от root + `npm ci`, права `/srv/proxima-ai/raw` (§1.3); (e) §1.1 правка `.env` с копией, §7 условный `sed`. Плюс PR #103 (форма `npm run <job>`, монтирование) и PR #110 (§3 проверка флага). Список 15 правок - runbook «Сверка 08.09.2026» | конвейер (сделано) |
| B4 | **Схема боевой базы = 6, кода = 18; ролей спайна нет; RLS выключен** | **закрыт вместе с B3** (ожидаемое состояние до §2) | 13:09 UTC: `max(version)` = `6`; чекаут на `006_…`; runbook §0 проверяет «ровно шесть строк», §2 накатывает 007-018 и ждёт `18\|18`. Ручных накаток не было (`status --short` пуст, тегов нет) | конвейер (через runbook) |
| B5 | **Story 6.4 - роль аналитика для проверки «в течение суток после релиза»** | **открыт** | D33: роль выдаётся с релизом 15.09, пересчёт 16.09; состав грантов LOGIN-роли не предложен (`bmad-architecture`, по образцу `provision-postgres-diagnostics.sh`); 6.4 `backlog` | Mike (роль) / конвейер (грант-лист) |
| B6 | **Копия артефактов бэкфилла в S3** (Given Story 1.5, D15) | **открыт**, `UNKNOWN` | `/var/log/proxima-backup.log` 08.09 03:00 → `OK s3` только для `*.sql.gz.age` (дампы); записи о копии фикстур в `API-FACTS.md`/`INVENTORY.md` нет. Проверить может только владелец S3-ключей | Mike |
| B7 | **Живая сеть WB не разрешена в контуре деплоя** (`WB_ALLOW_LIVE_NETWORK=1`, AD-4). Найден 08.09 после v1: ни `infra/jobs.env`, ни `infra/compose.yaml`, ни runners, ни юниты, ни runbook §3 флаг не задавали; `collect.ts` → `networkTransport()` → `WB_NETWORK_FORBIDDEN` (`transport.ts:34-37`) | **закрыт в `main`** (PR #110, 13:07 UTC); **на сервере проверяется в §3** | `infra/compose.yaml:35-42` - флаг только у сервиса `collector` (jobs.env не подходит: allowlist `SAFE_ENV_KEYS` в `tools/wb_async_report.py` уронил бы `apply-migrations`); гейт `tools/verify_live_network.py` - на `ad2b3f4` 13:14 UTC `live-network: PASS (collector WB_ALLOW_LIVE_NETWORK=1 in infra/compose.yaml; 3 other services, 14 units, 37 test files clean)`; read-only рендер `docker compose … config` с чекаута → `1` (13:12 UTC). Боевой чекаут `fd95fcb` флага не несёт - он приходит с релизным тегом; runbook §3 строка 237: `compose config \| grep -c 'WB_ALLOW_LIVE_NETWORK: "1"'` → `1`, ноль - вернуться к §0 | конвейер (сделано) / оркестратор (§3 на сервере) |
| B8 | **Репетиция на VPS** (D35 п. 2): цепочка §2-§4 до 08.09 ни разу не шла на артефактах 31.08 и живом хвосте | **проведена** 13:10:14-13:11:55 UTC на `main` `1c5e256` (PR #111); результат - §4 | `infra/compose.rehearsal.yaml` (проект `proxima-rehearsal`, postgres `127.0.0.1:5434`, `ports: !override`, `restart: "no"`, `WB_ALLOW_LIVE_NETWORK=${PROXIMA_REHEARSAL_LIVE:-0}`), `tools/rehearsal_run.sh` (`init → up → backfill → tail --live → steps → check → down`), runbook «Репетиция на VPS (D35, не деплой)». `sudo -n docker compose ls` 13:12 UTC: `proxima-rehearsal running(1)` из чекаута `~/orca/proxima-ai-night`, рядом `proxima-ai running(1)` - боевой контур не тронут. Все четыре прогона SUCCEEDED, 2 read-вызова WB выполнены (исключение D35); находки - PR #112 (эталон W10), PR `docs/w35-gate-rule`, B9, B10 | оркестратор (сделано) |
| B9 | **Webapp не читает свой URI-секрет** (найдено на превью репетиции, блокер релиза 2.6): контейнер webapp работает под uid 1001 (`services/webapp/Dockerfile:28, 32` - `useradd --uid 1001 webapp`, `USER webapp`), а `proxima_webapp_uri` создаётся provision'ом как `1010:1010 0600` → «файл `WEBAPP_DATA_DATABASE_URI_FILE` не читается» | **в работе 08.09** (решение Mike: секрет webapp становится `1001:1001` - `provision-runtime-roles.sh`, runbook §1.4, memlog AD-6/AD-15; PR `fix/webapp-secret-uid`) | Превью `/brief` на стенде репетиции (`:3434`, overlay `webapp.staging.compose.yaml`); в 1.14 витрина на фикстурах, поэтому релиз 1.14 не держит, 2.6 - держит | конвейер |
| B10 | **Postgres-режим webapp падает на каждой странице** (найдено там же, блокер 2.6): `MetricStrip` в `services/webapp/src/app/(app)/layout.tsx:40` зовёт `getMetrics()`, который postgres-провайдер отвергает `NOT_IMPLEMENTED` (`postgres-provider.ts:40-43, 243`) → 500 на всех маршрутах в `WEBAPP_DATA_MODE=postgres` | **в работе 08.09** (решение Mike: полоса метрик скрывается, когда провайдер метрики не поддерживает; настоящие метрики - follow-up к 2.5 до 2.6; PR `fix/webapp-metrics-postgres-mode`) | Тот же стенд; в 1.14 не проявляется (фикстуры), в 2.6 - блокер | конвейер |

Рядом, не блокер 1.14: ротация analytics-токена (PA-13 / OQ-10) обязательна **до релиза 2.6** (D32, D35 «Не меняется»; срок в Jira - до 22.09 по D33). В 1.14 `collect` идёт на statistics-токене; боевой `wb_statistics_token` прошёл `assertLeastPrivilegeToken` (read-only бит + только категория statistics, `services/collector/src/business-signal/secrets.ts:59-81`) на живом хвосте репетиции 13:11 UTC (§4) - вопрос закрыт.

## 3. Что конвейер готовит без деплоя - состояние на 13:14 UTC

Нумерация - как в v1 §3.

| # | Пункт v1 | Состояние v2 |
|---|---|---|
| 1 | Тег | Имя определено: `v2026.09.15-2` (runbook строка 38); ставится на `main` пн 14.09 (за сутки); `v2026.09.0-baseline` на `fd95fcb` - в §0 в день релиза. Замечание v1 в силе: тег увозит 2.1-2.5, 3.1-3.3, 4.0-4.3 и PA-65; утро = `collect → norm → brief`, таймеры воронки в 1.14 не включаются (runbook §5 строка 310) |
| 2 | Черновик журнала релиза и `CHANGELOG.md` | **не сделано**: `docs/operations/releases/` нет, `CHANGELOG.md` нет (13:12 UTC) |
| 3 | Обновление runbook (B3) | **сделано** - PR #99, #103, #110; остаток: `proxima-psql-owner` вне `main` (октябрь, runbook строка 402), исполнитель по D7 - эта ветка |
| 4 | `API-FACTS.md` - эталоны W10/W35 | **сделано** - PR #99 (`API-FACTS.md:144-151`) |
| 5 | `WORKS-TODAY.md` - пункты конвейера | **сделано в этой ветке** - WT-34…WT-40, WT-03, NW-2, NW-3; серверная половина (WT-01…WT-18) до релиза не прогонялась целиком - только точки §5 |
| 6 | Dry-run read-only команд §0-§2 | повторён частично 13:09-13:12 UTC (§5): `rev-parse` → `fd95fcb`, `status` пуст, `remote -v` пуст, `tag --list` пуст, `max(version)` = 6, `docker ps` - те же шесть контейнеров (Up 9 days), артефакты на месте. Полный повтор - в день релиза до первой записи |
| 7 | Черновик D33 | D33 записан параллельной сессией в PR #89 (открыт, `docs/analyst-week2-2026-09-08`); D35 в `main` ссылается на него по номеру PR |
| 8 | Story 6.4 - состав грантов | **не сделано** (B5) |
| 9 | *(новое)* Репетиция B8 | инструменты в `main` (PR #111), стенд поднят оркестратором 13:10 UTC; отчёт - §4 |
| 10 | *(новое)* Исполнитель runbook по D7 | **эта ветка**: строка 3 runbook |

## 4. Результат репетиции (B8, D35 п. 2)

Проведена оркестратором 13:10:14-13:11:55 UTC на `main` `1c5e256` (PR #111), стенд `proxima-rehearsal` на этом же VPS; факты ниже - из отчёта оркестратора, `run_id` сокращены до 8 знаков. Порядок шагов - `tools/rehearsal_run.sh` (`up → backfill → tail --live → steps → check`), ожидания - раздел runbook «Репетиция на VPS».

| Шаг | Ожидалось (runbook / `rehearsal_run.sh`) | Получено | Время UTC |
|---|---|---|---|
| `init` | `<root>/secrets` - 14 файлов `1010:1010 0600`, значения не печатаются | выполнен (стенд поднят, `docker compose ls` 13:12 UTC → `proxima-rehearsal running(1)`); число файлов в отчёт не вошло - UNKNOWN | 13:10 |
| `up`: миграции | `apply-migrations` → `migrations: already current` (свежий том); `schema_migrations` → `18\|18` | `already current` (миграции применил initdb-mount при первом старте), `schema_migrations` → `18\|18` | 13:10 |
| `up`: provision ×2 | оба прогона `ok (5 login roles, database proxima_test, …)`, без `WARNING` | оба прогона `ok`, второй ничего не изменил - идемпотентность подтверждена (AC 1.14 T2 на стенде) | 13:10 |
| `backfill` | sha256 пары 31.08 сошлись (`d2f1dc95…`, `0c0318ff…`); `backfill committed`, `run_day: 2026-08-31`; ledger `backfill\|SUCCEEDED` | SUCCEEDED, run `9c1a9d1a…`: `run_day` 2026-08-31, нижняя граница ряда 2026-03-01, 183 дня, `orders_inserted` 13 386, `sales_inserted` 10 675, `nm_subjects` 201, `nm_rows` 36 783 (AD-19: `fact_nm_daily`/`dim_nm_subject` пишутся тем же прогоном) | 13:10-13:11 |
| `tail --live` (2 read-вызова WB) | `"date_from": "2026-08-27"`, `"source": "flag"`, `aggregate`, `kind: collect`, SUCCEEDED; токен прошёл `assertLeastPrivilegeToken` | SUCCEEDED, run `c583eb57…`: ровно 2 read-вызова (`orders`, `sales`, `dateFrom` 2026-08-27, исключение D35 п. 2); orders 353 inserted / 217 skipped, sales 215 / 191 (skipped = идемпотентный `DO NOTHING` по наблюдениям из пары 31.08). **Боевой `wb_statistics_token` прошёл `assertLeastPrivilegeToken`** - read-only, одна категория (закрывает §6 п. 3) | 13:11 |
| `steps` | `norm` и `brief` SUCCEEDED в `collector_runs` | `norm` SUCCEEDED, `brief` SUCCEEDED | 13:11 |
| `check`: W10 | `W10\|649\|700860.00` | `W10\|649\|700860.50` - заказы точно, выручка **равна пересчёту по фикстурам с точностью до копейки**: записанный эталон «700 860» был округлён до рубля (`SPEC.md:33`/`epics.md:29` от 30.08). Гейт по W10 пройден; эталон в `API-FACTS.md` и константа `W10_EXPECTED` в `rehearsal_run.sh` правятся PR #112 (`docs/rehearsal-facts-2026-09-08`) | 13:11 |
| `check`: W35 | на момент прогона - `W35\|225\|263089.00` (снимок 30.08); с 08.09 ~13:40 UTC правило заменено (D35 дополнение, PR #116): дни 24-26.08 против пары 31.08, дни 27-30.08 против последних наблюдений - повторный `check` по новому правилу: 8/8 PASS | `W35\|228\|282836.08` - **разошлось с эталоном 30.08, причина объяснена**: продажи равны паре 31.08; заказы ниже 254 (пара 31.08), потому что `isCancel` у заказов продолжает меняться после снимка (28.08: 7 → 18 отмен), а факты строятся по последним наблюдениям (AD-2/AD-3) - это не ошибка кода, а движение источника. **Решение Mike 08.09 - правило гейта W35:** W10 точно + дни 24-26.08 равны паре 31.08 (заказы 55 / 40 / 28) + дни 27-30.08 равны последним наблюдениям живого хвоста; записывается отдельным PR `docs/w35-gate-rule` (runbook §4, `API-FACTS.md`, скрипт) | 13:11 |
| `check`: ledger | `collector_runs`: `backfill`, `collect`, `norm`, `brief` - все SUCCEEDED; `data_status_current`: `last_full_day` = вчера по МСК, `stale = f`; `norm_daily_current`, `brief_current` - строки за вчера | все четыре прогона SUCCEEDED; `data_status_current`: `last_full_day` 2026-09-07, `stale = false`; `brief_current`: `brief_day` 2026-09-07, `status` ok (= WT-35…WT-37 на стенде) | 13:11 |
| `down` + уборка | `down -v --remove-orphans`, `rm -rf <root>`, образы `proxima-rehearsal-*` удалены; `proxima-ai-postgres-1` с прежним аптаймом | в отчёт не вошло - UNKNOWN (после `down` на стенде ещё смотрели превью webapp - B9/B10); боевой контур по `docker compose ls` 13:12 UTC не тронут | UNKNOWN |
| Находки | что упало / разошлось → единицы в очередь, не правки на сервере | (1) эталон W10 округлён - PR #112; (2) W35: `isCancel` после снимка - правило гейта, PR `docs/w35-gate-rule`; (3) B9 - webapp uid 1001 против секрета `1010:1010`, PR `fix/webapp-secret-uid`; (4) B10 - `MetricStrip`/`getMetrics()` → 500 в postgres-режиме, PR `fix/webapp-metrics-postgres-mode`; (5) §6 п. 1 (пара строк на `/brief` до первого `brief`) на стенде не проверялась - остаётся | - |

Что репетиция **не** проверяет (и это надо помнить при чтении её результата): инкрементальный накат 007-018 поверх живой схемы 6 (свежий том), первый прогон provision до миграций с ожидаемым `WARNING … memberships deferred`, provenance `git_sha`/`image_id` через боевые runners (в репетиции `PROXIMA_GIT_SHA` передаётся явно `run -e`, §6 п. 2), таймеры и алерт (§5-§6 runbook), права боевых секретов после `chown 1010:1010` (§1.2).

## 5. Факты сервера, проверенные 08.09.2026 13:09-13:12 UTC (read-only)

Повтор ключевых строк v1 §5; что не повторялось - помечено «v1».

| Команда | Вывод | Что значит |
|---|---|---|
| `printf 'SELECT max(version) FROM schema_migrations;\n' \| sudo -n /usr/local/sbin/proxima-psql-readonly` (13:09:34 UTC) | `6` | Схема 6, не менялась с v1 (B4, NW-3) |
| `systemctl list-timers --all \| grep -i proxima` | `proxima-host-monitor.timer` (last 13:09:34 UTC) - единственный | Утренних таймеров нет (NW-2, WT-34/WT-38 `NOT APPLICABLE`) |
| `ls /etc/systemd/system/ \| grep proxima` | `proxima-host-monitor.service`, `proxima-host-monitor.timer`, `proxima-tg-bot.service` | `proxima-morning@`, `proxima-restore-check@`, `proxima-alert@` не установлены |
| `systemctl is-enabled codex-conductor.timer; is-active` | `disabled`, `inactive` | Дирижёр выключен (D31/D35) |
| `sudo -n git -C /srv/proxima-ai/repo rev-parse --short HEAD; status --short \| wc -l; remote -v \| wc -l; tag --list` | `fd95fcb`; `0`; `0`; пусто | Боевой чекаут как в v1: detached, чистый, без remote и тегов (runbook §0) |
| `sudo -n ls /srv/proxima-ai/repo/db/migrations/ \| tail -1` | `006_raw_artifact_headers.sql` | Чекаут на 6 миграциях |
| `sudo -n ls -l /etc/proxima-ai/secrets/` (имена/владельцы/права) | 23 файла; `wb_analytics_token` (25.08), `wb_finance_token`, `wb_statistics_token` (13.08) - `-rw------- proxima-admin:proxima-admin`; `postgres_user`, `postgres_password` - `-rw-r----- root:proxima-monitor`; `amirova-test_wb_*` нет | Runbook §1.2 (`mv` + `chown 1010:1010`) не выполнялся; значения не читались |
| `sudo -n stat /srv/proxima-ai/raw` | `No such file or directory` | Каталог §1.3 не создан (ожидаемо) |
| `sudo -n docker ps --format '{{.Names}}\t{{.Status}}'` | `proxima-ai-postgres-1` Up 9 days (healthy); `glitchtip-web/worker/postgres/redis-1`; `proxima-webapp-staging` - все Up 9 days | Боевой контур не перезапускался |
| `sudo -n docker compose ls` (13:12 UTC) | `glitchtip running(4)`; `proxima-ai running(1)` (`/srv/proxima-ai/repo/infra/compose.yaml`); **`proxima-rehearsal running(1)`** (`~/orca/proxima-ai-night/infra/compose.yaml` + `compose.rehearsal.yaml`) | Репетиция B8 поднята оркестратором рядом с боевым проектом; `ls -ld ~/orca/rehearsal` → каталог создан 13:10 UTC |
| `sudo -n tail -4 /var/log/proxima-backup.log` | 08.09 03:00:02-03: `OK local proxima (64K)`, `OK s3 2026-09-08-proxima.sql.gz.age`, `OK local proxima_dev`, `OK s3 …proxima_dev` | Дамп для `restore-check` есть; S3 - только дампы (B6) |
| `ls /etc/cron.d/` | `e2scrub_all`, `openhands-zone-check`, `proxima-pg-backup` | Как в NW-2 |
| `ls ~/signal-inputs/fixtures/wb-api/statistics/{supplier-sales,supplier-orders}/` | обе пары `20260830T…` и `20260831T155341Z__dateFrom-2023-01-01_flag-0.json` на месте (плюс `flag-1` снимки 30.08 и `step0` 31.08) | Вход репетиции и runbook §3 на месте; sha256 - v1 (4/4) |
| `git -C ~/proxima-ai rev-parse --short HEAD; status --short \| wc -l` | `db56429`; `0` | Зеркало `main` на сервере отстало (WT-18), как в v1 |
| `sudo -n env PROXIMA_SECRETS_DIR=<scratch> PROXIMA_RAW_DIR=<scratch> docker compose -f infra/compose.yaml --profile jobs config \| grep -c 'WB_ALLOW_LIVE_NETWORK: "1"'` (чекаут `ad2b3f4`, не сервер) | `1` | B7: релизный compose несёт флаг (runbook §3 ждёт `1`) |
| то же с `PROXIMA_GIT_SHA=<значение>` в окружении, `grep -c PROXIMA_GIT_SHA` | `0` | Provenance не доходит до контейнеров (§6 п. 2) |
| `uv run --python 3.14 python tools/verify_live_network.py` (чекаут `ad2b3f4`, 13:14 UTC) | `live-network: PASS (…)`, exit 0 | Гейт B7 зелёный на `main` |
| `gh pr view 111`; `gh run list --branch main` | #111 MERGED 13:09:45 UTC (`1c5e256`), CI `verify`/`images` на нём `in_progress`; на `ad2b3f4` оба `success` | Голова `main` на момент записи ещё без зелёного CI на #111 |

## 6. Открытые вопросы и кандидаты в единицы (не блокеры 1.14)

1. **`/brief` в режиме «статус есть, сводки нет» показывает противоречивую пару.** `services/webapp/src/lib/data/postgres-provider.ts:200-202`: нет строки в `brief_current` → `status: "no-brief"` с живым `dataStatus`; `brief-summary.tsx:81-88` `numbersWarning()` для любого статуса, кроме `insufficient`, возвращает «Сбор не проходил больше суток», а `brief-summary.tsx:127-131` при свежем `data_status_current` рисует «Данные до …, обновлено …». Оба текста на одном экране, когда `collect` прошёл, а `brief` ещё нет: между §3 runbook и первым утром, между `tail` и `steps` репетиции, при упавшем `brief` на первом утре. AC 1.11/2.5 (`epics.md:308, 418`) этот случай не описывают (там `stale`/пустой view); в 1.14 витрина на фикстурах, так что видно это будет на превью репетиции (`:3434`) и в 2.6. Кандидат - малая правка webapp вне auth-зоны (текст для `no-brief` вроде «Сводка за <дата> ещё не посчитана»), не здесь.
2. **Provenance прогонов на сервере будет `NULL`.** `tools/morning_run.sh:46, 69` и `tools/funnel_v3_run.sh:64` экспортируют `PROXIMA_GIT_SHA`/`PROXIMA_IMAGE_ID` в окружение `docker compose run`, но `infra/compose.yaml` у сервисов `collector`/`control-plane` этих ключей в `environment:` не объявляет (строки 35-42, 62-63), а compose передаёт внутрь только объявленные - рендер `config` с переменной в окружении даёт 0 вхождений (§5). Задания читают их из `process.env` (`collect.ts:137`, `backfill.ts:173`, `funnel-v3.ts:228`; control-plane `norm/run_ledger.py:32`, `brief/run_ledger.py:33`) и пишут `?? null` (`run-ledger.ts:55-56`); колонки `git_sha`/`image_id` в `011_run_ledger.sql:15-16` nullable - прогон не упадёт, но столбцы происхождения будут пустыми. `tools/funnel_csv_run.sh:58, 65` передаёт их явно `--env` - единственный runner, где это работает; репетиция тоже передаёт явно (`run -e`) и гэп не поймает. Кандидат: `PROXIMA_GIT_SHA: ${PROXIMA_GIT_SHA:-}` / `PROXIMA_IMAGE_ID: ${PROXIMA_IMAGE_ID:-}` в `environment:` обоих сервисов (или `--env` в runners по образцу `funnel_csv_run.sh`) + проверка в `tools/tests/test_compose_collector_mounts.py`. Не чинить в этой ветке.
3. ~~Scope боевого statistics-токена~~ - **закрыт репетицией 13:11 UTC**: `wb_statistics_token` (13.08) прошёл `assertLeastPrivilegeToken(token, 'statistics')` (`collect.ts:132`; `business-signal/secrets.ts:59-81`) на живом хвосте (§4) - read-only, одна категория. Остаётся только analytics-токен read-write (PA-13) - ротация до 2.6, в 1.14 не используется.
4. **Инкрементальный накат 007-018 поверх живой схемы 6 нигде не прогоняется.** Репетиция (свежий том), CI `apply-migrations-in-container` (`images.yml:103-110`, `up -d --wait postgres` на пустом томе) и `pg-roundtrip` (WT-30) стартуют с нуля. Боевая база несёт 13 таблиц с данными пилота (WT-04). Кандидат: в репетиционном проекте восстановить дамп `2026-09-08-proxima.sql.gz` (`restore_check.sh` уже умеет читать `/var/backups/proxima/`) и прогнать `apply-migrations` поверх него до 15.09.
5. **`proxima-psql-owner`** (runbook §1.4) создаётся руками в день релиза, в `main` его нет - кандидат в `infra/bootstrap/` (runbook «Октябрь», строка 402).
6. **Журнал релиза и `CHANGELOG.md`** - заготовок нет (§3 п. 2); нужны до 15.09.
7. **Story 6.4** - состав грантов LOGIN-роли аналитика (B5) и процедура выдачи; роль обещана D33 вместе с релизом 15.09.
8. **Копия артефактов в S3** (B6) - за Mike.
9. **D33 и D34 живут в открытых PR** (#89, #98): D35 в `main` ссылается на D33 по номеру PR; пока #89 не смержен, дата 15.09 в `main` держится только этой ссылкой. Смержить #89 до 15.09 (решение Mike).
10. **Токены на сервере под старыми именами** (`wb_*_token`, `proxima-admin`): переименование под AD-13 и `chown 1010:1010` - только в §1.2 runbook после «деплой»; репетиция копирует файл `install`-ом, боевой не трогает.

Первая редакция этого документа - коммит `a1504ab` (PR #93); её §4 «Решение, которое должен принять Mike» закрыт D35 (вариант A по CP-12, дата 15.09).
