# Готовность к релизу: Story 1.14 «Первый релиз M-01 на сервере»

**Дата проверки:** 08.09.2026, 06:21-06:40 UTC. **Кто:** оркестратор Claude Code (режим D31/D32), read-only. **База кода:** `origin/main` `a0c2e37` (08.09.2026 06:20 UTC, CI `verify` + `images` зелёные). **Сервер:** `claudette` (135.106.186.210) под `proxima-admin`, только чтение: `systemctl`, `sudo -n git … log/describe/tag/remote`, `sudo -n ls`, `sudo -n docker ps/inspect`, `sha256sum`, SELECT через `/usr/local/sbin/proxima-psql-readonly` (роль `proxima_diagnostics`). Ничего не запускалось, не останавливалось и не менялось. Значения секретов не читались - только имена файлов и владельцы.

**Источник критериев:** `_bmad-output/planning-artifacts/epics.md`, Story 1.14 (строки 338-350). Каждый пункт Given/When/Then/And ниже процитирован дословно.

**Статусы:** `готово` - подтверждено файлом или выводом команды; `блокер` - релиз не может начаться, пока пункт не закрыт; `частично` - есть заготовка, но условие в формулировке AC не выполнено; `UNKNOWN (до деплоя)` - пункт проверяется только исполнением runbook, проверены лишь предпосылки. Владелец: `конвейер` (оркестратор/воркеры, без деплоя), `Mike`, `Владислав`.

## 1. Пункты AC Story 1.14

| # | Пункт AC (дословно) | Статус | Evidence (файл:строка, команда → вывод) | Владелец |
|---|---|---|---|---|
| G1 | **Given** «Stories 1.0-1.7, 1.9, 1.10, 1.12, 1.13 смержены (1.8 - октябрь, 1.11 - в составе 2.5: правило сжатия)» | готово | `_bmad-output/implementation-artifacts/sprint-status.yaml` (last_updated 07.09.2026 17:52): 1.0-1.10, 1.12, 1.13 `done`; 1.11 `backlog` - влита в 2.5 (PR #69, 03.09 22:58 МСК); 1.8 вышла раньше октября - PR #73, 07.09 15:13 UTC (`HANDOFF.md` строка 13). Мержи: 1.5 #64 03.09 20:35 МСК, 1.7 #68 03.09 22:44, 1.13 #71 04.09 03:47 МСК (`git log --merges origin/main`) | конвейер |
| G2 | «в том числе 1.5 с реальным бэкфиллом (без синтетических строк)» | готово (код) | PR #64 `4d6de35` 03.09.2026: `feat(collector): backfill WB history from CAS artifacts and the live tail` (`8f60ecb`, `394cf8e`); `services/collector/src/jobs/backfill.ts:22` - источник строго `artifact:<sha256_sales>,<sha256_orders>` из CAS; срок «в main не позже 08.09» (D25, решение 5а) выдержан. Фактический прогон на боевых данных - это сам релиз (Then-3) | конвейер |
| G3 | «теневой пересчёт шагов 1-4 и 6 (Story 6.1) выполнен - расхождений нет либо каждое объяснено записью в `docs/state/SHADOW-RECONCILIATION.md` (CP-12)» | **блокер** | `sprint-status.yaml:91` - 6.1 `backlog`; `docs/state/SHADOW-RECONCILIATION.md:17` - таблица пуста (шаблон, ни одной строки); `ls verification/` → каталога нет; `tools/verify_shadow.py` нет; в `Makefile:4` цели `shadow` в `verify` нет. Предусловие 6.1 - конвенции расчёта - закрыто D27 (03.09.2026). Ни одного коммита Владислава в `origin/main` (`git log --format=%an`); ручные шаги онбординга (NDA, приглашения) за Mike - `TASKS.md:54`, статус `UNKNOWN` | Владислав (пересчёт) / Mike (waiver по D26) |
| G4 | «тег `v2026.09.NN-2`» | частично | `git ls-remote --tags origin` → пусто; `git tag --list 'v2026*'` → пусто; `sudo -n git -C /srv/proxima-ai/repo tag --list` → пусто. Правило именования есть (runbook строка 30: «релизный тег ставится на `main` перед релизом»); `NN` = день деплоя из календаря Mike - см. §3 | конвейер (после даты) |
| G5 | «runbook по CP-1» | частично | `docs/operations/release-m01.md` в `main` (Story 1.13, `7fb3394` 04.09; §6 обновлён `a232bdc` 07.09), разделы 0-8 в порядке CP-1. Но runbook отстал от `main` и от сервера - см. блокер B3: §2 ждёт `max(version) = 16` (строки 79, 84), в `main` уже `017_funnel.sql` (`3aad979`, 07.09 16:55 UTC); команда §0 (строка 34) на сервере падает без `PROXIMA_SECRETS_DIR`; `.env` сервера не содержит переменных, которые §7 правит `sed`-ом. По правилу runbook (строка 3: «ответ другой - идти в откат») исполнение «как есть» остановится на §0 | конвейер |
| G6 | «явное «деплой» от Mike (D7)» | **блокер** | Не получено. `DECISIONS.md` D7 (30.08.2026): «Деплой - Claude, после явного «деплой» в чате на каждый релиз»; D32 (08.09): «деплоя нет без слова «деплой»» | Mike |
| G7 | «дата деплоя стоит в календаре Mike - не позже 20.09.2026 при выполненном бэкфилле (13.09 без него) `[ожидает календаря Mike]`» | **блокер** | `DECISIONS.md` D25 «Открыто (решение 1)»: даты ждут календаря Mike; `STATE.md:7`: «ждут подтверждения Mike календарём, иначе семь утр гейта 24-30.09 не помещаются»; `TASKS.md:23`: «1.14 и 2.6 держат даты деплоя в календаре Mike». Даты не назначены | Mike |
| W1 | **When** «Claude выполняет runbook шаг за шагом, фиксируя вывод в `docs/operations/releases/2026-09-NN-m01.md`» | UNKNOWN (до деплоя) | `ls docs/operations/` → каталога `releases/` нет; журнал не начат. Заготовку можно подготовить сейчас (§3) | конвейер |
| T1 | **Then** «миграции применены полностью» | UNKNOWN (до деплоя) | Боевая база 08.09: `SELECT version, name FROM schema_migrations` → `1..6`, `max(version) = 6`; `sudo -n ls /srv/proxima-ai/repo/db/migrations/` → `001…006`. В `main`: `ls db/migrations/` → `001…017` (ожидают применения 007-017, а не 007-016 как в runbook §2). Локально/CI: job `images` на `a0c2e37` зелёный (миграция в контейнере, `HANDOFF.md:22`) | конвейер |
| T2 | «provision идемпотентен» | UNKNOWN (до деплоя) | Скрипт `infra/bootstrap/provision-runtime-roles.sh` в `main`; CI `pg-roundtrip` печатает `provision: idempotent (2 runs)` (`WORKS-TODAY.md` WT-30, CI run 33588868397, 02.09.2026). На боевой: роли `proxima%` = `proxima` (superuser), `proxima_dev`, `proxima_diagnostics`; ролей спайна нет; `pg_policies` = 0 (SELECT 08.09) | конвейер |
| T3 | «бэкфилл из артефактов 30.08 + живой хвост SUCCEEDED» | UNKNOWN (до деплоя) | Артефакты на месте и sha256 совпадают с `docs/state/API-FACTS.md` (раздел «Артефакты для бэкфилла»), проверено `sha256sum` 08.09 - все 4 файла, см. §5. Runbook §3 берёт пару **31.08** (`d2f1dc95…`, `0c0318ff…`), AC говорит «30.08» - расхождение формулировок признано в runbook строка 107, факт в API-FACTS. Вторая копия в S3 (Given Story 1.5, D15): `UNKNOWN` - в `API-FACTS.md`/`INVENTORY.md` записи о копии фикстур нет, `/var/log/proxima-backup.log` 08.09 03:00 показывает `OK s3` только для дампов БД | конвейер / Mike (S3) |
| T4 | «`fact_cabinet_daily_current` содержит дни с 02.03.2026 по вчера» | UNKNOWN (до деплоя) | `to_regclass('public.fact_cabinet_daily_current')` → NULL на боевой (08.09); view приходит миграцией `013_fact_cabinet_daily.sql`. Ряд в артефактах начинается с 2026-03-01 (`API-FACTS.md`, абзац после таблицы артефактов) | конвейер |
| T5 | «W10 и W35 совпадают с API-FACTS» | частично | Эталоны W10 = 649 / 700 860 ₽, W35 = 225 / 263 089 ₽ записаны в `epics.md:29` (FR3), runbook §4 строки 143/145, хартии `analyst-vladislav.md:33` (§2.2 шаг 5, ссылка «SPEC CAP-2»). В самом `docs/state/API-FACTS.md` этих чисел **нет** (`grep -n "W10\|W35\|700 860"` → пусто): пункт AC ссылается на файл, в котором эталон не записан. Сверка на боевой - до деплоя невозможна | конвейер (доки) |
| T6 | «`WORKS-TODAY.md` прогнан и дополнен (таймеры активны, `data_status_current`, `restore-check`)» | частично | Последний полный прогон 30.08.2026, 33/33 (`WORKS-TODAY.md`, шапка). Пунктов про `data_status_current`, утренние таймеры и `restore-check` нет (`grep` → только NW-2); WT-03 ждёт `6\|6` с пометкой «после накатки обновить». Серверная половина прогона - read-only, можно выполнить до релиза | конвейер |
| T7 | «план отката записан до первого шага» | частично | Runbook §7 «Откат» (строки 192-224): таймеры → `delete_run.py --dry-run` → `WEBAPP_DATA_MODE=fixtures` → `checkout v2026.09.0-baseline`; `tools/delete_run.py` в `main` (Story 1.7, #68). Точка возврата - тег `v2026.09.0-baseline` - на сервере ещё не создана (`tag --list` пуст), создаётся в §0 в день релиза. Шаг `sed WEBAPP_DATA_MODE` не сработает: в `/srv/proxima-ai/repo/.env` (14.08.2026) такой переменной нет (см. §5) | конвейер |
| T8 | «порядок исполнения - бэкфилл → первый `collect` → включение таймеров (CP-1)» | готово (документ) | Runbook §3 (бэкфилл, живой хвост) → §4 (проверка) → §5 (таймеры «только после SUCCEEDED бэкфилла»), строки 91-175; сам порядок соблюдается при исполнении | конвейер |
| T9 | «утренний прогон `collect` завершается SUCCEEDED и `data_status_current` обновлён не позже 06:30 МСК (PRD FR-22; `brief_current` появляется в релизе 2.6), превышение видно как алерт через `TimeoutStartSec` юнита `proxima-morning@` с `OnFailure` (задача PA-65 sync-brief 03.09, до релиза 1.14)» | UNKNOWN (до деплоя); код готово | PA-65 в `main` - PR #80, 07.09 17:20 UTC. `infra/systemd/proxima-morning@.service:5` `OnFailure=proxima-alert@%n.service`, `:15` `TimeoutStartSec=55min`; `proxima-morning@.timer:5` `OnCalendar=*-*-* 05:30:00 Europe/Moscow`; `tools/proxima_alert.sh:35-36` `retry = 3`, `retry-delay = 5`; цепочка `tools/morning_run.sh:83-85` `collect → norm → brief`. На сервере юнитов нет: `ls /etc/systemd/system/ \| grep proxima` → только `proxima-host-monitor.{service,timer}`, `proxima-tg-bot.service` | конвейер |
| A1 | **And** «три утра подряд `collector_runs` содержит SUCCEEDED `collect` (CAP-1)» | UNKNOWN (до деплоя + 3 дня) | `to_regclass('public.collector_runs')` → NULL на боевой (08.09); таблица приходит `011_run_ledger.sql`. Календарно: три утра после деплоя D - это D+1…D+3; при D = 20.09 (вс) утра 21-23.09, релиз 2.6 «не позже 23.09» (D25) остаётся без запаса | конвейер |
| A2 | «`data_status_current` отдаёт `last_full_day` = вчера и `stale = false` (проверка SQL под `proxima_webapp_readonly` через `ssh -N proxima-db`, пункт `WORKS-TODAY.md`)» | UNKNOWN (до деплоя); пункт WORKS-TODAY - частично | Роль `proxima_webapp_readonly` - `db/migrations/011_run_ledger.sql:5` (NOLOGIN), LOGIN-пользователь - `provision-runtime-roles.sh`; view - `013_fact_cabinet_daily.sql`. На боевой ни роли, ни view нет (08.09). Пункта в `WORKS-TODAY.md` нет - заготовить (§3) | конвейер |
| A3 | «строка статуса на `/brief` появляется в релизе 2.6 (Story 2.5 с объёмом 1.11)» | готово (вне объёма 1.14) | Story 2.5 `done` (`sprint-status.yaml:63`, PR #69 03.09). Проверяется в релизе 2.6, не здесь (runbook строка 158) | конвейер |
| A4 | «не пришёл - откат по плану» | готово (план) | Runbook §7; см. T7 и оговорку про `WEBAPP_DATA_MODE` | конвейер |
| A5 | «Владислав в течение суток после релиза повторяет шаги 1-4 и 6 теневого пересчёта против боевых данных (Story 6.4) и заполняет первую строку журнала сверки с кабинетом (`docs/state/CABINET-RECONCILIATION.md`) по дню, закрытому не менее 3 суток назад» | **блокер** (предпосылки) | `sprint-status.yaml:94` - 6.4 `backlog`; `CABINET-RECONCILIATION.md:5` - эталон (экран, фильтр, поле выручки) `UNKNOWN`, строка 9 пуста. Доступ Владислава к боевой базе физически отсутствует: LOGIN-роль аналитика создаёт Mike (D26, хартия §6.2, Story 6.4 Given); на боевой 08.09 ролей спайна нет, RLS выключен (0 политик). Ssh не выдаётся (D26) | Владислав / Mike (роль) |
| A6 | «Mike выполняет одно действие: по пункту `WORKS-TODAY.md` одной командой через `ssh -N proxima-db` видит `last_full_day` = вчера и `stale = false`; экран `/brief` со строкой статуса - в релизе 2.6» | UNKNOWN (до деплоя) | Нужен пункт WORKS-TODAY (A2) и LOGIN-роль `proxima_webapp` с URI-файлом (`provision-runtime-roles.sh:15-18`: `<secrets-dir>/proxima_webapp…`) - появятся в §1 runbook | Mike (после релиза) |

## 2. Жёсткие блокеры

Релиз 1.14 не стартует, пока не закрыты все пункты ниже. Порядок - по тому, кто закрывает.

| # | Блокер | Факт (источник, дата) | Кто закрывает |
|---|---|---|---|
| B1 | **Story 6.1 - теневой пересчёт не начат.** CP-12 требует шаги 1-4 и 6 в журнале; нет ни одной строки, ни каталога эталонов, ни гейта | `SHADOW-RECONCILIATION.md:17` пусто; `verification/` отсутствует; `tools/verify_shadow.py` отсутствует; `sprint-status.yaml:91` `backlog` (проверено 08.09 на `a0c2e37`). Конвенции для 6.1 утверждены D27 (03.09) - предусловие закрыто, работа не начата. Снять условие может только Mike записью в `DECISIONS.md` (D26: «снять блокировку может только Mike записью в `DECISIONS.md` с причиной») | Владислав, либо Mike (waiver, §4) |
| B2 | **Дата деплоя и слово «деплой».** Обе кнопки Mike (D7): даты 1.14/2.6 не в календаре, «деплой» не сказано | D25 «Открыто (решение 1)» (02.09); `STATE.md:7`; `TASKS.md:23`; D32 (08.09): «деплоя нет без слова «деплой»» | Mike |
| B3 | **Runbook отстал от `main` и от сервера** - при буквальном исполнении остановка на §0, а §2 даст «неожиданный» ответ и по правилу runbook уведёт в откат. Расхождения: (a) §0 строка 34 `sudo docker compose -f …/compose.yaml exec -T postgres psql …` → на сервере `error while interpolating secrets.postgres_user.file: required variable PROXIMA_SECRETS_DIR is missing a value` (проверено `sudo -n` 08.09; `.env` сервера этой переменной не содержит, `infra/.env` нет); рабочий read-only путь - `printf 'SQL' \| sudo proxima-psql-readonly`; (b) §2 строки 79, 84: «миграции 007…016», «ожидается `16`» - в `main` 017 (`3aad979`, 07.09 16:55 UTC, Story 3.1); (c) §3 строка 129 `collect --date-from 2026-08-28` против AC Story 1.13 (`epics.md:335`) `2026-08-27`; (d) §3 строка 112 `sudo -u \#1010 npx tsx tools/cas_import.ts` из `/srv/proxima-ai/repo`: каталог `drwxr-x--- proxima-admin:proxima-admin`, `PROXIMA_RAW_DIR=/srv/proxima-ai/data/day1-wb-api` тоже `0750 proxima-admin`, а uid 1010 на хосте не существует (`getent passwd 1010` → пусто) - шага с правами/каталогом в runbook нет; `services/collector/node_modules` в чекауте нет; (e) `.env` сервера от 14.08.2026 без `PROXIMA_SECRETS_DIR`, `WEBAPP_DATA_MODE`; `jobs.env` (`infra/jobs.env`) говорит «`PROXIMA_SECRETS_DIR`/`PROXIMA_RAW_DIR` задаются на VPS при релизе» - шага правки `.env` в §1 нет, а §7 правит `WEBAPP_DATA_MODE` через `sed`, которого нечему менять | конвейер (docs-PR, без деплоя) |
| B4 | **Схема боевой базы = 6, кода = 17; ролей спайна нет; RLS выключен.** Это ожидаемое состояние до релиза (NW-3 в `WORKS-TODAY.md`, `STATE.md:19`), не дрейф: применялись ровно 001-006, ручных накаток 007+ не было. Блокером становится только вместе с B3 (runbook ждёт другой номер) | SELECT через `proxima-psql-readonly` 08.09: `max(version) = 6`, роли `proxima`/`proxima_dev`/`proxima_diagnostics`, `pg_policies` = 0, таблиц 13 | конвейер (через B3) |
| B5 | **Story 6.4 - роль аналитика для проверки «в течение суток после релиза».** Без LOGIN-роли (создаёт Mike) пункт A5 невыполним; Story 6.4 `backlog`, состав грантов не предложен | D26; хартия §6.2; `epics.md` Story 6.4 Given; боевая база 08.09 - ролей нет | Mike (роль) / конвейер (`bmad-architecture` - состав грантов) |
| B6 | **Копия артефактов бэкфилла в S3** - Given Story 1.5 (`epics.md:217`) и D15; подтверждения нет | `API-FACTS.md`, `INVENTORY.md` - записи о копии фикстур нет; backup-лог 08.09 03:00 - `OK s3` только для `*.sql.gz.age`. Статус `UNKNOWN`, проверить может только владелец S3-ключей | Mike |

Не блокер 1.14, но висит рядом: ротация analytics-токена (PA-13 / OQ-10, решение 7а D25) - обязательна **до релиза 2.6** (D32, 08.09), runbook §1 (строка 59) предупреждает, что переименование токен не ротирует. Для 1.14 `collect` идёт на statistics-токене.

## 3. Что конвейер готовит без деплоя

Всё ниже - правки документов в ветках/PR и read-only команды. Ни одна не трогает сервер.

1. **Тег.** Предложение: `v2026.09.NN-2`, где `NN` = день деплоя из календаря Mike (при 13.09 - `v2026.09.13-2`, при 20.09 - `v2026.09.20-2`); ставится на коммит `main`, замороженный за сутки до релиза, и записывается в runbook §0 (строка 27 `<релизный тег>`) и в `CHANGELOG.md`. Точка отката `v2026.09.0-baseline` на `fd95fcb` создаётся на сервере в §0 в день релиза (это запись на сервере - только после «деплой»). Замечание: тег на `main` увозит не только Epic 1 - в `main` уже 2.1-2.5, 3.1-3.3, PA-65 и гигиена #85; утренняя цепочка `morning_run.sh` = `collect → norm → brief`, то есть `norm_daily`/`brief_daily` начнут заполняться с первого утра, а таймеры воронки (`proxima-funnel-v3@`, `proxima-funnel-csv@` в `infra/systemd/`) в 1.14 не включаются (Story 3.4, октябрь, D23).
2. **Черновик журнала релиза и релиз-нот.** Создать `docs/operations/releases/` и заготовку `docs/operations/releases/2026-09-NN-m01.md`: разделы 0-8 по runbook, на каждую команду три поля - «ожидалось / получено / время UTC»; плюс `CHANGELOG.md` в корне (runbook §8, строка 236: тег, дата, состав, ссылка на runbook). Сейчас нет ни каталога, ни файла.
3. **Обновление runbook (закрывает B3).** §0: путь к `schema_migrations` через `proxima-psql-readonly` (или `docker compose --env-file` с `PROXIMA_SECRETS_DIR`); §1: шаг правки `/srv/proxima-ai/repo/.env` (`PROXIMA_SECRETS_DIR`, новый `PROXIMA_RAW_DIR`, `WEBAPP_DATA_MODE`) и права каталогов для uid 1010 (`chown`/`setfacl` на репо и raw-каталог) с указанием, что uid 1010 - номинальный пользователь контейнеров без записи в `/etc/passwd`; §2: «007…017», ожидание `17` (или «последняя миграция `main` на день тега»); §3: единая дата `--date-from` с AC 1.13; §5: явно «таймеры воронки не включаются». Обновить ожидания WT-03 в `WORKS-TODAY.md`.
4. **`API-FACTS.md`:** добавить раздел «Эталоны недельных сумм» с W10/W35 и указанием, откуда числа (полные фикстуры 30.08, метод из SPEC CAP-2) - иначе пункт AC T5 сверять не с чем.
5. **`WORKS-TODAY.md`:** новые пункты с командой и ожидаемым выводом: таймеры `proxima-morning@amirova-test` и `proxima-restore-check@amirova-test` активны; `data_status_current` → `last_full_day` = вчера, `stale = false` под `proxima_webapp_readonly` через `ssh -N proxima-db`; последний `restore-check` `Result=success`. Прогнать серверную часть (WT-01…WT-18, read-only) до релиза - можно уже сейчас.
6. **Dry-run read-only команд §0-§2 (выполнено 08.09, повторить в день релиза до первой записи):**
   - `sudo git -C /srv/proxima-ai/repo log --oneline -1` → `fd95fcb Revert "fix: enforce READ-only Analytics token…"` (совпало с ожиданием §0);
   - `sudo git -C /srv/proxima-ai/repo remote -v`, `tag --list`, `status --short` → пусто, пусто, пусто (remote отсутствует, тегов нет, дерево чистое);
   - `printf 'SELECT version, name FROM schema_migrations ORDER BY version;\n' | sudo proxima-psql-readonly` → `1..6` (ожидание §0 «ряд 001-006» выполнено);
   - `sudo ls -la /etc/proxima-ai/secrets/ | grep wb_` → `wb_analytics_token`, `wb_finance_token`, `wb_statistics_token`, все `0600 proxima-admin:proxima-admin` (переименование §1 ещё не делалось; `amirova-test_wb_*` нет);
   - `sudo docker ps --format '{{.Names}}'` → `proxima-ai-postgres-1` (Up 9 days, healthy), `proxima-webapp-staging`, `glitchtip-*` ×4;
   - `sha256sum` четырёх артефактов бэкфилла → совпадение с `API-FACTS.md` 4/4 (§5);
   - `which uv node npx` → `/home/proxima-admin/.local/bin/uv`, `/usr/bin/node` (v22.23.2), `/usr/bin/npx` - toolchain §2/§3 на хосте есть.
   Команды §2 (`build`, `apply-migrations`, `up -d`) - запись, в dry-run не входят.
7. **Черновик записи для `DECISIONS.md` (D33) под решение Mike из §4** - текст готовит конвейер, решение и подпись - Mike.
8. **Story 6.4, подготовительная часть:** предложение состава грантов LOGIN-роли аналитика (`bmad-architecture`, по образцу `provision-postgres-diagnostics.sh`) и процедура выдачи в `docs/operations/` - без создания роли.

## 4. Решение, которое должен принять Mike

Два вопроса, оба нужны до старта runbook.

**4.1. CP-12: ждать Story 6.1 или снять условие.**

- **Вариант A - ждать Владислава.** Story 6.1 закрывается журналом с шестью строками и файлами `verification/golden/` (AC 6.1). Владислав ещё не в репозитории (коммитов нет; NDA/приглашения - ручные шаги Mike, `TASKS.md:54`), темп по D26 - «первая неделя - теневой пересчёт сентябрьской цепочки». Срок неизвестен → `UNKNOWN`; при старте после 13.09 релиз 1.14 в окне ≤ 20.09 под вопросом.
- **Вариант B - waiver CP-12 записью в `DECISIONS.md`.** D26 даёт Mike право снять блокировку записью с причиной. Формулировка: «Релиз 1.14 выпускается без Story 6.1; теневой пересчёт шагов 1-4 и 6 выполняется Владиславом на фикстурах 30.08 (6.1) и на боевых данных (6.4) после релиза; расхождение выше допуска блокирует релиз 2.6, а не 1.14». Цена: первые три утра CAP-1 идут без независимой проверки чисел; W10/W35 сверяются только нашим кодом против эталона runbook §4.
- В обоих вариантах B3 (runbook), B5 (роль аналитика) и B6 (S3) закрываются отдельно.

**4.2. Дата в календаре (D25, решение 1).**

- Бэкфилл в `main` с 03.09, артефакты на VPS и sha256 сверены 08.09 - действует ветка «не позже **20.09.2026** (вс) при выполненном бэкфилле». Ветка «13.09 (вс) без бэкфилла» становится ранним вариантом той же конфигурации, а не другим релизом.
- Арифметика гейта: три утра CAP-1 = D+1…D+3; релиз 2.6 «не позже 23.09 (ср)» (D25); семь утр гейта 24-30.09 (`STATE.md:7`). При D = 20.09 утра 21-23.09 упираются в 23.09 без запаса; при D = 13.09 запас неделя, и утра 14-16.09 успевают до 2.6. Ротация analytics-токена (PA-13) должна лечь между 1.14 и 2.6 (D32).
- Ответ Mike, который нужен конвейеру: дата `D` (13.09 или 20.09, либо другая ≤ 20.09), вариант A/B по 4.1, и в день `D` - слово «деплой» в чате (D7).

## 5. Факты сервера, проверенные 08.09.2026 (06:21-06:40 UTC, read-only)

| Команда | Вывод | Что значит |
|---|---|---|
| `systemctl list-timers --all \| grep -i proxima` | `proxima-host-monitor.timer` (last 06:20:55 UTC, next 06:21:55) - единственный | Утренних таймеров нет (ожидаемо, NW-2) |
| `systemctl is-active proxima-host-monitor.timer` | `active` | Монитор жив |
| `ls /etc/systemd/system/ \| grep proxima` | `proxima-host-monitor.service`, `proxima-host-monitor.timer`, `proxima-tg-bot.service` | Юниты `proxima-morning@`, `proxima-restore-check@`, `proxima-alert@` не установлены |
| `systemctl is-enabled codex-conductor.timer; is-active` | `disabled`, `inactive` | Дирижёр выключен (D31) |
| `sudo -n git -C /srv/proxima-ai/repo log --oneline -1` | `fd95fcb Revert "fix: enforce READ-only Analytics token and drop the temporary RW opt-in (PA-13)"` | Совпадает с runbook §0 и `STATE.md:19` |
| `git -C /srv/proxima-ai/repo describe --tags --always` (и с `sudo -n`) | `fd95fcb` | Тегов нет |
| `sudo -n git -C /srv/proxima-ai/repo rev-parse --abbrev-ref HEAD; status --short; remote -v; tag --list` | `HEAD`; пусто; пусто; пусто | Detached, чистое дерево, remote отсутствует (NW-5) |
| `sudo -n ls /srv/proxima-ai/repo/db/migrations/` | `001_bootstrap.sql … 006_raw_artifact_headers.sql` | Чекаут на 6 миграциях |
| `sudo -n stat -c '%A %U:%G' /srv/proxima-ai/repo /srv/proxima-ai/data/day1-wb-api` | `drwxr-x--- proxima-admin:proxima-admin` ×2 | uid 1010 не прочитает репо и не запишет в raw-каталог (B3-d) |
| `getent passwd 1010` | пусто (exit 2) | Пользователя 1010 на хосте нет |
| `sudo -n ls /etc/proxima-ai/secrets/` | 23 файла: `backup_age_key.txt`, `backup_age_recipient`, `conductor_deploy_key(.pub)`, `glitchtip_*` ×3, `known_hosts_github`, `postgres_*` ×4, `proxima_dev_*` ×4, `s3_access_key`, `s3_secret_key`, `telegram_bot_token`, `telegram_chat_id`, `wb_analytics_token`, `wb_finance_token`, `wb_statistics_token` | Токены под старыми именами; `amirova-test_wb_*` нет |
| `sudo -n ls -la /etc/proxima-ai/secrets/ \| grep wb_` | три файла `-rw------- proxima-admin proxima-admin` (analytics 25.08, finance/statistics 13.08) | §1 runbook (`mv` + `chown 1010`) ещё не выполнялся |
| `sudo -n grep -o '^[A-Za-z_]*=' /srv/proxima-ai/repo/.env` | 12 имён: `WB_STATISTICS_TOKEN_FILE`, `PROXIMA_RAW_DIR`, `WB_ANALYTICS_TOKEN_FILE`, `PROXIMA_SPOOL_DIR`, `POSTGRES_USER_FILE`, `POSTGRES_PASSWORD_FILE`, `POSTGRES_HOST`, `POSTGRES_PORT`, `POSTGRES_DB`, `WB_FINANCE_TOKEN_FILE`, `WB_PRICES_TOKEN_FILE`, `WB_PROMOTION_TOKEN_FILE` (файл 14.08.2026, 0600) | Нет `PROXIMA_SECRETS_DIR`, `WEBAPP_DATA_MODE`; `PROXIMA_RAW_DIR=/srv/proxima-ai/data/day1-wb-api` |
| `sudo -n docker compose -f /srv/proxima-ai/repo/infra/compose.yaml exec -T postgres psql …` (команда runbook §0) | `error while interpolating secrets.postgres_user.file: required variable PROXIMA_SECRETS_DIR is missing a value` | Команда §0 «как есть» не работает (B3-a) |
| `sudo -n docker inspect proxima-ai-postgres-1 --format '{{labels}}'` | config `/srv/proxima-ai/repo/infra/compose.yaml`, working_dir `/srv/proxima-ai/repo/infra`, project `proxima-ai`, image `postgres:16.10-alpine` | Живой контейнер поднят из этого же чекаута |
| `docker ps` (без sudo) | `permission denied … docker.sock` | `proxima-admin` не в группе docker; `sudo -n docker ps` работает |
| `sudo -n docker ps --format '{{.Names}}\t{{.Status}}'` | `proxima-ai-postgres-1` Up 9 days (healthy); `glitchtip-web-1`, `-worker-1`, `-postgres-1`, `-redis-1`; `proxima-webapp-staging` - все Up 9 days | Caddy/overlay нет (NW-4) |
| `printf 'SELECT version, name FROM schema_migrations ORDER BY version;\n' \| sudo -n proxima-psql-readonly` | `1\|bootstrap … 6\|raw_artifact_headers`; `max(version)` = `6` | Схема 6 (совпадает с runbook §0, `STATE.md:19`, WT-03) |
| `… SELECT rolname, rolsuper, rolcanlogin FROM pg_roles WHERE rolname LIKE 'proxima%'` | `proxima\|t\|t`, `proxima_dev\|f\|t`, `proxima_diagnostics\|f\|t` | Ролей спайна (011) и LOGIN-ролей provision нет |
| `… SELECT count(*) FROM pg_policies` | `0` | RLS не включён |
| `… SELECT count(*) FROM pg_stat_user_tables` | `13` | Совпадает с WT-03 |
| `… SELECT to_regclass('public.collector_runs'), …('fact_cabinet_daily_current'), …('data_status_current'), …('stg_wb_orders_obs')` | четыре NULL | Объектов Epic 1 в боевой базе нет |
| `ls ~/signal-inputs/fixtures/wb-api/statistics/{supplier-sales,supplier-orders}/` | sales: `20260830T055939Z__dateFrom-2023-01-01_flag-0.json` (8 766 279 B), `20260831T155341Z__…` (8 818 618 B); orders: `20260830T060041Z__…` (10 854 000 B), `20260831T155341Z__…` (10 902 781 B) | Обе пары артефактов на месте |
| `sha256sum` этих четырёх файлов | `77c99dda…13fc29` (sales 30.08), `d2d0ecfe…aa03e` (orders 30.08), `d2f1dc95…65ec96` (sales 31.08), `0c0318ff…4daa40` (orders 31.08) | Совпадение с `API-FACTS.md` «Артефакты для бэкфилла» 4/4; пара runbook §3 - 31.08 |
| `sudo -n ls /var/backups/proxima/ \| tail -2`; `sudo -n tail -4 /var/log/proxima-backup.log` | `2026-09-08-proxima.sql.gz`, `2026-09-08-proxima_dev.sql.gz`; 08.09 03:00:02-03 `OK local proxima (64K)`, `OK s3 2026-09-08-proxima.sql.gz.age`, `OK local proxima_dev`, `OK s3 …proxima_dev` | Дамп для `restore-check` есть; S3 - только дампы БД |
| `ls /etc/cron.d/` | `e2scrub_all`, `openhands-zone-check`, `proxima-pg-backup` | Как в NW-2 |
| `which uv node npx; node --version` | `~/.local/bin/uv` (и `/usr/local/bin/uv` → `openhands-agent`), `/usr/bin/node` v22.23.2, `/usr/bin/npx` | Toolchain для `make apply-migrations` и `cas_import.ts` на хосте есть |
| `sudo -n ls -d /srv/proxima-ai/repo/node_modules /srv/proxima-ai/repo/services/collector/node_modules` | первый есть, второго нет | После `checkout` тега потребуется `npm ci` (в runbook шага нет) |
| `git -C ~/proxima-ai rev-parse --short HEAD; status --short \| wc -l` | `db56429`, `0` | Зеркало `main` на сервере отстало (WT-18 ждёт равенства с `origin/main`) |
| `gh run list --branch main --limit 4` | `a0c2e37` `verify` success, `images` success (08.09 06:20 UTC) | Голова `main` зелёная |

## 6. Открытые вопросы

1. Вариант A или B по CP-12 (§4.1) и дата `D` (§4.2) - за Mike.
2. Есть ли копия артефактов бэкфилла в S3 (B6) - за Mike, нужен доступ к S3-ключам.
3. Когда Владислав получает GitHub/Jira/фикстуры (`TASKS.md:54`, `ONBOARDING-analyst-week1.md`) - от этого зависит срок 6.1 и выполнимость A5.
4. Кто и когда правит runbook по B3 - предлагается отдельный docs-PR конвейера до даты `D`; ответы на пункты (a)-(e) не требуют решений Mike, кроме нового значения `PROXIMA_RAW_DIR` (каталог сырых артефактов по Conventions AD-15).
