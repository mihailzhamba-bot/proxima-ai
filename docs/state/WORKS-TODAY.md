# WORKS-TODAY - база регрессии PROXIMA AI

**Назначение.** Перечень того, что работает сейчас, с воспроизводимыми шагами и ожидаемым результатом. Это ручная база регрессии до появления автотестов/CI: прогоняется **целиком перед каждым релизом** (деплой staging, накатка миграций, смена контейнеров, правки юнитов/cron). Пункт без воспроизводимых шагов сюда не попадает. Когда пункт покрывается автотестом - вычёркивается с ссылкой на тест.

**Последний полный прогон:** 30.08.2026, сервер 06:10-06:27 UTC, мак 09:20-09:28 МСК. **Результат: 33/33.** Каждый пункт выполнен в этот день, колонка «Результат» - факт, не ожидание.

Как читать:
- **сервер** = команды для shell на `claudette` после `ssh proxima` (user `proxima-admin`, sudo без пароля). Одной строкой с мака: `ssh -o BatchMode=yes proxima '<шаги>'` - шаги написаны так, что внутрь одинарных кавычек вставляются без правок.
- `psqlp "<SQL>"` - helper, вставить один раз в сессию на сервере (только SELECT): `psqlp() { sudo docker exec proxima-ai-postgres-1 sh -c "psql -U \"\$(cat /run/secrets/postgres_user)\" -d proxima -tAc \"$1\""; }`
- **мак** = клон `~/Desktop/Проекты/Proxima/proxima-ai`, cwd = корень. Toolchain на 30.08: Node v22.23.2 / npm 10.9.8, uv 0.11.7 (Python 3.14 ставит сам), homebrew `postgresql@16` (для WT-30). Перед WT-23..33 один раз `PUPPETEER_SKIP_DOWNLOAD=1 npm ci` (`ls node_modules | wc -l` ≥ 400).
- Туннели: в `~/.ssh/config` для `proxima` включён `ControlMaster auto` + `ControlPersist 600`. Фоновые `-f -N -L` форварды живут на мастере: `pkill` клиента их не снимает, `kill` мастера рвёт все сессии. Снимать только `ssh -O cancel -L <spec> proxima`.
- Правила прогона: сервер только чтение (ss/docker ps|inspect|logs/systemctl status/journalctl/curl к loopback/SELECT); WB API - не больше 2 вызовов `seller-info`; секреты не печатать (у `seller-info` в теле `tin` - тело не выводить).

## Пункты

| ID | Что проверяем | Где | Шаги | Ожидаемый результат | Результат 30.08.2026 | Переедет в автотест когда |
|---|---|---|---|---|---|---|
| WT-01 | SSH-доступ и sudo без пароля | сервер | `hostname; id -un; sudo -n true && echo SUDO_OK` | `claudette`, `proxima-admin`, `SUDO_OK` | PASS: claudette / proxima-admin / SUDO_OK | - |
| WT-02 | Postgres пилота: контейнер healthy, порт | сервер | `sudo docker inspect -f "{{.State.Status}} {{.State.Health.Status}}" proxima-ai-postgres-1; sudo ss -tln \| grep -c "127.0.0.1:5432 "` | `running healthy`; `1` | PASS: running healthy (старт 04:55:36Z, RestartCount 0); 1 | smoke на сервере после деплоя (нет CI) |
| WT-03 | База `proxima`: 13 таблиц, ledger миграций | сервер | `psqlp "SELECT count(*) FROM pg_stat_user_tables"; psqlp "SELECT count(*), max(version) FROM schema_migrations"` | `13`; `6\|6` (после накатки 007-010 обновить ожидание на `10\|10` и число таблиц) | PASS: 13; 6\|6 (последняя applied_at 14.08 17:53) | `verify_migrations.py` научится сверять ledger с живой БД |
| WT-04 | Данные пилота не потеряны (заморожены с 25.08) | сервер | `psqlp "SELECT count(*), max(staged_at) FROM stg_wb_nm_report_rows"; psqlp "SELECT count(*) FROM business_signal_runs"; psqlp "SELECT count(*), max(persisted_at) FROM raw_wb_analytics_responses"; psqlp "SELECT count(*) FROM tenants"` | `1220\|2026-08-25 16:33…`; `11`; `7\|2026-08-25 16:33…`; `2` - числа не меньше, даты не раньше | PASS: 1220/25.08 16:33:15; 11; 7/25.08 16:33:15; 2 | запуск ежедневного сбора: ожидание станет «max(staged_at) ≥ вчера» |
| WT-05 | Веб-морда :3000 - маршруты и title | сервер | `for p in / /brief /dashboard /inbox /admin /login /styleguide; do printf "%s %s\n" $p "$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3000$p)"; done; curl -s http://127.0.0.1:3000/brief \| grep -o "<title>[^<]*</title>"` | `/ 307` (Location `/brief`), 6 × `200`; `<title>Бриф · Proxima AI</title>` | PASS: 307 + 6×200; title совпал | curl-матрица/playwright smoke по staging в CI |
| WT-06 | Контейнер webapp: образ, рестарт-политика, fixtures-режим | сервер | `sudo docker inspect -f "image={{.Config.Image}} restart={{.HostConfig.RestartPolicy.Name}} restarts={{.RestartCount}}" proxima-webapp-staging; sudo docker inspect -f "{{range .Config.Env}}{{println .}}{{end}}" proxima-webapp-staging \| grep -E "^(WEBAPP_REQUIRE_AUTH\|NODE_ENV)="; curl -s http://127.0.0.1:3000/brief \| grep -o "fixture-[a-z0-9-]*" \| sort -u \| wc -l` | `image=proxima-webapp-staging:bae976c restart=unless-stopped`; `WEBAPP_REQUIRE_AUTH=false`, `NODE_ENV=production`; `4` | PASS: всё совпало, restarts=0 | после пересборки staging из main - сменить ожидаемый тег; с живыми данными счётчик fixture станет `0` |
| WT-07 | GlitchTip :8080 health + данные | сервер | `curl -s -w " (%{http_code})\n" http://127.0.0.1:8080/_health/; sudo docker exec glitchtip-postgres-1 psql -U glitchtip -d glitchtip -tAc "SELECT (SELECT count(*) FROM projects_project), (SELECT count(*) FROM organizations_ext_organization), (SELECT count(*) FROM issue_events_issue)"` | `ok (200)`; `1\|1\|1` (проектов ≥ 1) | PASS: ok (200); 1\|1\|1 | - |
| WT-08 | OpenHands agent-canvas: юнит и 4 порта | сервер | `sudo -u openhands-agent XDG_RUNTIME_DIR=/run/user/1002 systemctl --user is-active agent-canvas.service; for u in 8000/ 3001/ 18000/alive 18001/health; do printf "%s=%s\n" $u "$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:$u)"; done` | `active`; 4 × `200` | PASS: active (с 04:55:36 UTC); 8000=200 3001=200 18000/alive=200 18001/health=200 | zone-check уже смотрит юнит и :8000; добавить туда 3001/18000/18001 |
| WT-09 | Rootless Docker зоны и контейнер `proxima-dev` | сервер | `sudo -u openhands-agent XDG_RUNTIME_DIR=/run/user/1002 systemctl --user is-active docker-rootless.service; sudo -u openhands-agent DOCKER_HOST=unix:///run/user/1002/docker.sock docker ps --format "{{.Names}} {{.Image}} {{.Status}}"` | `active`; строка `proxima-dev postgres:16-alpine Up …` | PASS: active; proxima-dev postgres:16-alpine Up About an hour | zone-check (`containers` уже там) |
| WT-10 | Egress: SOCKS-туннель + privoxy, сквозной HTTPS | сервер | `systemctl is-active dutch-tunnel privoxy; sudo ss -tln \| grep -cE "127.0.0.1:(1080\|1081) "; curl -s -m 20 --socks5 127.0.0.1:1080 -o /dev/null -w "socks=%{http_code}\n" https://api.github.com/; curl -s -m 20 -x http://127.0.0.1:1081 -o /dev/null -w "privoxy=%{http_code}\n" https://api.github.com/` | `active` ×2; `2`; `socks=200`; `privoxy=200` | PASS: active/active; 2; socks=200 (0.22 с); privoxy=200 | zone-check проверяет юниты/порты, не сквозной HTTP - добавить |
| WT-11 | Reverse-туннель OpenHands (ingress) | сервер | `systemctl show openhands-ingress-tunnel -p ActiveState -p NRestarts` | `ActiveState=active`, `NRestarts=0` | PASS: active (с 04:55:35 UTC), NRestarts=0 | - |
| WT-12 | Host-monitor timer | сервер | `systemctl is-active proxima-host-monitor.timer; systemctl list-timers proxima-host-monitor.timer --no-pager; systemctl show proxima-host-monitor.service -p Result -p ExecMainStatus; sudo stat -c "%y %s" /var/lib/proxima-ai-monitor/state.json` | `active`; LAST ≤ 2 мин назад; `Result=success`, `ExecMainStatus=0`; mtime ≤ 2 мин, размер > 0 | PASS: active; LAST 06:19:03 UTC (58 с); success/0; state.json 06:19:03, 7803 B | - |
| WT-13 | Ночной бэкап в S3 | сервер | `sudo grep -v "^#" /etc/cron.d/proxima-pg-backup; sudo tail -4 /var/log/proxima-backup.log; sudo ls -la --time-style=long-iso /var/backups/proxima/ \| tail -2` | `0 3 * * * root …proxima-pg-backup.sh`; 4 строки за сегодня 03:00: `OK local proxima`, `OK s3 …proxima.sql.gz.age`, `OK local proxima_dev`, `OK s3 …proxima_dev`; `<сегодня>-proxima.sql.gz` ≥ 60 КБ | PASS: 4×OK 30.08 03:00:01-03; 2026-08-30-proxima.sql.gz 64559 B | - |
| WT-14 | Zone-check cron | сервер | `sudo grep -v "^#" /etc/cron.d/openhands-zone-check; sudo tail -2 /var/log/openhands-zone.log` | `*/5 * * * * root /usr/local/sbin/openhands-zone-check.sh`; 2 строки `ok: svc-up …` не старше 5 мин | PASS: 06:15:01 и 06:20:02 `ok: svc-up disk=19% zone-mem=1432M containers=1` | - |
| WT-15 | WB API доступен с сервера, кабинет верный | сервер | `curl -s -m 20 -o /dev/null -w "%{http_code} %{time_total}\n" -H "Authorization: $(cat /etc/proxima-ai/secrets/wb_statistics_token)" https://common-api.wildberries.ru/api/v1/seller-info` (лимит 10/мин; тело не печатать) | `200`, < 2 с; в теле ключи `name/sid/tin/tradeMark`, `name` = «ИП Амирова О. Ф.» | PASS: 200, 0.06 с; ключи и name совпали | `make probe-wb-api` на сервере - когда там появится `uv` |
| WT-16 | Периметр: ufw только 22, sshd без паролей | сервер | `sudo ufw status \| grep -c ALLOW; sudo sshd -T \| grep -E "^(passwordauthentication\|allowusers\|permitrootlogin\|maxauthtries) "` | `2` (22/tcp v4+v6); `passwordauthentication no`, `allowusers proxima-admin`, `permitrootlogin no`, `maxauthtries 3` | PASS: 2; все 4 значения совпали | `verify_vps_contract.py` проверяет контракт, не хост - расширить SSH-пробой |
| WT-17 | Ресурсы хоста | сервер | `df -h / \| tail -1; free -h \| head -2` | `/` < 75 %; available ≥ 4 GiB (пороги zone-check) | PASS: 28G/158G (19 %); available 13 GiB | zone-check уже алертит |
| WT-18 | Зеркало main на сервере чистое и актуальное | сервер | `git -C ~/proxima-ai status --short \| wc -l; git -C ~/proxima-ai rev-parse --short HEAD origin/main` | `0`; два одинаковых хеша | PASS: 0; db56429 = db56429 (= GitHub `main` на 30.08) | - |
| WT-19 | Веб-морда с мака через SSH-туннель | мак | `ssh -o ExitOnForwardFailure=yes -f -N -L 13000:127.0.0.1:3000 proxima && curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:13000/brief; ssh -O cancel -L 13000:127.0.0.1:3000 proxima` | `200`; после cancel `lsof -nP -iTCP:13000 -sTCP:LISTEN` пуст | PASS: 200, title «Бриф · Proxima AI»; cancel снял форвард | - (ручной навсегда) |
| WT-20 | GlitchTip и OpenHands с мака через туннель | мак | `ssh -o ExitOnForwardFailure=yes -f -N -L 18080:127.0.0.1:8080 -L 18000:127.0.0.1:8000 proxima && curl -s -w " %{http_code}\n" http://127.0.0.1:18080/_health/ && curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:18000/; ssh -O cancel -L 18080:127.0.0.1:8080 proxima; ssh -O cancel -L 18000:127.0.0.1:8000 proxima` | `ok 200`; `200` | PASS: ok (200); 200 | - |
| WT-21 | Постоянный туннель OpenHands на маке (launchd) | мак | `pgrep -fl "ssh -N proxima-openhands" \| wc -l; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8000/` | `≥ 1`; `200` | PASS: 1; 200 | - |
| WT-22 | Postgres пилота с мака через туннель | мак | `ssh -o ExitOnForwardFailure=yes -f -N -L 15432:127.0.0.1:5432 proxima && /opt/homebrew/opt/postgresql@16/bin/pg_isready -h 127.0.0.1 -p 15432; ssh -O cancel -L 15432:127.0.0.1:5432 proxima` | `127.0.0.1:15432 - accepting connections` | PASS | - |
| WT-23 | webapp: unit-тесты | мак | `npm --workspace @proxima/webapp test` | exit 0; `Test Files 5 passed`, `Tests 26 passed` | PASS: 5/26, 223 мс | уже автотест; в CI - с появлением workflow |
| WT-24 | webapp: lint | мак | `npm --workspace @proxima/webapp run lint` | exit 0, без warnings | PASS: exit 0, вывода нет | то же |
| WT-25 | webapp: typecheck | мак | `npm --workspace @proxima/webapp run typecheck` | exit 0 | PASS | то же |
| WT-26 | webapp: production build | мак | `npm --workspace @proxima/webapp run build; git checkout -- services/webapp/next-env.d.ts` | exit 0; `Compiled successfully`; 9 маршрутов (`/`, `/_not-found`, `/admin`, `/api/auth/[...all]`, `/brief`, `/dashboard`, `/inbox`, `/login`, `/styleguide`); после checkout `git status --short` пуст | PASS: exit 0 за 5 с, `Compiled successfully in 532ms`; next-env.d.ts откатил | добавить `webapp-build` в `make verify` |
| WT-27 | collector: typecheck + тесты + build | мак | `npm --workspace @proxima/collector run typecheck && npm --workspace @proxima/collector test` | exit 0; `# pass 52`, `# fail 0` | PASS: 52/52, 4 с | уже в `make verify` |
| WT-28 | control-plane + tools: pytest | мак | `uv sync --python 3.14 --project services/control-plane --extra test --locked && uv run --python 3.14 --project services/control-plane --extra test pytest services/control-plane/tests tools/tests -q` | exit 0; `202 passed, 4 skipped` (skip = «dedicated PostgreSQL DSN not configured», их гоняет WT-30) | PASS: 202 passed, 4 skipped, 5.7 с | уже в `make verify` |
| WT-29 | Верификаторы репо (8 гейтов) | мак | `for t in verify_migrations verify_provenance verify_runtime_boundary verify_vps_contract verify_business_signal; do uv run --python 3.14 python tools/$t.py; done; uv run --python 3.14 python tools/secret_scan.py --self-test; uv run --python 3.14 python tools/secret_scan.py; uv run --python 3.14 --project services/control-plane --extra test python tools/verify_contracts.py` | 8 строк `… passed`, все exit 0 | PASS: 8/8 | уже в `make verify` |
| WT-30 | Миграции 001-010 на чистом PG16 (roundtrip) | мак | `bash tools/pg_local_roundtrip.sh` (TMPDIR короче ~60 символов, иначе unix-socket > 103 байт и `pg_ctl` не стартует - воспроизведено 30.08) | `ledger complete (10/10)`, `pg-roundtrip: PASS`, exit 0 | PASS: 4 passed, idempotence OK, 10/10, 2 с | уже в `make verify` |
| WT-31 | Codegen контрактов детерминирован | мак | `npm run codegen:contracts && git status --short services/collector/src/contracts` | 6 строк `codegen: … -> …`; `git status` пуст | PASS: 6 файлов, diff пуст | в `make verify` нет проверки diff - добавить |
| WT-32 | Рендер архитектуры (mermaid) | мак | `npm run architecture:render && ls build/architecture` | `rendered 4 Mermaid sources to SVG and PDF`; 8 файлов (data-flow, delivery, deployment, system × svg/pdf) | PASS: 8 файлов, 5 с | уже в `make verify` |
| WT-33 | `make verify` целиком | мак | `PUPPETEER_SKIP_DOWNLOAD=1 make verify; git status --short` | exit 0; последняя строка `business signal contract verification passed`; дерево чистое | PASS: exit 0 за 40 с (npm ci 12 с), 13 целей, дерево чистое | это и есть автотест; в CI - с появлением workflow |

## Не работает / ожидаемо сломано

| ID | Что | Шаги воспроизведения | Факт 30.08.2026 |
|---|---|---|---|
| NW-1 | `/api/auth/get-session` → 500 (ожидаемо, пока auth выключен) | сервер: `curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:3000/api/auth/get-session; sudo docker logs --since 168h proxima-webapp-staging 2>&1 \| grep -c WEBAPP_AUTH_DATABASE_URI` | `500`; в логах 1 × `Error: webapp: env WEBAPP_AUTH_DATABASE_URI не задан` (05:57:32). Причина: в контейнере нет `WEBAPP_AUTH_DATABASE_URI`, `requiredEnv` бросает; `WEBAPP_REQUIRE_AUTH=false`. Станет регрессией после включения auth (октябрь по WEB-STATE §9) |
| NW-2 | Сбор данных WB не запланирован и не идёт | сервер: `systemctl list-timers --all --no-pager \| grep -E "proxima\|wb\|collect\|signal"; ls /etc/cron.d; sudo crontab -l -u proxima-admin; sudo crontab -l -u root` + WT-04 | единственный таймер `proxima-host-monitor`; cron.d = `e2scrub_all`, `openhands-zone-check`, `proxima-pg-backup`; crontab - «no crontab»; последние данные 25.08 16:33 UTC (5 дней). Внешний потребитель analytics-токена создаёт `detail_history_report` ежедневно ~00:51 UTC (API-FACTS), в БД это не попадает |
| NW-3 | Схема живой БД отстаёт от кода на 4 миграции | мак: `ls db/migrations \| tail -1`; сервер: `psqlp "SELECT max(version) FROM schema_migrations"; ls /srv/proxima-ai/repo/db/migrations \| tail -1` | `010_client_passport.sql` vs `6` vs `006_raw_artifact_headers.sql` (compose пилота монтирует именно `/srv/…/repo/db/migrations`). Локально 10/10 применяются (WT-30). Не чинить без решения (D2, бэклог) |
| NW-4 | Прод-оверлей webapp (Caddy, auth=true) не развёрнут | сервер: `sudo docker ps -a --format "{{.Names}}" \| grep -ci caddy; ls -d /etc/caddy /etc/nginx` | `0`; обоих каталогов нет. `infra/webapp.compose.yaml` + `infra/Caddyfile` из main существуют только в git |
| NW-5 | Серверные чекауты без живого remote - `git pull` невозможен | сервер: `sudo git -C /srv/proxima-ai/repo remote -v \| wc -l; git -C ~/proxima-webapp-staging/repo remote -v \| head -1; ls /tmp/pa49-head.bundle` | `0` (HEAD `fd95fcb` 25.08); `origin /tmp/pa49-head.bundle`; файла нет (HEAD `bae976c` 26.08) |
| NW-6 | Алиас `proxima-app` (LocalForward 3000) на маке неоднозначен | мак: `lsof -nP -iTCP:3000 -sTCP:LISTEN` | порт 3000 занят локальным `wildberries-mcp --http` (pid 842, IPv6). Форвард биндится на IPv4 и `curl localhost:3000/brief` даёт 200 с title Proxima, но зависит от порядка ::1/127.0.0.1. В регрессии - явный `-L 13000` (WT-19) |

## Мост BAD → OpenHands (01.09.2026)

- **WT-BAD-1.** `uv run --python 3.14 --project services/control-plane --extra test pytest tools/tests/test_bad_dev_story.py` - 12 тестов, офлайн: провижин из бандла, ноль remote'ов, гейты (база, контрактные файлы, ancestry, запрещённые пути, неизменность существующих миграций, секрет-скан, грязное дерево, ноль коммитов), коллизия workspace, валидация аргументов. Входит в `make verify` через `tools/tests`.
- **WT-BAD-2.** Гейт Дирижёра: при активном `codex-conductor.timer` вызов `tools/orchestrator/bad_dev_story.sh --preflight-only` обязан вернуть exit 3. Проверено 01.09.2026.
- **WT-BAD-3.** Живой круг проверен 01.09.2026 (Дирижёр остановлен, профиль `fedor`, ветка `chore/bad-smoke` от `81ddddf`): provision → dispatch → wait → collect → fetch, 1 коммит в `refs/openhands/bad-smoke/6/head`, все гейты `pass` кроме `stop_hook: unknown`. Прогон занял ~35 с. Артефакты и workspace удалены после проверки.
- **Факт, найденный этим прогоном:** `.openhands/hooks.json` (стоп-гейт с `make verify`) к разговорам agent-server **не применяется** - `hook_config` равен `null` и у моста, и у воркеров Дирижёра. Красный `make verify` в песочнице никого не остановит; гейт обязан гоняться снаружи (Дирижёр - шаг 3 своего тика, BAD - шаг 4 в `SKILL.md`).
- **Ловушка, стоившая двух попыток:** `working_dir` разговора обязан быть **корнем workspace**, а не чекаутом. Профиль `fedor` - это Codex поверх ACP; стартуя внутри чекаута, он читает проектный `.codex/config.toml` и падает с `ACPInitError ... invalid transport` (та же причина, что в «Known pitfalls» AGENTS.md от 27.08). Поэтому путь к репозиторию должен быть назван в самом промте.

## Не удалось проверить

1. **Telegram-алерты** host-monitor и zone-check - нужен реальный инцидент (запись/останов сервиса запрещены). Проверено косвенно: `state.json` `active: {}`, лог zone-check `ok`.
2. **Восстановление бэкапа из S3** (`s3cmd ls`, age-decrypt, `pg_restore` в пустую базу) - нужны секреты и запись. Проверена только строка `OK s3` в логе (WT-13).
3. **Дальняя сторона reverse-туннеля** (`127.0.0.1:8000` на 153.56.134.240) - на тот хост не заходил; только `ActiveState=active` (WT-11).
4. **WB Statistics `sales`/`orders`, `reportDetailByPeriod`, POST `nm-report/downloads`** - лимит 1/мин на `sales` уже израсходован 30.08, POST = запись. Глубина и лимиты - в `API-FACTS.md` (вызовы 05:59-06:09 UTC того же дня).
5. **GlitchTip UI** (логин суперпользователем) - пароль в секретах; проверены `/_health/` и SELECT-счётчики (WT-07).
6. **OpenHands: реальный прогон задачи агентом** - создаёт workspace и тратит LLM-квоту; проверены только юнит и HTTP-коды (WT-08).
7. **Python-тесты и `make verify` на сервере** - `uv` там нет (INVENTORY п.11); прогнаны на маке (WT-28, WT-33).
8. **Auth-контур при `WEBAPP_REQUIRE_AUTH=true`** и `/login` end-to-end - требует перезапуска контейнера с env и БД-роли `webapp_auth`, которой нет в миграциях (WEB-STATE §4).
9. **`proxima_dev`** (хост и rootless) - по 0 таблиц, назначение неясно (INVENTORY §3.1); регрессионного ожидания нет.
