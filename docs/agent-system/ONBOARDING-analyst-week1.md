# ONBOARDING — внешний аналитик, неделя 1 (2026-09-01 … 2026-09-05)

> План онбординга утверждён Mike 2026-09-01 (грилль-сеанс, 6 решений). Задачи недели: PMM-58 (data-quality аудит), PMM-22 (₽-оценка v1), PMM-59 (data dictionary, первый PR). Аналитик — внешний исполнитель (вайпкодер, работает со своим ноутбуком).

## Зафиксированные решения

| # | Решение |
|---|---|
| 1 | Роль: микс — дни 1-4 аналитика, день 5 первая инженерная задача под присмотром |
| 2 | Поверхность: свой ноутбук + GitHub-клон. SSH к VPS на неделе 1 — ноль |
| 3 | Данные: разовый экспорт от Mike (маскированные cabinet ID / SKU, реальные суммы). NDA подписывается в день 1 до выдачи доступов |
| 4 | Доступы после NDA: GitHub collaborator (Write) + Jira (PA, PMM). Сервер, WB-токены, секреты, OpenHands — не даём |
| 5 | Ритм: ежедневный отчёт 3 строки в Telegram; ревью PR от Mike — в тот же день; эскалация любого вопроса про доступы/сервер — только к Mike |

## День 1 — ручные шаги Mike (~90 минут)

1. NDA: одностраничная взаимная конфиденциальность; подпись до выдачи любого доступа.
2. GitHub: invite collaborator в `mihailzhamba-bot/proxima-ai` (Write, не Admin); проверить branch protection на `main` (запрет direct push и force push).
3. Jira: добавить пользователя в проекты PA + PMM (обычный член, не админ); assign PMM-22 (бриф уже в комментарии 2026-09-01).
4. Выгрузка: `ssh proxima` → `pg_dump` доменных схем → маскирование (cabinet ID и SKU → суррогатные ключи; суммы реальные — покрываются NDA) → zip с паролем, пароль отдельным сообщением. Выгрузка никогда не попадает в репо.
5. Передать аналитику инструкцию ниже (разделы «Инструкция аналитику» — «Ритм»).

## Инструкция аналитику (день 1)

1. Окружение: Node.js 22 (nvm/fnm), uv (Python 3.14), `git clone`, затем `make verify` из корня репо. Зелёный результат обязателен; 1 Docker-зависимый skip — норма.
2. Прочитать: `AGENTS.md` (целиком), `.planning/PRODUCT-VISION.md`, `docs/governance/dod-checklist.md`.
3. Условный допуск: пересказать Mike 5 жёстких запретов из AGENTS.md (секреты; WB WRITE; Torgstat automation; выдуманные данные; destructive ops).
4. Коммиты: атомарные, conventional (`fix:`, `docs:`, …). Работа только в ветках `analyst/<pmm-ключ>-<slug>`; в `main` не пушить никогда.

## Задачи недели

| День | Задача | Definition of Done |
|---|---|---|
| 2-3 | **PMM-58** — data-quality аудит экспорта (EDA: пропуски, аномалии, консистентность provenance, свежесть) | Ноутбук локально (в репо не коммитится) + отчёт `docs/analytics/2026-09-0X-data-quality.md`: цифры из staging с маркировкой `unreleased`, суррогатные ключи |
| 3-4 | **PMM-22** — ₽-оценка «стоимость молчания» v1 (revenue-based, profit v2 после ADR FIN-001) | Методика + расчёт по экспорту + markdown-отчёт с `unreleased`; Jira-комментарий со ссылкой |
| 5 | **PMM-59** — data dictionary доменных таблиц (docs-only, первый PR) | Ветка `analyst/pmm-59-data-dictionary`, `make verify` зелёный, ревью Mike, merge Mike |

Если занятость частичная — PMM-22 съезжает на неделю 2, это нормально.

## Guardrails (почему сервер не сломается)

Доступа к VPS нет вообще — сломать сервер невозможно технически. Дополнительно:

- Запретные зоны правок (AGENTS.md): `infra/`, `Makefile`, `package.json`, `src/lib/auth*`, `src/lib/db/*`, `Dockerfile`.
- Никаких WB-токенов и секретов у аналитика не появляется.
- Выгрузка маскирована; всё, что попадает в репо, проходит ревью Mike.
- Числа в артефактах: только из staging-выгрузки, с маркировкой `unreleased`; реальные cabinet ID / SKU не коммитятся (DoD `docs/governance/dod-checklist.md`).

## Ритм

- Ежедневно до 19:00 отчёт в Telegram: сделал / застрял / дальше.
- Застрял больше 2 часов — эскалация Mike, не буксовать молча.
- PR аналитика ревьюится Mike в тот же день.

## Неделя 2 — кандидаты (не решения)

1. Tunnel-only SSH-юзер (`analyst`, без shell, `PermitOpen localhost:5432`) + read-only роль в PostgreSQL — если аналитика стала ежедневной.
2. PMM-23 (SCN-005, OOS-детектор) — следующая инженерная, если день 5 прошёл чисто.
3. Закрыть пункт onboarding в TASKS.md Queue.

## UNKNOWN (уточнить при онбординге)

- macOS или Linux у аналитика.
- Есть ли собственный GitHub-аккаунт.
- Full-time или частичная занятость.

---

Last updated: 2026-09-01
