# Онбординг Владислава: маршрут первой недели

> Обновлено 03.09.2026. Этот файл - маршрут, а не описание роли. Роль, метод, допуски, зоны кода и права - хартия `docs/agent-system/roles/analyst-vladislav.md` (v2, решение D26). При расхождении прав хартия.

Дни считаются от дня выхода, а не от календарной даты. Занятость по хартии - полный день, 30-40 часов в неделю.

## День 0: ручные шаги Mike (около часа)

1. **NDA** - взаимная конфиденциальность на одну страницу, подпись до выдачи любого доступа.
2. **GitHub** - collaborator в `mihailzhamba-bot/proxima-ai` с правом Write, не Admin. Проверить branch protection на `main`: запрещены прямой push и force-push.
3. **Jira** - добавить в проекты PA и PMM обычным участником. Задачи создаются только после approve таблиц синхронизации (`docs/state/JIRA-SYNC-BRIEF-2026-09-03.md`).
4. **Кабинет WB** - доступ только на просмотр, без API и без выпуска токенов.
5. **Фикстуры** - передать копию **только** каталога `fixtures/wb-api/` по §6.3 хартии. Уровнем выше лежат файлы токенов, поэтому `~/signal-inputs/` целиком не копируется никогда.

Что не выдаётся ни в каком виде: ssh на сервер, токены WB и Telegram, ключи бэкапа и хранилища, sudo, права записи где-либо на сервере. Доступ к боевой базе на чтение сегодня выдать нечем: схема версии 6, RLS выключен, ролей спайна нет. Роль аналитика заводится отдельной единицей (Story 6.4) после релиза 1.14.

## День 1: контекст и окружение

Чтение по порядку: `STATE.md` → `DECISIONS.md` (D1-D27) → PRD §1-§4 → хартия целиком → `docs/operations/dev-onboarding.md`.

Окружение по `dev-onboarding.md`: Node 22, uv с Python 3.14, `make verify` из корня. Зелёный результат обязателен. Без локального PostgreSQL 16 шаг `pg-roundtrip` печатает `SKIP` и выходит с кодом ноль - это ожидаемо, но означает, что миграции локально не проверены.

Условие допуска к работе: пересказать Mike пять жёстких запретов из `AGENTS.md` своими словами.

## Дни 2-3: шаги 1-4 теневого пересчёта

Шаги и допуски - таблица §2.2 хартии. Считать своим кодом на своём стеке, наши функции не переиспользовать: вход только исходные данные.

Конвенции расчёта **уже приняты** решением D27 от 03.09 и предлагать их заново не нужно. Они лежат в `glossary.md`, раздел «Конвенции расчёта», и продублированы в контрактах `norm` и `brief`: медиана при чётном числе точек - среднее двух средних; деньги half-up до двух знаков строкой; `deviation_pct` до 0.1 п.п. один раз на последнем шаге после деления; недели по ISO-8601. Если какое-то из правил на практике окажется неудобным или неверным, это задача в Jira и правка D27, а не молчаливое отклонение.

Ограничение, задающее порядок работ: деньги в обезличенных фикстурах репозитория умножены на секретный коэффициент и строки прорежены, поэтому недельные суммы W10 и W35 по репозиторию невоспроизводимы. Сначала эталоны на синтетике, недельные суммы - на полных фикстурах локально.

## Дни 3-4: шаги 6-7 и первые эталоны

Норма на окне 14 дней и отклонение со статусами. Результат - файлы в `verification/golden/` по формату §2.3 хартии и строка на каждый шаг в `docs/state/SHADOW-RECONCILIATION.md`. Это содержание Story 6.1 (`epics.md`, Epic 6).

Story 6.1 по CP-12 стоит предусловием релиза 1.14, поэтому её результат нужен раньше даты деплоя, а не к концу месяца.

## День 5: сверка и заготовка инструмента

Первая сверка сводки с кабинетом WB по §3.1 хартии: день, закрытый не менее трёх суток назад; эталонный экран и фильтр фиксируются первой строкой `docs/state/CABINET-RECONCILIATION.md` и дальше не меняются без записи. Допуск до решения Mike - `[ASSUMPTION]` ±1 заказ и ±0.5 % выручки.

Заготовка `tools/retro_alarms.py` для Story 6.3: только чтение полных фикстур заказов, без сети и без базы.

## Границы правок

Свои зоны, куда агенты не заходят: `verification/`, собственные инструменты (`tools/verify_shadow.py`, `tools/retro_alarms.py`), четыре журнала в `docs/state/`, истории Epic 6.

Не берётся в первую неделю: критический путь сентября (истории 1.4-1.14, 2.3-2.6, 3.1) и единицы с пометкой `[Claude]`. Существующие `tools/verify_*.py` не правятся: гейт, который можно было бы ослабить, меняется чужими руками.

Процесс тот же, что у агентов: ветка от `origin/main`, PR, агент-ревьюер, зелёный `make verify`, мерж делает Mike.

## Ритм

Отдельного еженедельного отчёта нет: журналы и задачи и есть отчёт. Строка в журнал пишется в день прогона, даже если всё сошлось. Расхождение выше допуска - задача в Jira с меткой блокера, сообщение Mike и John в тот же день, и релиз не выпускается, пока расхождение не закрыто.

Застрял больше двух часов - эскалация Mike, не буксовать молча.

## Что остаётся неизвестным

- Операционная система и наличие своего GitHub-аккаунта.
- Его собственный кабинет WB как второй набор данных: есть ли доступ, чьи данные, на каких условиях обезличенная выгрузка (решение Mike, PRD §8.0 пункт 13).
- Допустимы ли реальные дневные суммы кабинета в репозитории (PRD §8.0 пункт 12). До решения эталон недельных сумм закрывается только локальным прогоном.
- Нужен ли доступ к тестовой базе `proxima_test` в дополнение к боевой на чтение.
