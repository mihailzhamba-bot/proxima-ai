#!/usr/bin/env bash
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$ROOT"
exec uv run --python 3.14 --project services/control-plane python -m tools.now_orchestrator "$@"
