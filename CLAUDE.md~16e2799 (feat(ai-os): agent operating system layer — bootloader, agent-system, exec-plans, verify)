# PROXIMA AI — Claude Code adapter

@AGENTS.md

<!-- Claude-specific instructions ONLY. Canonical rules live in AGENTS.md — do not duplicate them here. -->

- History: `CLAUDE.md` was previously a symlink to `AGENTS.md`; since 2026-08-16 it is a real adapter file that imports `@AGENTS.md`.
- Project MCP servers for Claude Code are defined in `.mcp.json` (context7 for current library docs; Postgres MCP Pro in restricted read-only mode via `DATABASE_URI` from the environment). Never place credential values in configs or Git.
- Cross-session memory loop (per MILV global rules): append significant-task learnings to `~/.claude/projects/-Users-mikezhamba-Desktop-MILV/memory/`; verify memory facts against repo files before acting on them.
- Communication language: Russian-first; code identifiers and agent-system docs in English (see AGENTS.md).
