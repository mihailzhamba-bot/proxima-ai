# Сверка PRD v2 с артефактами исполнения (epics / sprint-status / Jira-sync / backlog)

- **Дата сверки:** 02.09.2026
- **Проверяемый документ:** `_bmad-output/planning-artifacts/prds/prd-PROXIMA-AI-2026-08-28/prd.v2.md` (828 строк)
- **Источники истины (D17):** `_bmad-output/planning-artifacts/epics.md` (574 строки), `_bmad-output/implementation-artifacts/sprint-status.yaml`, `docs/state/JIRA-SYNC-2026-08-31.md`, `docs/state/BACKLOG-REVIEW.md`, `STATE.md`, `DECISIONS.md` (D14, D23, D24)
- **Режим:** read-only, PRD не изменялся.

## 0. Какая версия sprint-status.yaml использована

Рабочая копия (ветка `sunfish`) **устарела** относительно `origin/main`:

| | рабочая копия | `origin/main` (коммит `b2c3861`, 02.09.2026 03:50) |
|---|---|---|
| `last_updated` | `08-30-2026 19:45` (`sprint-status.yaml:32`) | `09-02-2026` |
| Story 1.3 | `in-progress` (`sprint-status.yaml:42`) | `done` |
| Story 2.2 | `in-progress` (`sprint-status.yaml:58`) | `done` |
| Итого done | 7 | **9** |

То же и со `STATE.md`: рабочая копия — «Обновлено: 01.09.2026 … Готово 7/22 единиц», `origin/main` — «Обновлено: 02.09.2026 (VPS, Claude Code): влиты #48 (Story 1.3 …) и #46 (Story 2.2 …) - 9/22 единиц сентября».

**Для всей сверки использована версия `origin/main`** — она новее. Файлы `epics.md`, `JIRA-SYNC-2026-08-31.md`, `BACKLOG-REVIEW.md` в рабочей копии и на `origin/main` идентичны (проверено `diff`).

Следствие: **все расхождения по статусам между PRD и рабочей копией — дефект репо-файла, а не PRD** (см. F1).

---

## 1. Статусы историй (PRD §1, §6.1, §10.1)

Проверены все 32 истории. **Ошибочных статусов в PRD не найдено** — против `origin/main` совпадает всё: Epic 1 done = 1.0, 1.1, 1.2, 1.3, 1.9, 1.10, 1.12 (7/15); Epic 2 done = 2.1, 2.2 (2/6); Epic 3 = 3.1 backlog; Epic 4/5 = backlog. Очередь `1.4 → 1.5 → 1.6 → 1.7 → 1.8 → 1.11 → 1.13 → 1.14` (`prd.v2.md:544`) корректна, «следующая 1.4» (`prd.v2.md:98`) совпадает со `STATE.md` («Следующие единицы: 1.4 (от 1.3) и 2.3 (от 2.2)»).

### F1. Рабочая копия репо отстаёт от PRD на две истории — **Важно** *(дефект репо-файла, не PRD)*

- **PRD:** `prd.v2.md:93` «Таблица ступеней (статусы по `sprint-status.yaml` и `STATE.md` на 02.09.2026)»; `prd.v2.md:106` «выполнено 9 на 02.09.2026 (`STATE.md`)»; `prd.v2.md:24` (frontmatter) «`sprint-status.yaml` (02.09.2026)».
- **Источник:** `_bmad-output/implementation-artifacts/sprint-status.yaml:32` `last_updated: 08-30-2026 19:45`, `:42` `1-3-…: in-progress`, `:58` `2-2-…: in-progress`; `STATE.md:5` «Готово 7/22 единиц сентября».
- **Вердикт:** PRD прав, устарела ветка. Данные PRD подтверждаются `origin/main:sprint-status.yaml` и `origin/main:STATE.md`.
- **Фикс:** подтянуть в ветку `sunfish` `sprint-status.yaml` и `STATE.md` с `origin/main`; в frontmatter PRD указать точный источник статусов — `origin/main@b2c3861`, а не просто дату.

### F2. Строка «FR-28 реализовано частично (Story 1.10 done)» переоценивает готовность — **Незначительно**

- **PRD:** `prd.v2.md:257` «Статус: `реализовано` частично (Story 1.10 провайдер done), `сентябрь` (1.6, 1.11)».
- **Источник:** `epics.md:273` «принять … ветку `origin/ai/pa-50` … **без реализации SQL**»; `epics.md:280` «postgres-режим бросает `NOT_IMPLEMENTED`». Статус данных на `/brief` целиком реализуется Story 1.11 (`epics.md:283-295`).
- **Фикс:** заменить на «`реализовано` частично: принят каркас провайдера (Story 1.10 done, SQL не реализован); `сентябрь` (1.6, 1.11)».

### F3. FR-6: история 1.12 указана в группе `сентябрь`, хотя она done — **Незначительно**

- **PRD:** `prd.v2.md:314` «Статус: `реализовано` частично (Story 1.3), `сентябрь` (1.6, 1.11, 1.12 done)».
- **Источник:** правило статусов `prd.v2.md:78`: «`реализовано` (истории done в `sprint-status.yaml`…), `сентябрь` (в 22 единицах D23)»; 1.12 = done в `origin/main:sprint-status.yaml`.
- **Фикс:** «`реализовано` частично (1.3, 1.12), `сентябрь` (1.6, 1.11)».

### F4. §1: «M-02 … backlog (после 2.2)» — предпосылка уже снята — **Незначительно**

- **PRD:** `prd.v2.md:100` «M-02 База нормы | … | 2.3 | backlog (после 2.2)».
- **Источник:** 2.2 = done (`origin/main:sprint-status.yaml`); `origin/main:STATE.md` «Следующие единицы: 1.4 (от 1.3) и **2.3** (от 2.2)».
- **Фикс:** «backlog; следующая после 2.2 (готова к запуску)» — по аналогии со строкой M-01 «следующая 1.4».

---

## 2. Story → эпик → ключ Jira (PRD §10.1 против JIRA-SYNC-2026-08-31)

**Все 22 ключа сентября сверены построчно — ошибок нет.** Ни одного выдуманного, ни одного пропущенного, ни одной неверной привязки к эпику:

`PMM-43`(1.0), `PMM-44`(1.3), `PMM-45`(1.4), `PMM-46`(1.5), `PMM-47`(1.6), `PMM-48`(1.10), `PMM-49`(1.11), `PMM-50`(1.14) → `PMM-38`; `PMM-51`(3.1) → `PMM-39`; `PMM-52`(2.3) → `PMM-40`; `PMM-53`(2.1), `PMM-54`(2.2), `PMM-55`(2.4), `PMM-56`(2.5), `PMM-57`(2.6) → `PMM-3`; `PA-57`(1.1), `PA-58`(1.2), `PA-59`(1.7), `PA-60`(1.8), `PA-61`(1.9), `PA-62`(1.12), `PA-63`(1.13) → `PA-36`. Совпадает с `JIRA-SYNC-2026-08-31.md:20-43` и с D24 (`DECISIONS.md:188`). Список эпиков во frontmatter (`prd.v2.md:12-19`) совпадает с `JIRA-SYNC:7-16`.

### F5. Столбец «Задачи и состояние» смешивает статус истории и статус Jira — **Инфо**

- **PRD:** `prd.v2.md:634` «PMM-43 (1.0 [Claude], **done**)», `:635` «PA-57 (1.1, **done**)».
- **Источник:** `JIRA-SYNC-2026-08-31.md:64` «Статусы PMM-43 (Story 1.0) и PA-57 (Story 1.1) оставлены в Backlog/К выполнению - переводить по шагу 10 конвейера после мержа PR #36/#37».
- **Вердикт:** не ошибка — `prd.v2.md:627` прямо оговаривает лаг Jira. Но столбец читается как статус задачи Jira.
- **Фикс:** переименовать столбец в «Задачи и состояние истории (`sprint-status.yaml`)» либо пометить `done*` со сноской на лаг.

---

## 3. Мэппинг требований: PRD FR-26..FR-35 ↔ epics FR1..FR12 / NFR / AR

Сверено с `epics.md` «Requirements Inventory» (`:24-70`) и «FR Coverage Map» (`:78-91`).

Корректные ссылки (проверены дословно): FR-26 → epics FR1, FR2 ✓ (`prd.v2.md:236`); FR-27 → FR3, NFR9 ✓ (`:248`); FR-28 → FR4, FR5 ✓ (`:257`); FR-29 → FR10, AR9 ✓ (`:268`); FR-30 → FR8, FR9 ✓ (`:277`); FR-31 → FR6 ✓ (`:292`); FR-32 → FR7 ✓ (`:302`); FR-22 → AR3 ✓ (`:331`).

### F6. §10.1 привязывает FR-29 и FR-22 не к тем эпикам; §13 говорит обратное — **Важно**

- **PRD:** `prd.v2.md:634` «M-01 Сбор и хранение | **PMM-38** | FR-26, FR-27, FR-28, **FR-29**, FR-6 | … | PMM-43 (1.0), PMM-44 (1.3), PMM-45 (1.4), PMM-46 (1.5), PMM-47 (1.6), PMM-48 (1.10), PMM-49 (1.11), PMM-50 (1.14)»; `prd.v2.md:638` «M-03 Сводка | **PMM-3** | FR-32, FR-7, **FR-22** | … | PMM-53 (2.1) … PMM-57 (2.6)».
- **Источник:** FR-29 реализует только Story 1.7 (`prd.v2.md:268`), а 1.7 = `PA-59` под `PA-36` (`JIRA-SYNC:39`). FR-22 реализуют Stories 1.12 и 1.14 (`prd.v2.md:331`), то есть `PA-62`/`PA-36` (`JIRA-SYNC:42`) и `PMM-50`/`PMM-38` (`JIRA-SYNC:29`). Ни одна из задач `PMM-53..57` не реализует FR-22.
- **Внутреннее противоречие PRD:** `prd.v2.md:766` (§13) ставит FR-22 и FR-29 в строку **M-01**, а `prd.v2.md:769` (M-03) даёт только «FR-32, FR-7».
- **Фикс:** в §10.1 перенести FR-29 в строку «Конвейер лестницы / PA-36», а FR-22 — в строку M-01 с оговоркой «шаги `norm`/`brief` таймера добавляются 2.5/2.6»; привести §10.1 в соответствие с §13.

### F7. epics FR11 и FR12 не покрыты ни одной ссылкой PRD — **Важно**

- **PRD:** ни в одном требовании §4.C/§4.D нет цитаты `epics FR11` / `epics FR12` (проверено grep по всему файлу: цитируются только epics FR1-FR10, NFR3/5/9/10/11/12/13, AR3/AR9/AR16).
- **Источник:** `epics.md:34` «FR11 (октябрь, M-04): Система отделяет сигнал от шума и ранжирует отклонения по потерянной выручке в разрезе SKU и категории. [CAP-7]»; `epics.md:35` «FR12 (октябрь, M-05): К каждой аномалии система даёт гипотезу причины и что проверить, с источником каждой цифры. [CAP-8]».
- **Вердикт:** содержательно покрыто (FR-1/FR-8/FR-9/FR-34 и FR-4/FR-14), но трассировка разорвана — §0.2 (`prd.v2.md:79`) обещает цитирование как `epics FRn`.
- **Фикс:** добавить `epics FR11` в «Источники» FR-1, FR-8, FR-9, FR-34 и `epics FR12` — в FR-4, FR-14.

### F8. §10.1 «epics NFR1..NFR13, AR1..AR11» под PA-36 оставляет AR12-AR16 без строки — **Незначительно**

- **PRD:** `prd.v2.md:636` «Конвейер лестницы | PA-36 | epics NFR1..NFR13, AR1..AR11 | - | …».
- **Источник:** `epics.md:91` «NFR1-NFR13 действуют во всех эпиках; AR1-AR11 - Epic 1; **AR7, AR12, AR13 - Epic 2**; AR5, AR6 (async CSV), AR12 (`wb_async_report.py`) - Epic 3; AR14 - Epic 2 … и Epic 4; AR15 - после Ворот 3 (Jira); AR16 - каждый релиз».
- **Вердикт:** «AR1..AR11 → PA-36» верно, но строки M-02/M-03/M-01b в §10.1 в столбце «Требования PRD» не несут AR7/AR12/AR13/AR14, а AR15/AR16 не имеют дома вовсе.
- **Фикс:** добавить в строки M-02/M-03 «epics AR7, AR12, AR13, AR14», в M-01b — «AR5, AR6, AR12», в примечание к таблице — AR15 (§10.2) и AR16 (§15).

### F9. epics.md сам двоит AR5/AR6 между Epic 1 и Epic 3 — **Незначительно** *(дефект репо-файла)*

- **Источник:** `epics.md:91` «AR1-AR11 - Epic 1; … AR5, AR6 (async CSV) … - Epic 3». AR5 (WB-клиент) и AR6 (живые проверки) приписаны одновременно двум эпикам.
- **Фикс (в epics.md, не в PRD):** «AR1-AR4, AR7-AR11 - Epic 1; AR5, AR6 - Epic 1 (v3) и Epic 3 (async CSV)».

### F10. FR-6 не цитирует ни одного epics FRn, дублируя epics FR4/FR5 — **Незначительно**

- **PRD:** `prd.v2.md:314` «Источники: AD-3, AD-7, AD-17; CAP-3».
- **Источник:** `epics.md:27-28` (FR4, FR5) описывают то же поведение; FR-28 их уже цитирует — получается два требования PRD на одну пару epics FR без разграничения.
- **Фикс:** дописать «epics FR4, FR5 (инженерная сторона)» либо явно указать, что FR-6 — это NFR-надстройка над FR-28 (алерт + FAILED-прогон), а не дубль.

### F11. §10.1 строка «Вне лестницы» неполна по ключам — **Незначительно**

- **PRD:** `prd.v2.md:642` «Вне лестницы | - | FR-2, FR-3, FR-10, FR-11, FR-13, FR-17, FR-19, FR-23, FR-24 → CM-n | - | PMM-23, PMM-27, PMM-34 (Готово), PMM-36, PA-44, PA-53».
- **Источник:** §4.E (`prd.v2.md:486-494`) отправляет FR-2/FR-3/FR-24 → CM-3, FR-19 → CM-7; ключи CM-3/CM-7 по §12 (`prd.v2.md:716`, `:720`) — `PMM-35`, `PMM-24`, `PA-51`. Их в строке нет.
- **Фикс:** добавить `PMM-24`, `PMM-35`, `PA-51` в столбец ключей.

---

## 4. Ссылки «Story N.M»: существование и соответствие Given/When/Then

Все номера историй, упомянутые в PRD (1.0-1.14, 2.1-2.6, 3.0-3.4, 4.1-4.3, 5.1-5.3), **существуют** в `epics.md` (32 заголовка `### Story`). Выдуманных историй нет. Числовые факты, перенесённые в PRD, сверены и совпадают: W10 = 649 / 700 860 ₽ и W35 = 225 / 263 089 ₽ (`prd.v2.md:251` ↔ `epics.md:26`), норма 34.5 / 34 595 ₽ (`prd.v2.md:295` ↔ `epics.md:29`), `dateFrom = min(run_day − 3, last_full_day + 1)` (`prd.v2.md:242` ↔ `epics.md:223`), 8 недель воронки к 27.10 (`prd.v2.md:280` ↔ `epics.md:104`), 201 nmId = 11 вызовов при пакетах ≤ 20 (`prd.v2.md:281` ↔ `epics.md:445`), quality-check «кабинетный ряд против суммы nmId» (`prd.v2.md:356` ↔ `epics.md:503`), `SignalRow` из каркаса PA-49 (`prd.v2.md:390` ↔ `epics.md:529`), порог в `DECISIONS.md` до реализации (`prd.v2.md:401` ↔ `epics.md:514`).

### F12. FR-20 приписан Story 4.2 поведение, которого в её AC нет — **Важно**

- **PRD:** `prd.v2.md:371` «Система хранит и применяет правила и пороги с метаданными: глобальные значения по умолчанию в конфигурации и переопределения по кабинету в паспорте клиента; правило без источника не применяется. Статус: … `октябрь` (**применение в 4.2**)»; consequences `:374-377` (5 полей метаданных, уровни, CSV-конфиг по паттерну `products.csv`).
- **Источник:** Story 4.2 (`epics.md:506-517`) целиком: **Given** «решение Mike по порогу … записано в `DECISIONS.md`»; **When** «`brief` собирает `signals[]` из результатов детектора»; **Then** «дни в пределах шума (CV) не попадают; список отсортирован по `rub_assessment`; `brief.status` и правило показа цифр не меняются». Ни паспорта клиента, ни метаданных правила, ни CSV-конфига в AC нет.
- **Фикс:** либо добавить в Story 4.2 AC-пункт «пороги читаются из паспорта клиента с 5 полями метаданных», либо (предпочтительно, чтобы не расширять октябрьскую историю) завести отдельную Story 4.4 «Policy Layer: пороги с провенансом» и сослаться на неё в FR-20.

### F13. FR-21 не имеет ни одной истории — **Важно**

- **PRD:** `prd.v2.md:379-386` «FR-21: Значения по умолчанию как внешние ориентиры … Статус: `октябрь`». Ни одной ссылки «Story N.M».
- **Источник:** в `epics.md` нет истории про загрузку значений брифа со статусом «внешний ориентир» и дисклеймером происхождения; §10.1 (`prd.v2.md:639`) по PMM-41 прямо пишет «Детей нет (4.1-4.3 октябрь)».
- **Вердикт:** требование со статусом «октябрь» без носителя в нарезке — по §0.2 (`prd.v2.md:78`) статус `октябрь` определён как «Epic 4/5, D23», а этого требования там нет.
- **Фикс:** либо завести историю в Epic 4 (вместе с F12), либо перевести FR-21 в §4.E/реестр CM с условием возврата.

### F14. FR-25: `scenario_code` в строке аномалии — за пределами AC Story 4.3 — **Незначительно**

- **PRD:** `prd.v2.md:393` «Категория и код сценария (`scenario_code`) видны без раскрытия строки».
- **Источник:** `epics.md:529` «блок аномалий … показывает nmId/артикул, категорию, отклонение и `rub_assessment` с `source_refs`; пустой список - «критичных нет»» — `scenario_code` не упомянут.
- **Фикс:** добавить `scenario_code` в AC Story 4.3 либо снять consequence (он всё равно помечен «детальные AC уточняются после M-03»).

### F15. Время обновления сводки в §1/UJ-4 расходится с таймером — **Незначительно**

- **PRD:** `prd.v2.md:89` и `:160` «статус «данные до 29.09, обновлено сегодня **06:xx**»» — против `prd.v2.md:98` «обновлено сегодня **05:xx**».
- **Источник:** `epics.md:334` «`/brief` показывает «данные до <вчера>, обновлено сегодня **05:3x**»»; таймер `OnCalendar 05:30` (`epics.md:57`, `epics.md:305`).
- **Фикс:** привести оба вхождения к «05:3x» либо явно оговорить, что 06:xx — верхняя граница длительности прогона.

---

## 5. Вердикты старого бэклога (PRD §10.2 против BACKLOG-REVIEW и JIRA-SYNC)

Проверены все 24 строки таблицы. **22 строки корректны** — вердикты 30.08 и действия 31.08 совпадают с источниками: PMM-20 (`BACKLOG-REVIEW.md:118`), PMM-21 (`:120`), PMM-22 (`:121`), PMM-23 (`:119`), PMM-24 (`:127`), PMM-25 (`:123`), PMM-26 (`:128`), PMM-27 (`:126`), PMM-28 (`:131`), PMM-29 (`:117`), PMM-31 (`:103`), PMM-32 (`:125`), PMM-33 (`:124`), PMM-34 (`:159`), PMM-35 (`:129`), PMM-36 (`:130`), PMM-5 (`:122`), PMM-8 (`:154`), PA-38 (`:73`), PA-41 (`:82`), PA-50 (`:75`), PA-51/PA-52 (`:76`, `:77`), PA-53 (`:78`). Действия 31.08 сверены с `JIRA-SYNC:51-54, 65`. Ссылки на PR (`PR #41` → Story 2.1, `PR #39` → Story 1.10, `PR #33` → PMM-21) подтверждены `STATE.md:5` и `BACKLOG-REVIEW.md:120`. «Закрыть честно (D14)» для PMM-8 подтверждено `DECISIONS.md:118` («`pmm-8-glossary-…` (Done-без-кода → закрыть честно)»).

### F16. Строка PA-48: неверный вердикт и несуществующее действие 31.08 — **Важно**

- **PRD:** `prd.v2.md:668` «| PA-44, PA-48 | Реклама / DRR (SCN-007) | переписать / **свёрнуто** | **комментарий** | → CM-4 |».
- **Источник:** `BACKLOG-REVIEW.md:152` «| PA-48 | 17.08 | W2: SCN-007 DRR + SCN-002 заказы + SCN-004 маржа | `superseded-by-pmm`, **преемника в PMM нет** | **Done-без-кода по замыслу; скоуп потерян** |». PA-48 — не открытая задача, а Done; вердикта «свернуть» (он принадлежит 16 другим открытым задачам, `BACKLOG-REVIEW.md:8`) у неё нет. В `JIRA-SYNC-2026-08-31.md` ключа PA-48 нет ни в одной из четырёх строк «Старый бэклог» (`:51-54`) — 31.08 с ней ничего не делали.
- **Фикс:** разделить строку. PA-44: «переписать | комментарий | → CM-4». PA-48: «Done-без-кода по замыслу (superseded-by-pmm), преемника нет, скоуп потерян | 31.08 не трогалась | скоуп SCN-002/004/007 → CM-4/CM-5, решение по §12».

### F17. Второй флаг Done-без-кода (PA-29) выпал из §10.2 — **Незначительно**

- **PRD:** `prd.v2.md:665` есть строка PMM-8; строки PA-29 нет.
- **Источник:** `BACKLOG-REVIEW.md:142` «| PA-29 | 15.08 | отказ от собственного agent harness | **Нет:** … ветка `pa-29-agent-harness` … не слита | **Done-без-кода - флаг** |»; `BACKLOG-REVIEW.md:203` «Два флага Done-без-кода: **PMM-8 и PA-29** - либо домержить ветки … либо снять Done»; `epics.md:69` (AR15) «Done-без-кода **PMM-8 и PA-29** - закрыть честно»; `JIRA-SYNC:54` «PMM-8, PA-29 | комментарий-флаг Done-без-кода … статус не менялся».
- **Фикс:** добавить строку PA-29 симметрично PMM-8.

### F18. JIRA-SYNC сам считает 28 «актуальных», а перечисляет 29 — **Инфо** *(дефект репо-файла)*

- **Источник:** `JIRA-SYNC-2026-08-31.md:65` «Актуальные задачи без изменений (**28 шт.** из BACKLOG-REVIEW: PA-2, PA-13, PA-17, PA-21, PA-26, PA-34, PA-35, PA-40, PA-42, PA-50, PA-53, PA-54, PA-56, PMM-2, PMM-6, PMM-16, PMM-20..23, PMM-25, PMM-26, PMM-29, **PMM-31..36**)» — 13 PA + 16 PMM = 29. Диапазон `PMM-31..36` затягивает `PMM-34`, которая в `BACKLOG-REVIEW.md:159` числится Done-с-артефактом, а не «актуально» (`BACKLOG-REVIEW.md:191`: PMM актуально = 15).
- **Вердикт:** PRD прав («PMM-34 … не трогалась», `prd.v2.md:661`); неточен источник.
- **Фикс (в JIRA-SYNC):** заменить диапазон на «PMM-31..33, PMM-35, PMM-36» и оставить 28.

---

## 6. Счётчики

| Число в PRD | Место | Проверка | Вердикт |
|---|---|---|---|
| 22 единицы сентября | `:65`, `:106`, `:540` | `DECISIONS.md:182` (D23: 15 + 6 + 3.1 = 22); `JIRA-SYNC:18` «Задачи сентября (22)» — ровно 22 строки (`:22-43`) | ✅ |
| Epic 1 = 15 | `:98`, `:544` | 15 заголовков Story 1.0-1.14 в `epics.md` | ✅ |
| Epic 2 = 6 | `:545` | Story 2.1-2.6 | ✅ |
| Epic 3 = 5 (сентябрь 1) | `:99`, `:546` | Story 3.0-3.4; D23 «CSV-воронка (3.0, 3.2-3.4) уходит в октябрь» (`DECISIONS.md:182`) | ✅ |
| 9 done | `:106`, `:548` | `origin/main:sprint-status.yaml` (7 в Epic 1 + 2 в Epic 2) | ✅ |
| 32 истории | `:24` | 32 заголовка `### Story` в `epics.md` (15+6+5+3+3) | ✅ |
| 12 FR / 13 NFR / 16 AR в epics | `:79` | `epics.md:24-35` (FR1..FR12), `:39-51` (NFR1..NFR13), `:55-70` (AR1..AR16) | ✅ |
| 7/15 done (M-01), 2/6 done (M-03) | `:766`, `:769` | Совпадает | ✅ |
| [Claude]-единицы 1.0, 1.14, 2.6 (+ 1.1) | `:548` | `DECISIONS.md:182` (D23) дословно | ✅ |

### F19. §6.2 «10 единиц октября» и четвёртый пункт списка — **Незначительно**

- **PRD:** `prd.v2.md:550` «### 6.2 Октябрь 2026 (D23) - 10 единиц», далее четыре пункта: Epic 3 (4) + Epic 4 (3) + Epic 5 (3) = 10, плюс `:555` «Второй tenant (после M-03, D18) - единица без истории; auth и Caddy/домен - октябрь».
- **Фикс:** вынести пункт про второй tenant за пределы списка «10 единиц» или написать «10 единиц из нарезки + 1 единица без истории».

### F20. Реестр §12 объявляет CM-9..CM-16, а содержит CM-9..CM-19; таблица разорвана — **Незначительно**

- **PRD:** `prd.v2.md:710` «Посев CM-1..CM-8 - из v1; **CM-9..CM-16** - из наработок»; то же на `:502` и `:623`. Фактически в таблице есть CM-17, CM-18, CM-19 (`prd.v2.md:732-734`, разбор rmagent).
- **Дополнительно:** `prd.v2.md:731` — пустая строка внутри таблицы; CM-17..CM-19 рендерятся отдельным фрагментом без строки заголовка.
- **Фикс:** «CM-9..CM-16 - из наработок (`extract-drops`), CM-17..CM-19 - из разбора rmagent (`extract-teardown`)»; убрать пустую строку 731.

### F21. SM-6 «Validates FR-26..FR-32» включает FR-30 (воронка), которая не входит в success signal M-03 — **Незначительно**

- **PRD:** `prd.v2.md:574` «SM-6 … Validates FR-26..FR-32» (диапазон включает FR-30); при этом `prd.v2.md:575` «SM-7 … Validates FR-30».
- **Источник:** воронка в сентябрьской сводке — non-goal (`prd.v2.md:512`; `epics.md:422` D23).
- **Фикс:** «Validates FR-26..FR-29, FR-31, FR-32» в SM-1(a) и SM-6.

### F22. FR-33 не существует — дыра в нумерации без объяснения — **Инфо**

- **PRD:** заголовки идут FR-32 (`:300`) → FR-34 (`:396`); `prd.v2.md:79` обещает «Идентификаторы стабильны … новые - FR-26+».
- **Источник:** `.memlog.md:24` перечисляет «FR-26..30, FR-31, FR-32 … + FR-34 порог … + FR-35» — FR-33 не выделялся.
- **Фикс:** одна строка в §0.2: «FR-33 не выделен; номер зарезервирован и не переиспользуется».

---

## 7. Тихие пропуски (что есть в epics.md и не имеет дома в PRD)

### F23. Четыре из 22 сентябрьских единиц не покрыты ни одним требованием §4 — **Важно**

- **Пропущены:** Story 1.2 «TS↔PG harness в make verify» (`epics.md:161`), Story 1.8 «Тестовая база и сандбокс OpenHands» (`epics.md:244`), Story 1.9 «Образы, compose и staging-overlay» (`epics.md:257`), Story 1.13 «Runbook первого релиза» (`epics.md:310`).
- **Где они в PRD:** только как элементы очереди в §6.1 (`prd.v2.md:544`) и как ключи `PA-58`, `PA-60`, `PA-61`, `PA-63` в §10.1 (`prd.v2.md:635`) под общей формулой «epics NFR1..NFR13, AR1..AR11». Ни одного FR, ни одного гейта приёмки, ни одной строки в §13.
- **Вердикт:** 4 из 22 единиц (18 % объёма сентября, из них 2 уже done) читатель PRD не может отобразить ни на требование, ни на метрику.
- **Фикс:** добавить в §4.A подраздел «FR-nn: воспроизводимая среда проверки» (harness + тестовая база + образы + runbook) со ссылками epics AR2, AR4, AR10, AR11 и NFR10 — либо явную оговорку в §10.1: «требования конвейера сформулированы как epics NFR/AR и в §4 не дублируются».

### F24. epics AR1 (миграции 011-016 и правило перенумерации) не упомянут — **Незначительно**

- **Источник:** `epics.md:55` (AR1 — полный список миграций и объектов), `epics.md:120` «`tools/verify_migrations.py` запрещает пропуски в нумерации - номера в историях целевые; ветка, мержащаяся второй, перенумеровывает свои миграции», `epics.md:339` (Epic 2 целевые `014`-`015`), `epics.md:422` (Epic 3 целевая `014`) — прямая коллизия номера между Epic 2 и Epic 3.
- **Вердикт:** столбец «Данные-предпосылки» §13 (`prd.v2.md:767-769`) не отражает зависимость по порядку мержа, хотя Epic 2 и Epic 3 в §6.1 идут параллельно (`prd.v2.md:548`).
- **Фикс:** в §13 в строках M-02/M-01b добавить «порядок мержа: вторая ветка перенумеровывает миграцию `014` (epics AR1, AD-14)».

### F25. epics NFR2 (фикстуры ≤ 200 КБ в git, FixtureTransport, токены не заданы) сжат до одной фразы — **Незначительно**

- **PRD:** `prd.v2.md:688` «Тесты только на фикстурах; сеть в тестах запрещена; живой WB API - когда без него никак».
- **Источник:** `epics.md:40` «NFR2: … `FixtureTransport` + обезличенные фикстуры **≤ 200 КБ в git**; полные ответы - VPS + S3; **в тестовом окружении токены не заданы**».
- **Фикс:** дописать в §11.1 предел размера и правило «в тестах токены не заданы» — это guardrail, а не деталь реализации.

### F26. epics AR2 (образы/compose) и AR4 (provision-runtime-roles, `proxima_test`) не имеют дома — **Незначительно**

- **Источник:** `epics.md:56` (AR2), `epics.md:58` (AR4). В PRD §11.2 (`prd.v2.md:697`) есть только «Один VPS; боевая база + `proxima_test`; сандбокс OpenHands - только `proxima_test`»; про образы, `.dockerignore`, `profiles: [jobs]`, `secrets:` и идемпотентный provision нет ничего.
- **Фикс:** одна строка в §15 «Операционные требования»: «поставка — образы collector/control-plane, одноразовые контейнеры из compose с секретами; роли создаются идемпотентным provision (epics AR2, AR4)».

### F27. epics «UX Design Requirements» не отражён положительно — **Незначительно**

- **Источник:** `epics.md:74` «Нет UX-контракта: веб-морда доделывается на существующем каркасе (D19); единственное UI-изменение в сентябре - экран `/brief` читает `brief_current`/`data_status_current` и показывает предупреждение при `stale`/`insufficient`/`blocked`».
- **PRD:** только как non-goal `prd.v2.md:513` «Переписывание веб-морды и `bmad-ux`».
- **Фикс:** добавить в §4.B (после FR-32) строку «UX-контракта нет: единственное UI-изменение сентября — блок сводки и строка статуса на существующем каркасе (D19, epics UX Design Requirements)».

---

## 8. Вердикт

### Категория × серьёзность

| Категория | Критично | Важно | Незначительно | Инфо | Итого |
|---|---|---|---|---|---|
| 1. Статусы историй | 0 | 1 (F1 — дефект репо-файла) | 3 (F2, F3, F4) | 0 | 4 |
| 2. Story → эпик → ключ Jira | 0 | 0 | 0 | 1 (F5) | 1 |
| 3. Мэппинг требований (epics FR/NFR/AR) | 0 | 2 (F6, F7) | 4 (F8, F9, F10, F11) | 0 | 6 |
| 4. Ссылки на истории | 0 | 2 (F12, F13) | 2 (F14, F15) | 0 | 4 |
| 5. Вердикты бэклога | 0 | 1 (F16) | 1 (F17) | 1 (F18) | 3 |
| 6. Счётчики | 0 | 0 | 3 (F19, F20, F21) | 1 (F22) | 4 |
| 7. Тихие пропуски | 0 | 1 (F23) | 4 (F24, F25, F26, F27) | 0 | 5 |
| **Итого** | **0** | **7** | **17** | **3** | **27** |

Из 27 находок **3 — дефекты репо-файлов**, а не PRD: F1 (устаревшие `sprint-status.yaml` и `STATE.md` в ветке `sunfish`), F9 (двойная приписка AR5/AR6 в `epics.md:91`), F18 (диапазон `PMM-31..36` в `JIRA-SYNC:65`). Остальные 24 — дефекты PRD.

**Общая оценка:** блокеров нет. Фактическая база PRD прочная — все 22 ключа Jira, все счётчики (22 / 15+6+5 / 9 done / 32 / 12-13-16) и все статусы историй верны против `origin/main`. Дефекты сосредоточены в трассировке (§10.1 против §13, epics FR11/FR12) и в двух требованиях без носителя (FR-20, FR-21).

### 5 главных фиксов

1. **§10.1:** перенести FR-29 в строку `PA-36` (носитель — Story 1.7 = `PA-59`) и FR-22 в строку M-01 (носители 1.12 = `PA-62`, 1.14 = `PMM-50`); привести таблицу в соответствие с §13, где FR-22 уже стоит в M-01. *(F6)*
2. **§10.2:** разделить строку «PA-44, PA-48» — у PA-48 вердикт `BACKLOG-REVIEW` = «Done-без-кода по замыслу, преемника нет, скоуп потерян», а действия 31.08 не было вовсе; добавить пропущенную строку PA-29 (второй флаг Done-без-кода). *(F16, F17)*
3. **FR-20 и FR-21:** дать им носителя — либо новая Story 4.4 «Policy Layer: пороги с провенансом» в `epics.md`, либо перевод в §4.E/реестр CM; сейчас FR-20 приписывает Story 4.2 поведение, которого нет в её AC, а у FR-21 истории нет вообще при статусе «октябрь». *(F12, F13)*
4. **Трассировка:** добавить ссылки `epics FR11` (в FR-1/FR-8/FR-9/FR-34) и `epics FR12` (в FR-4/FR-14) и развернуть формулу «epics NFR1..NFR13, AR1..AR11» до AR7/AR12/AR13/AR14 в строках M-02/M-03/M-01b и AR15/AR16 в примечании. *(F7, F8)*
5. **Синхронизация и покрытие конвейера:** подтянуть в ветку `sprint-status.yaml` и `STATE.md` с `origin/main` и указать в frontmatter источник статусов (`origin/main@b2c3861`); закрыть в §4 дыру по единицам 1.2 / 1.8 / 1.9 / 1.13 — 4 из 22 сентябрьских единиц сейчас не отображаются ни на одно требование. *(F1, F23)*
