# Спор персон по спорным находкам рецензии v3 (bmad-party-mode, режим subagent)

- **Дата:** 02.09.2026. **Объект:** `prd.md` ревизии v2.1. **Состав:** Winston 🏗️ (архитектор), Amelia 💻 (инженер), Dana 🚢 (прагматик), Grumbal 😤 (адверсарий), Level 📏 (проверяющий заявления), John 📋 (PM).
- **Как проходило:** два раунда; персоны работали отдельными сабагентами с доступом к репозиторию и VPS только на чтение. Ходы второго раунда Winston, Amelia, John, Level и Dana получены основной сессией напрямую; ход Grumbal второго раунда получен после сборки файла и вынесен в отдельный раздел в конце. Файл собран основной сессией (John, PM) по этим ходам; резюме комнаты не смягчено.
- **Что комната проверила по репозиторию и серверу (факты, не мнения):**
  - Боевой чекаут `/srv/proxima-ai/repo` стоит на `fd95fcb` от 25.08 (реверт «enforce READ-only Analytics token»), `git remote` пуст, 210 коммитов позади `origin/main`; `tools/morning_run.sh` там отсутствует; `systemctl list-timers` не содержит ни `proxima-morning@`, ни `proxima-funnel-csv@` (только `proxima-host-monitor.timer`, `codex-conductor.timer`); боевой Postgres монтирует initdb-миграции 001-006 из этой копии против 001-010 в main (`INVENTORY.md:24,111`). AD-15 предусматривает восстановление `origin` и переход на тег, но в AC Story 1.13 (`epics.md:310-322`) этого шага нет.
  - **Окно WB не «уходит по дню в сутки»**: два независимых снимка (30.08 и 31.08, разные md5, ~34 часа между ними) оба начинаются с 2026-03-01 по заказам и продажам. Скорость сдвига окна никем не измерена (единственные пробы `flag=1`: 28.02 отдаёт, 21.02 пусто, сделаны 30.08). Фраза FR-27 «март выпадает по одному дню в сутки» - модель, которой замер противоречит.
  - Поздние правки прошлого: между снимками изменились 11 дней из 183, суммарный дрейф +25 заказов на 10 258 (+0.24 %); «вчера»/«позавчера» на момент снимка не сдвинулись; максимальная переписка одного дня - 20.08 с 33 на 28 (−15.2 %), дотянувшаяся до 30.07 (32 дня назад). Это довод против порога 15 %, не против метрики.
  - Ретро-статистика дневного ряда (`~/signal-inputs/fixtures/wb-api/statistics/supplier-orders/20260831T155341Z__…json`, 13 386 строк, 184 дня 01.03-31.08), независимо пересчитана Level и Amelia: серийный CV 0.308-0.315 - это падение уровня (март → август: 62 → 32 заказа в день, −48 %), не шум; скользящий CV-14 медианно 0.185; медианное |отклонение| дня от медианы предыдущих 14 - 13.3-13.8 %, p90 37.6-38.2 %; доля тревожных утр из 169-170 при порогах 15/20/30/40 % - 46/31-32/20-21/8 %. Размеченного ground truth «за сработкой стояло событие» нет ни в одном источнике.
  - `services/collector/src/jobs/collect.ts:100-102`: `dateFrom = min(run_day−3, last_full_day+1)`, recovery floor не ограничен - при `last_full_day = 31.08` первый серверный `collect` закрывает весь сентябрь одним вызовом (~2 200 строк против потолка 80 000). Механизм «день теряется навсегда» относится к `funnel_v3` (окно 7 дней без глубины назад), а не к заказам; и по AD-5 `funnel_v3` тянет окно `[run_day−7, run_day−1]` каждое утро, поэтому день воронки теряется после семи провальных утр подряд, а не после одного 429.
  - `origin/feat/m01-story-1.6` стоит в `review`, хотя второй Given Story 1.6 (`epics.md:225`) требует бэкфилл из Story 1.5, которого на ветке нет; тест `services/collector/tests/cabinet-daily.db.test.ts:16` кладёт четыре строки прямым INSERT, и весь файл идёт `{ skip }` без `PROXIMA_TEST_DSN_COLLECTOR`. Given Story 1.14 (`epics.md:331`): «Stories 1.0-1.13 смержены» - 1.14 не стартует без 1.13; внутри 1.13 записана последовательность `cas_import.ts` → `backfill --source artifact:<sha256>` → `collect --date-from`.
  - Находка рецензии реализуемости «серверное включение `funnel_v3` - только Story 3.4» неверна: в Story 3.1 (`epics.md`) стоит «`morning_run.sh` получает шаг `funnel_v3`». Верно другое: на сервере сегодня нет ничего, что можно включить.
  - Сторож алертов: `README.md:28` и `docs/agent-system/MEMORY.md:29` - пин `api.telegram.org` на `149.154.167.220` в `/etc/hosts` с просьбой «перепроверь пин, если доставка снова упадёт»; `tools/proxima_alert.sh:22` - один `curl` с `max-time 20`, `fail`, `silent`, без `--retry`; `WORKS-TODAY.md:65` признаёт, что путь регрессией не проверяется. Частота 429/5xx у `funnel_v3` - UNKNOWN (живых прогонов на сервере ноль).

## Т1. Ветка отказа гейта 30.09 и порядок сброса

**Позиции.** Winston: список сброса = только 1.8; 1.11 слить в 2.5 (строка статуса уже в её AC); 1.13 не резать, а дописать ей шаг оживления чекаута. Amelia: 1.14 по своему Given не стартует без 1.13; 1.6 «готова» только на бумаге. John: §13 написан в неверном жанре - это не список единиц на выброс, а правило сжатия объёма внутри единиц; «M-03 частично» вычеркнуть, гейт бинарный; триггер §14 «16 единиц к 15.09» - число без вывода. Level: доказаны только зависимости (1.13 → 1.14, 1.14 → 2.6); цену 1.7/1.8/1.11 никто не посчитал ни в часах, ни в файлах, «5 волн» - граф, не расписание. Dana: 1.7 нетронута безоговорочно (без `delete_run` откат невозможен, adversarial против предложения реализуемости сбросить 1.7 первой); ветку отказа утверждает Mike до 08.09, не по факту 30.09.

**Где разошлись.** Не разошлись по существу; расходятся в том, сколько оставить от 1.13 («ядро деплой + откат» у John и Dana против «полный runbook плюс шаг оживления чекаута» у Winston).

**Скрытые допущения.** PRD считал 1.13 «документом», а это вход в Given 1.14 и единственное место, где записан запуск бэкфилла; ветка отказа в PRD резала ровно те две единицы, на которых держится и деплой, и откат.

**Кандидат-решение для Mike.**
- (а) *Рекомендация комнаты:* ветка отказа переписывается как правило сжатия: снимается только 1.8 (сандбокс OpenHands весь сентябрь на старой копии базы, зона сдаётся по CI - AD-12 разрешает); 1.11 сливается в 2.5; 1.13 сжимается до ядра «деплой + откат + оживление чекаута» (вывод `proxima-webapp-staging` и пустых `proxima_dev` - октябрь); 1.7 неснимаема; цепочка `1.4 → 1.5 → 1.6 → 1.7 → 1.13 → 1.14 → 2.3 → 2.4 → (2.5+1.11) → 2.6`; «M-03 частично» вычёркивается; триггер §14 - «к 15.09 Story 1.13 в main и дата деплоя 1.14 стоит в календаре». Цена: 13 → 11 единиц, полдня внутри 1.13.
- (б) Оставить список сброса из PRD (1.13, 1.8) - комната единогласно против: выпускает 2.6 без инструмента её же отката и без входа в 1.14.
- Утверждает Mike до 08.09.

## Т2. Достижимость SM-7 и старт воронки

**Позиции.** Все пятеро: 27.10 − 8 недель = 01.09, цель напечатана со сроком годности в сутки (D23 приняли её 31.08, когда она ещё была верна). Winston и John: SM-7 = «N целых недель между датой первого успешного `funnel_v3` на сервере и 27.10», при 2.6 ≈ 23.09 N ≈ 4.9-5; «8 недель» вычеркнуть из SM-7 и из гейта §13 M-01b; носитель - Story 3.1 в main до тега 2.6 плюс ближайший серверный релиз [Claude]. Level: находка «только 3.4» ошибочна, носитель есть, но сервер пуст - «включить» сегодня нечего. Dana: отдельной единицы не заводить, ранний таймер воронки ради октябрьской метрики не покупать. John: «активный nmId» определить в PRD дословно из 3.1 (distinct из `stg_wb_orders_latest` за 30 дней); OQ-16 закрыть записью о сужении CAP-6, а не молчанием.

**Где разошлись.** Нет. Grumbal (первый раунд) настаивал на буфере под потерю дня воронки - опровергнут AD-5 (окно 7 дней перезапрашивается каждое утро).

**Кандидат-решение для Mike.**
- (а) *Рекомендация комнаты:* SM-7 переформулировать как N недель от фактического включения (≈ 5 при плане 2.6 к 23.09); «8 недель к 27.10» убрать из SM-7 и §13; в §14 записать принятую цену: Epic 4 стартует на ~5 неделях воронки, недельная этапность по SKU в октябре - на коротком ряде с явной пометкой либо ждёт ноября. Цена: один абзац, ноль единиц.
- (б) Держать 8 недель - невозможно арифметически; (в) отдельный ранний таймер воронки в сентябре - против: стоит сентябрьского фокуса ради метрики, которую SM-6 и так исключает.

## Т3. Цепочка «стоп на первой ошибке» с воронкой перед сводкой

**Позиции.** Winston: отдельный `proxima-funnel-v3@.{service,timer}` на 06:15 МСК со своим `OnFailure` (паттерн `proxima-funnel-csv@` уже в доме); AD-6 → `collect → norm → brief`; правка `ARCHITECTURE-SPINE.md:75` одной строкой в том же коммите; плюс `--retry 3 --retry-delay 5` в `proxima_alert.sh` и шаг runbook «уронить юнит намеренно и увидеть сообщение». John: FR-22 не трогать (цитата AD-6), подать change request к AD-6; §13 «воронка гейт не блокирует» до CR заменить строкой риска §14; в §15 - семь утр SM-6 считаются по `collector_runs`, не по факту доставки алерта. Level: механизм доказан, величина риска нет - частота 429 у v3 UNKNOWN; SPOF алерта подтверждён. Dana: CR как есть; разовая проверка живости пина перед стартом 3.1; ретрай и запасной канал - после недели живого лога. Amelia: против «фонового шага внутри скрипта» - он размножит обход `OnFailure`.

**Где разошлись.** Ретрай в алерте сейчас (Winston, Level) против «после недели живого лога» (Dana). Всё остальное - единогласно.

**Скрытые допущения.** PRD писал риск так, будто при остановке цепочки придёт алерт; на деле алерт - один выстрел по пиннутому IP, который уже однажды молчал, то есть отказ тихий, и Mike узнаёт о нём, зайдя руками, что и есть исключённый SM-6 «ручной запуск».

**Кандидат-решение для Mike.**
- (а) *Рекомендация комнаты:* утвердить CR к AD-6 до мержа Story 3.1: обязательные `collect → norm → brief` (стоп на первой ошибке остаётся), `funnel_v3` - отдельный юнит после `brief` со своим `OnFailure`; в PRD: §13 строка «воронка не блокирует» → строка риска §14 до утверждения CR; §15 - подсчёт семи утр по `collector_runs`; правка спайна одной строкой в том же коммите; ретрай в `proxima_alert.sh` и проверка пина - в runbook 1.13. Цена: два маленьких файла, полдня внутри 3.1/1.12, до тега 2.6.
- (б) Оставить цепочку как есть и принять, что один сбой воронки в конце сентября молча переносит гейт на неделю.

## Т4. Место бэкфилла в очереди и «дата отсечки»

**Позиции.** Level и Dana: «март утекает по дню в сутки» - неправда по двум снимкам; §8.0 п.1 «дата отсечки бэкфилла» не вычисляется - вычёркивается. Amelia: сентябрьские дни заказов догоняются одним прогоном благодаря `dateFrom` из AD-2; механизм потери - только у воронки. John: настоящее условие - 1.5 в main до тега 1.14, потому что без бэкфилла `sample_days < 14` (AD-8) и экран честно молчит две недели, SM-6 не наступает; настоящая потеря - версии: поздний бэкфилл кладёт один устоявшийся снимок, отмены доезжают днями (69 из 172 строк меняли дни до 25 суток назад), значит норма строится на отстоявшихся днях, а «вчера» - свежее и систематически завышенное; артефакты живут только на VPS и в S3, у пары 31.08 нет `.headers` - бэкфилл идёт с пары 30.08 плюс живой хвост. Winston: AD-6 всё ещё описывает бэкфилл как ручной `collect --from` по 1/мин, хотя AD-2 переписал его на CAS-артефакты - одна фраза правки спайна. Dana: 1.6 не считается готовой, пока в её ветке нет реального `backfill --source artifact`; зазор «01.09 → дата живого сбора» закрывает один человек.

**Где разошлись - дата серверного релиза 1.14.** Winston/John: 1.14 ≤ ~20.09 и 2.6 ≤ 23.09 (семь утр 24-30.09, сентябрь догоняется одним прогоном). Level: без бэкфилла окно нормы AD-8 для первого оцениваемого дня 23.09 - [09.09, 22.09], первый `collect` версионирует [R−3, R−1], значит R ≤ 12.09 (средняя уверенность: зависит от того, считается ли день первого прогона полным); с бэкфиллом ограничение снимается. Dana: 1.14 ≤ 13.09 как жёсткая дата. Общее у всех: **1.5 с настоящим бэкфиллом обязана быть в main до тега 1.14; 1.6 без неё не готова.**

**Скрытые допущения.** PRD и senior PM опирались на «необратимость» бэкфилла по окну WB; необратимость есть, но в другом месте - в версиях дней и в единственной копии артефактов вне git.

**Кандидат-решение для Mike.**
- (а) *Рекомендация комнаты:* из FR-27 убрать фразу «по одному дню в сутки»; §8.0 п.1 снять; записать условие «1.5 (реальный бэкфилл с пары 30.08 плюс живой хвост) в main не позже 08.09, 1.5 и 1.14 едут одним тегом; 1.6 принимается только с реальным `backfill --source artifact`»; в §14 - риск версий (норма на отстоявшихся днях, «вчера» свежее) и риск единственной копии артефактов (контрольные суммы есть в `API-FACTS.md`, вторая копия в S3 - проверить до старта 1.5); дата 1.14 на сервере - не позже 20.09 при выполненном бэкфилле (жёстче, 13.09, если бэкфилл сорвётся). Цена: переоткрыть почти готовую 1.6 - дни, не абзац; живой хвост `sales` 1/мин с пагинацией в релизном окне 1.14.
- (б) Развилка по семантике «вчера»: (б1) свежее «вчера» против устоявшейся нормы - быстро, отклонение смещено вверх, порог с запасом; (б2) показывать позавчера (полностью отстоявшийся день) - честная арифметика, но SM-6 и формулировка продукта «вчера против нормы» переписываются. Комната склоняется к (б1) с явной записью смещения.

## Т5. Порог тревоги OQ-7 против октябрьского плана

**Позиции.** Level: распределение ретро подтверждено на двух снимках; CV 0.32 из D21 - не мера шума утра, а падение уровня, медиана-14 его гасит; порог 30 % предварительно (тревога примерно каждое пятое утро), значение в конфиге с источником «ретро 183 дня, снимок 31.08, sha256» и датой; перекалибровка через две недели живых утр; на живых утрах шум будет выше (отмены в ретро посчитаны задним числом); главное не доказано - стояло ли за сработкой событие, разметку может дать только Mike. John: 30 % и только на падение (рост показывается числом без тревоги); обоснование в FR-34 по числам ретро; Story 4.2 пополам - «ранжирование по деньгам» без порога едет первым; Policy Layer FR-20/21 в M-04 не строить - одно значение в конфиге с источником и датой. Winston, Dana, Amelia: 30 %; 40 % теряет августовское событие −36 % (p90 шума 37.6 %); 15-20 % - «игровой автомат». Grumbal (первый раунд): порог из ретро - гадание; опровергнут распределением, но его оговорка про тишину как второй провал принята Dana.

**Где разошлись.** Односторонний порог (John) против симметричного (остальные не возражали явно; точная экономия частоты от односторонности - UNKNOWN до одного пересчёта).

**Кандидат-решение для Mike.**
- (а) *Рекомендация комнаты:* предварительный порог 30 % на падение, записанный в `DECISIONS.md` до релиза 2.6 с источником и датой; перекалибровка после двух недель живых сводок (скорее вверх, чем вниз); Story 4.2 разрезать; Policy Layer в M-04 не строить (правило трёх не наступило); в FR-34 - таблица частот 15/20/30/40 % как обоснование и оговорка про нижние оценки. Цена: ноль единиц в сентябре, константа приезжает с 2.4/2.5; примерно одна тревога в неделю - потолок терпения единственного читателя, записывается как известная цена.
- (б) 40 % - две-три тревоги в месяц, спокойное утро, но событие того размера, ради которого сводку строили, проходит мимо.
- (в) Ждать живых недель без порога - против: две недели тишины не калибруют глаз, а октябрьские единицы стоят на пороге.
- Отдельно к Mike: разметить, какие из ~35 ретро-сработок при 30 % стояли за реальным событием кабинета - без этого любой порог откалиброван по разбросу, а не по смыслу.

## Что комната просит у Mike первым

Не порог и не порядок единиц, а **две даты в календаре под слово «деплой»** (по D7 без Mike релиза не существует): 1.14 не позже 20.09 (при выполненном бэкфилле; 13.09 без него) и 2.6 не позже 23.09 - иначе семь утр 24-30.09 не помещаются. Если хотя бы одна дата не встаёт, гейт 30.09 переносится сегодня, а не констатируется проваленным утром 30.09. Первый релиз - не «включить таймер», а оживить сервер: чекаут на ревертном коммите 25.08 без `remote`, таймеров нет, миграции на боевом 001-006.

## Grumbal 😤 (адверсарий), второй раунд - получен после сборки

- Т1: контрадикция по 1.7 закрыта комнатой; но сжатие 1.13 «до абзаца отката» рискует выкинуть именно порядок шагов, от которого зависит Т4.
- Т2: формула SM-7 починена; хрупкость никто не тронул - один пропущенный день воронки = минус неделя, буфера нет.
- Т3: данных о частоте 429 у v3 нет, но `docs/state/API-FACTS.md:154,162` показывает, что на том же токене в тот же день живой лимит `stocks-report` разошёлся со спецификацией (1 против 3/мин) - основание не доверять цифре «3/мин» для v3 вслепую. Даже после выноса `funnel_v3` фоном единственный алерт-канал (`proxima-alert@` через пин Telegram в `/etc/hosts`) остаётся SPOF для обязательных шагов `collect → norm → brief`.
- Т4 (код-пруф): `selectCollectDateFrom` в `services/collector/src/jobs/collect.ts` (ветка `origin/feat/m01-story-1.6`) - при `last_full_day === null` откат к истории отключается, `dateFrom = run_day − 3`. В AC Story 1.14 (`epics.md:320`) `systemctl enable --now` таймера `morning` идёт ДО шага бэкфилла: единственная защита от дыры - ручной явный `--date-from 2026-08-27`, записанный только в тексте 1.13, который Т1 предлагает сжать. **Первым сломается не гейт и не SM-7, а тихая необнаруживаемая дыра в данных на первом деплое 1.14.**
- Т5: механизм CV признаёт (просадка уровня, не шум), но скорректированные числа Level (медиана-14 отклонения 13.8 %, порог 15 % → 46 % тревожных утр) хоронят «спокойное утро» ещё сильнее; отмены в ретро считались задним числом - живой шум выше.

**Следствие для рекомендации Т1/Т4 (John):** ядро 1.13 обязано сохранить порядок «бэкфилл → первый `collect --date-from` → включение таймера» дословно, и это же условие переносится в AC Story 1.14 (порядок шагов - предмет правки epics, не только PRD); в §14 PRD - риск «таймер раньше бэкфилла = тихая дыра».

## Что из этого меняет находки рецензии v3

- Реализуемость R3 «`funnel_v3` только в 3.4» - неверна, носитель в 3.1 есть; вердикт FAIL октября держится на остальном (SM-7 недостижим, FR-20/21 и разрез «категория» без носителя).
- Критическая находка «Story 1.5 исполняется последней» усиливается: 1.6 закрыла зависимость от 1.5 синтетическими строками.
- Критическая находка «единственная копия истории до окна WB» верна, но мотив «окно уходит по дню в сутки» - нет; мотив - версии и отсутствие второй копии.
- FR-27 и §14 PRD содержат неподтверждённую фразу об окне WB - правится.
- Новый факт для §14/§15: состояние боевого сервера (чекаут, таймеры, миграции) - риск, которого в PRD нет.
