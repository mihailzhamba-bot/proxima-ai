# Adversarial-ревью PRD PROXIMA AI v2.1

**Директива:** `skill:bmad-review lenses=adversarial` (только линза `adversarial`, `.claude/skills/bmad-review/references/lens-adversarial.md`).
**Объект:** `_bmad-output/planning-artifacts/prds/prd-PROXIMA-AI-2026-08-28/prd.md` (v2.1, 1038 строк) + `addendum.md`.
**Дата:** 02.09.2026. **Метод:** последовательный обход §0-§16 и §4.A-§4.G; поиск того, чего в документе НЕТ, наравне с тем, что неверно.
**Сверено с:** `docs/state/API-FACTS.md`, `ARCHITECTURE-SPINE.md` (AD-1..AD-18), `_bmad-output/specs/spec-wb-morning-brief/SPEC.md` + `glossary.md`, `_bmad-output/planning-artifacts/epics.md`, `AGENTS.md`.
**Не читалось по условию задачи:** `validation-report*`, `review-*`, `reconcile-*`, `module-selection.md`, `extract-*`, `.memlog.md`.
**`also_consider`:** нагрузка и лимиты WB API (замер побеждает спецификацию), внешние зависимости (токены категорий, подписки), additive-only миграции, ops-конверт (таймер 05:30 МСК, один VPS, один Postgres), заморозки сентября, инварианты AD-1..AD-18, срок 30.09.2026 и темп единиц.

Серьёзность - оценка рецензента по влиянию на пригодность PRD как зонта для нисходящих работ (SPEC, epics, единицы, гейт 30.09), не часть канонической линзы.

## Сводка

| Серьёзность | Находок |
|---|---|
| critical | 3 |
| high | 20 |
| medium | 9 |
| low | 2 |
| **Итого** | **34** |

Пять самых дорогих: A-09 (шаг воронки блокирует сентябрьскую сводку), A-10 (единственная копия истории до окна WB не защищена), A-31 (read-write analytics-токен у неизвестного потребителя), A-19 (октябрьский план арифметически невозможен), A-03 (гейт 30.09 не имеет процедуры и допуска сверки).

## Покрытие по разделам

| Раздел | Находок (основных) | ID |
|---|---|---|
| §0 Назначение | 2 | A-01, A-02 |
| §1 Видение | 1 | A-03 |
| §2 Пользователь | 2 | A-04, A-05 |
| §3 Глоссарий | 1 | A-06 |
| §4.0 Источники данных | 2 | A-07, A-08 |
| §4.A Сбор (M-01/M-01b) | 5 | A-09, A-10, A-11, A-12, A-13 |
| §4.B Норма и сводка (M-02/M-03) | 3 | A-14, A-15, A-16 |
| §4.C Аномалии (M-04) | 3 | A-17, A-18, A-19 |
| §4.D План действий (M-05) | 2 | A-20, A-21 |
| §4.E Отложенные FR v1 | 1 | A-22 |
| §4.F Модули M-06+ | 2 | A-23, A-24 |
| §4.G Карта FR | 1 | A-25 |
| §5 Non-Goals | 1 | A-26 |
| §6 Объём | 1 | A-27 |
| §7 Метрики | 1 | A-28 |
| §8 Открытые вопросы | 0 (перекрёстно: A-05, A-22) | - |
| §9 Индекс допущений | 1 | A-29 |
| §10 Jira Sync | 1 | A-30 |
| §11 Ограничения | 1 | A-31 |
| §12 + §12.1 Реестр кандидатов | 1 | A-32 |
| §13 Дорожная карта | 0 (перекрёстно: A-02, A-19, A-27) | - |
| §14 + §15 Риски и операции | 1 | A-33 |
| §16 Best practices | 1 | A-34 |

---

## §0. Назначение документа

### A-01 · high · PRD объявляет себя неглавным и тут же вводит требования, которых нет в контракте

- **Место:** `prd.md:74` (правило «побеждает SPEC/спайн; PRD не переопределяет»), `prd.md:320` (`[NOTE FOR PM]` CAP-6 сужен PRD), `prd.md:385` («Число задаёт этот PRD, SPEC и спайн его не задают»), `prd.md:781` (OQ-16).
- **Триггер:** исполнитель Story 3.1 (сентябрь, старт ≤ 08.09) берёт канон из SPEC по D17/D22 и front-matter `canonical_contract` - там CAP-6 всё ещё «async CSV с 01.09 и v3 ежедневно», а крайнего срока цикла 06:30 нет нигде, кроме PRD. Правка SPEC (OQ-16) не имеет ни владельца-даты, ни привязки к дате старта зависимой единицы.
- **Guard / правка:** пометить такие требования единым ярлыком «контракт ожидает правки; до неё действует PRD» и повесить дату правки SPEC раньше старта зависимой единицы (для CAP-6 - до 08.09, для 06:30 - до AC Story 2.4). Либо запретить писать AC зависимой единицы до ревизии SPEC.
- **Последствие:** сентябрьская единица строится по расходящемуся интенту; на октябрьском разборе гейта спорят, что именно требовал CAP-6. PRD де-факто становится контрактом - ровно то, что D17 запрещает.

### A-02 · medium · Канон статуса содержит скоропортящиеся счётчики без владельца обновления

- **Место:** `prd.md:20-21` (`story_status_source` с пином на `b2c3861`), `prd.md:131-138` (витрина §1), `prd.md:695-703` (§6.1), `prd.md:961` («2/6 done» в §13, объявленном каноном).
- **Триггер:** мержится любая история - три таблицы расходятся с `sprint-status.yaml`; §13 объявлен каноном статуса, но сам несёт счётчики, которые никто не обязан обновлять.
- **Guard / правка:** оставить в §13 только ступень/гейт/срок, а счётчики - в генерируемой витрине из `sprint-status.yaml`; либо добавить строку «обновляет конвейер при каждом мерже; данные старше N дней считать недействительными».
- **Последствие:** триггер отказа гейта («к 15.09 меньше 16 единиц done», `prd.md:989`) проверяется по таблице, которую никто не ведёт; решение о сбросе объёма принимается по устаревшим числам.

## §1. Видение

### A-03 · high · Сентябрьский гейт сформулирован как «цифры совпадают с кабинетом WB» без процедуры и допуска

- **Место:** `prd.md:120` (горизонт 1), `prd.md:344` (FR-32 consequence), `prd.md:729` (SM-6), `prd.md:961` (гейт §13). В SPEC (`SPEC.md:42`, `SPEC.md:83`) допуска тоже нет - PRD как зонт приёмки его не добавил.
- **Триггер:** утро 30.09: сводка показывает 31 заказ за 29.09, экран кабинета WB - 33 (другой учёт отмен, другой экран отчёта, другой часовой пояс агрегата).
- **Guard / правка:** описать процедуру сверки как требование: какой именно экран/отчёт WB эталон, в какое время снимается, какая метрика (заказы без `isCancel`), какой допуск (например ±1 заказ и ±0.5 % по выручке), кто фиксирует результат и в каком артефакте репозитория.
- **Последствие:** единственный гейт сентября решается спором, а не проверкой; «пройден / не пройден» невоспроизводимо, а ветка отказа (§13) включается на ощущении.

## §2. Пользователь

### A-04 · high · UJ-5 «Mike переключает кабинет» противоречит AD-9: webapp привязан к одному tenant переменной окружения

- **Место:** `prd.md:193` (UJ-5 «без изменения схемы»), `prd.md:683` (§5), `prd.md:965` (§13, «единица без истории»). Против AD-9: `WEBAPP_TENANT_ID` обязателен и валидируется при старте, ровно два SELECT под одним tenant (спайн, `epics.md` AR13).
- **Триггер:** после M-03 подключается второй кабинет; Mike ожидает переключатель внутри одного `/brief`.
- **Guard / правка:** либо переформулировать UJ-5 как «один инстанс webapp на кабинет (свой порт/туннель)», либо записать, что переключение требует нового AD (выбор tenant на запрос + GUC на транзакцию) и отдельной единицы; в обоих случаях снять формулу «без изменения схемы» как достаточную - схема БД действительно не меняется, но витрина и доступ меняются.
- **Последствие:** «единица второго кабинета без истории» недооценена: за ней прячется правка спайна и UI-работа, не попавшая ни в §6.2, ни в epics; в октябре это конкурирует за те же дни, что M-04/M-05.

### A-05 · high · Все решения замкнуты на одного человека, без сроков и значений по умолчанию

- **Место:** `prd.md:143` (Mike - единственный decision-maker), `prd.md:744-762` (девять пунктов очереди §8.0 без дат и владельцев исполнения), `prd.md:987` (риск окна WB, митигация = решение Mike).
- **Триггер:** Mike недоступен 5-7 дней в сентябре или просто не отвечает на пункт 1 очереди (дата отсечки бэкфилла). Окно WB продолжает выдавливать по дню в сутки, пока решение ждёт.
- **Guard / правка:** каждому пункту §8.0 - дата ответа и действие по умолчанию при молчании («если решения нет к 05.09 - бэкфилл запускается с 01.03 как есть»); отдельно назвать, кто подтверждает деплой, если Mike недоступен, либо явно записать, что деплой в этом случае не происходит и срок сдвигается.
- **Последствие:** срок 30.09 расходуется на ожидание решений; потеря истории и просрочка старта 3.1 происходят без владельца и без сигнала.

## §3. Глоссарий

### A-06 · high · «Заказы без isCancel» - величина предварительная, но в документе нет ни термина, ни правила показа

- **Место:** `prd.md:204` (глоссарий: заказы за день по полю `date`, без `isCancel`), `prd.md:206` («полный день»), `prd.md:279` (FR-26: поздние отмены и возвраты до двух недель пересчитывают версии затронутых дней), `prd.md:344`.
- **Триггер:** 30.09 сводка показывает 31 заказ за 29.09; через неделю тот же день - 28 заказов (пришли отмены). Окно нормы из 14 дней всегда смешивает «устоявшиеся» дни и дни моложе лага отмен.
- **Guard / правка:** ввести термин («предварительный день» / флаг `settled` у версии дня) и требование: сводка помечает дни моложе лага отмен, а норма либо считается на устоявшихся днях, либо документирует смещение; оценить смещение по фикстурам 30.08 (доля `isCancel`, пришедших позже дня заказа) и записать число.
- **Последствие:** цифры не сходятся сами с собой и с кабинетом (усиливает A-03); ML-контур (§11.3) позже учится на ряде с систематическим смещением, о котором нигде не сказано.

## §4.0. Источники данных

### A-07 · medium · Часовой пояс внешнего потребителя квоты противоречит замеру (UTC против МСК)

- **Место:** `prd.md:265` («~00:51 UTC»), `prd.md:775` (OQ-10, «~00:51 UTC»). Против `docs/state/API-FACTS.md:45`: «создаёт `detail_history_report` ежедневно около 00:51 **МСК**».
- **Триггер:** планирование `funnel_csv` (понедельник 06:30 МСК) и дележа квоты 20 отчётов/сутки: разница в 3 часа переносит событие через границу суток, а часовой пояс сброса квоты нигде не зафиксирован.
- **Guard / правка:** привести PRD к формулировке замера (или исправить `API-FACTS` по `createdAt` артефакта, явно указав пояс) и отдельной строкой записать «пояс сброса дневной квоты - UNKNOWN до замера».
- **Последствие:** PRD нарушает собственное правило «замер побеждает спецификацию» (`prd.md:249`) против собственного же источника; CSV-задание может планироваться в сутки, уже израсходованные чужим потребителем.

### A-08 · high · Нет бюджета вызовов и времени на утреннее окно и нет правила для нескольких кабинетов

- **Место:** `prd.md:255-263` (лимиты), `prd.md:318` (`[ASSUMPTION]` 201 nmId = 11 вызовов), `prd.md:381-385` (05:30 старт, 06:30 крайний срок), `prd.md:193`/`prd.md:683` (второй tenant = инстанс того же таймера), `prd.md:993` (риск «один VPS» упоминает только ML и бэкфилл).
- **Триггер:** ассортимент растёт (201 nmId - факт августа, потолка нет: 500 nmId = 25 вызовов = 9 минут только на воронку), либо второй и третий кабинеты запускают `proxima-morning@` в те же 05:30 на том же VPS и том же Postgres.
- **Guard / правка:** внести в §4.0/§15 бюджет: вызовы и время по шагам внутри окна 05:30-06:30, формула роста (`вызовы = ceil(активные nmId / 20)` при 3/мин), правило разнесения инстансов таймера по tenant (смещение или `RandomizedDelaySec`) и алерт при превышении бюджета.
- **Последствие:** срок 06:30 объявлен без опоры; подключение второго кабинета молча ломает гейт первого; рост ассортимента обнаруживается как деградация, а не как исчерпанный бюджет.

## §4.A. Данные кабинета собираются сами (M-01, M-01b)

### A-09 · critical · Шаг воронки стоит перед нормой и сводкой в цепочке «стоп на первой ошибке» - октябрьская ценность блокирует сентябрьский гейт

- **Место:** `prd.md:381` (FR-22: `collect → funnel_v3 → norm → brief` строго последовательно, стоп на первой ошибке), `prd.md:312-323` (FR-30: воронка в сводке - октябрь), `prd.md:1002` (§15, та же цепочка). Подтверждается `epics.md` Story 1.12 («шаги `funnel_v3`/`norm`/`brief` добавляются своими эпиками») и Story 3.1.
- **Триггер:** после мержа Story 3.1 (сентябрь) любой сбой v3 - 429 сверх трёх повторов AD-4, 5xx, дрейф схемы, сетевой таймаут - останавливает цепочку до `norm` и `brief`. Сводка за вчера не материализуется, `/brief` уходит в `stale`, счётчик «7 утр подряд» обнуляется.
- **Guard / правка:** записать в FR-22 требование: сентябрьская сводка не зависит от шага воронки - либо `funnel_v3` переносится ЗА `brief`, либо выносится в отдельный юнит со своим `OnFailure`, не влияющий на утренний прогон; оформить как change request к AD-6 (в OQ-16 этого пункта сейчас нет).
- **Последствие:** единственный гейт сентября (SM-6) становится заложником capability, чья ценность заявлена на октябрь; риск отсутствует в §14, поэтому митигации нет ни у кого.

### A-10 · critical · Единственная копия истории до окна WB лежит в untracked-фикстурах и не покрыта требованием бэкапа

- **Место:** `prd.md:288-290` (FR-27: бэкфилл из двух артефактов 30.08, запуск - Story 1.5/1.14, дата отсечки - решение Mike), `prd.md:1006` (§15: бэкап = Postgres и `PROXIMA_RAW_DIR`), `prd.md:870` (§11.1). Факт: артефакты 30.08 - `fixtures/wb-api/` (~436 МБ, gitignored) на VPS и в клоне; в `PROXIMA_RAW_DIR` они попадают только после `cas_import` в Story 1.14 (`epics.md` Story 1.14).
- **Триггер:** к 30.09 скользящее окно WB стоит примерно на 01.04 - март-август 2026 из API уже не вернуть. До запуска 1.14 достаточно уборки фикстур, потери диска VPS или ошибочного `--retrieved-at`, чтобы история исчезла безвозвратно.
- **Guard / правка:** внести в FR-27 и §15 требование к защищённому активу: контрольные суммы двух артефактов, вторая копия в объектном хранилище, проверка целостности до старта Story 1.5, включение их в набор бэкапа немедленно (не после 1.14); утрата = `BLOCKED` для M-02 с явным сообщением.
- **Последствие:** без них не строится ни норма (14 полных дней), ни проверка CAP-2 (W10 = 649/700 860 ₽, W35 = 225/263 089 ₽), ни любой backtest §11.3 - и восстановить нечем: API окно закрыл.

### A-11 · high · «Активный nmId» в PRD не определён, а нисходящее определение выбрасывает ровно те SKU, ради которых строится диагноз

- **Место:** `prd.md:317-318` (FR-30), `prd.md:730` (SM-7), `prd.md:566` (FR-36), `prd.md:577` (FR-37). Определение живёт только в `epics.md` Story 3.1: «активные nmId = distinct из `stg_wb_orders_latest` за 30 дней».
- **Триггер:** у SKU заказы упали до нуля → через 30 дней он выходит из активного набора → его воронка перестаёт сниматься → в M-04 (FR-8, «локализация этапа воронки») и в FR-36 у обвалившегося SKU нет данных именно за период обвала. Плюс SM-7 «8 недель по каждому активному nmId» неизмерима, пока набор меняется ежедневно.
- **Guard / правка:** определить термин в §3/FR-30 (окно, источник, минимальный порог), потребовать фиксацию набора на прогон (снимок с `run_id`) и удержание SKU в наборе N дней после последнего заказа; из определения вывести потолок вызовов (см. A-08).
- **Последствие:** слепое пятно диагноза совпадает со случаем, ради которого продукт существует; метрика SM-7 не проверяема.

### A-12 · high · FR-30 не описывает поведение при исчерпанной квоте async-отчётов

- **Место:** `prd.md:321-323` (не более одного отчёта в сутки; `[ASSUMPTION]` квоты хватает), `prd.md:792` (§9), `prd.md:991` (риск).
- **Триггер:** внешний потребитель (A-31) выбирает квоту 20 отчётов/сутки, или создание отчёта в понедельник возвращает ошибку квоты.
- **Guard / правка:** дописать consequence: исчерпанная квота → прогон `FAILED` с отдельной причиной, алерт и повтор на следующие сутки; пропуск CSV виден в статусе данных воронки, а не молчит; квота 20 указана как значение из кода репозитория и подлежит замеру.
- **Последствие:** воронка тихо не накапливается; провал SM-7 обнаруживается в конце октября, когда пропущенные дни уже невосстановимы (v3 - окно 7 дней).

### A-13 · medium · Для ежедневного `collect` не описан потолок страницы и пагинация после простоя

- **Место:** `prd.md:279` (окно инкремента `dateFrom = min(run_day − 3, last_full_day + 1)`), `prd.md:289` (80 000 строк и пагинация названы только для бэкфилла).
- **Триггер:** таймер стоял неделю (или `Persistent=true` догоняет пропуск после перезагрузки VPS) - инкремент запрашивает большой интервал; потолок страницы и лимит `sales` 1/мин действуют и здесь.
- **Guard / правка:** распространить требование на FR-26: `collect` использует ту же пагинацию и тот же бюджет, что бэкфилл; прогон, не укладывающийся в утреннее окно, продолжается отдельным прогоном и не блокирует сводку.
- **Последствие:** первое утро после простоя обрывается по времени или пишет версию дня из усечённой страницы - тихая потеря данных при формально SUCCEEDED прогоне.

## §4.B. Норма и утренняя сводка (M-02, M-03)

### A-14 · high · Норма не защищена от известных искажений: акций WB, дней отсутствия товара, смены ассортимента

- **Место:** `prd.md:329-337` (FR-31), `prd.md:207-208` (глоссарий), `prd.md:421-429` (паспорт клиента: `weekend_days`, `priority_categories`, но никакого календаря акций). Слова «акция», «промо», «OOS» применительно к норме в документе отсутствуют; для ML-контура правило исключения OOS-дней есть (`prd.md:895`), для M-02/M-03 - нет.
- **Триггер:** кабинет участвует в акции WB (или топовый SKU уходит в ноль по остаткам) внутри окна 14 дней. Медиана смещается: назавтра после акции появляется «аномалия −40 %», а реальный провал во время акции маскируется.
- **Guard / правка:** минимум - записать это как ограничение M-02/M-03 отдельным testable consequence («норма не корректируется на акции и OOS; ложные срабатывания ожидаемы и учитываются при калибровке OQ-7»). Лучше - добавить в паспорт клиента отметку дней акций/OOS и исключать их из окна с уменьшением `sample_days`.
- **Последствие:** первые живые недели дают ложные тревоги; порог OQ-7 калибруется на загрязнённых данных и закрепляет искажение в M-04.

### A-15 · high · Не определено поведение при нулевой или вырожденной норме

- **Место:** `prd.md:208` (отклонение = (вчера − норма)/норма), `prd.md:331-337` (FR-31), `prd.md:340-346` (FR-32). В AD-8 этого случая тоже нет.
- **Триггер:** `норма = 0` - новый кабинет, период полного отсутствия товара, а на уровне SKU/категории (FR-8, FR-36 - «против собственной нормы SKU») это регулярный случай для редко заказываемых артикулов.
- **Guard / правка:** задать правило в FR-31/FR-32: `норма = 0` → `deviation_pct = UNKNOWN` (или статус `insufficient`), в payload никогда не попадают `Infinity`/`NaN`; отразить в `contracts/brief.schema.json` (nullable-отклонение) - это правка контракта, а не только текста.
- **Последствие:** невалидный payload не проходит валидацию контракта, прогон `brief` падает, утренней сводки нет - ровно в те утра, когда кабинет стоит в нуле.

### A-16 · high · SM-6 требует «без ручного запуска», но эффект ручного перезапуска на счётчик не описан

- **Место:** `prd.md:344`, `prd.md:729` (SM-6), `prd.md:385` (крайний срок цикла), `prd.md:1008` (наблюдение после релиза).
- **Триггер:** на пятое из семи утр прогон падает; инженер запускает `morning_run.sh` руками в 09:00, сводка появляется.
- **Guard / правка:** задать в SM-6 и §13: ручной перезапуск обнуляет серию (либо засчитывается с пометкой), серия считается автоматическим запросом по `collector_runs`/`brief_daily`, артефакт подсчёта прикладывается к гейту.
- **Последствие:** гейт объявляется пройденным на неделе, которую вели руками, - или отвергается из-за одной починки, правила для которой никто не согласовал.

## §4.C. Аномалии с приоритетом (M-04)

### A-17 · high · «Деньги под риском» считаются в `finishedPrice` - это не деньги продавца

- **Место:** `prd.md:418` (FR-9: `rub_assessment` по выручке `finishedPrice`), `prd.md:205` (глоссарий: выручка = сумма `finishedPrice`), `prd.md:916` (CM-13 знает про правило «не вычитать дважды», но FR-9 об этом молчит). Поле `forPay` присутствует в тех же строках `sales` (API-FACTS §B/C) - нового источника не требуется.
- **Триггер:** провал на 100 000 ₽ по `finishedPrice` показывается как потеря, тогда как перечисление продавцу меньше на комиссию и логистику; при ранжировании (FR-1) сравниваются SKU с разной комиссией, и порядок систематически неверен.
- **Guard / правка:** явно задать семантику метрики в FR-9 («оценка по цене покупателя, не по перечислению; завышает потерю на комиссию и логистику») и записать переход на `forPay` как следующий шаг с датой; до перехода - дисклеймер в строке аномалии.
- **Последствие:** приоритизация «смотреть первым на дорогое» ошибается между категориями; первые же цифры «деньги под риском», показанные клиенту, не сходятся с финотчётом WB.

### A-18 · medium · Порог сверки кабинетного ряда с суммой по nmId повешен на OQ, который отвечает на другой вопрос

- **Место:** `prd.md:404` («порог расхождения и каденция - `UNKNOWN` до OQ-7»), `prd.md:776` (OQ-7 = порог тревоги и правило «сигнал против шума»). В спайне это отдельный Deferred-пункт (quality-check в M-04).
- **Триггер:** Story 4.1 нужен критерий quality-check; OQ-7 закрывается решением о пороге тревоги и вопрос сверки остаётся без владельца.
- **Guard / правка:** завести отдельный OQ (владелец - архитектура) либо зафиксировать порог сразу («расхождение > 2 % за полный день → quality-check FAILED, сигнал не публикуется»), со ссылкой на Deferred-пункт спайна.
- **Последствие:** висячая ссылка: проверка качества уезжает в реализацию без критерия либо тихо выпадает из AC.

### A-19 · high · Октябрьский объём требует решения, которое по собственным условиям PRD принимается во второй половине октября

- **Место:** `prd.md:448-456` (FR-34: порог задаёт Mike после первых живых сводок), `prd.md:776` (OQ-7: «после 2-3 недель живых сводок, после релиза 2.6»), `prd.md:705-710` (§6.2: 10 единиц октября), `prd.md:996` (риск), `prd.md:968-969` (§13).
- **Триггер:** релиз 2.6 в лучшем случае 23-30.09 → 2-3 недели наблюдения → порог около 15-20.10 → Story 4.2 (а за ней 4.3 и весь Epic 5) стартуют, когда от октября осталось ~10 дней.
- **Guard / правка:** развести: (а) сократить наблюдение до недели с явно предварительным порогом и перекалибровкой; (б) разрезать Story 4.2 на «ранжирование по деньгам» (порога не требует) и «порог тревоги» (позже); (в) либо честно перенести Epic 4/5 в §6.2 и §13 на ноябрь.
- **Последствие:** октябрьский план арифметически невыполним; расхождение обнаружится в середине октября, когда единицы уже распределены, - повтор сентябрьского сценария на новом круге.

## §4.D. План действий (M-05)

### A-20 · high · Внешний LLM встраивается в утреннюю цепочку без таймаута, бюджета и правила деградации

- **Место:** `prd.md:529-538` (FR-35), `prd.md:381` (цепочка «стоп на первой ошибке»), `prd.md:998` (риск: только одобрение, флаг, аудит), `prd.md:761`/`prd.md:780` (OQ-15 - только про ПДн покупателей).
- **Триггер:** октябрь, шаг диагноза добавлен в `morning_run.sh` по образцу A-09; провайдер отвечает медленно, недоступен или упирается в лимит в 05:40.
- **Guard / правка:** записать в FR-35: таймаут и потолок вызовов/стоимости на одно утро; деградация при `llm_enabled=false` или ошибке - «диагноз недоступен», сводка выходит без него; шаг диагноза не может блокировать `brief`. Отдельно - что уходит провайдеру: цифры кабинета клиента за пределы РФ требуют согласия владельца кабинета, а OQ-15 сейчас покрывает только ПДн покупателей.
- **Последствие:** инцидент внешнего провайдера гасит утреннюю сводку по всем кабинетам; расход без потолка; коммерческие данные клиентского кабинета уезжают наружу без записанного решения.

### A-21 · high · Первая запись из UI происходит, пока auth заморожен: поле «кто» в решении непроверяемо

- **Место:** `prd.md:474-481` (FR-5), `prd.md:511-518` (FR-16, `[NOTE FOR PM]` про атомарность), `prd.md:1012` (§15: до октября ssh-туннель, `WEBAPP_REQUIRE_AUTH=false`), `prd.md:887` (§11.2), `prd.md:764` (OQ-8), AD-18 (заморозка auth-зоны до октября).
- **Триггер:** Story 5.3 приходит в октябре; auth тоже «октябрь», порядок между ними нигде не задан; `/brief` открыт без авторизации.
- **Guard / правка:** сделать auth (или хотя бы идентифицированного писателя) предусловием первой записи из UI и записать это в §13 как зависимость; до тех пор поле автора - константа с явной пометкой «не аутентифицировано»; для клиентского кабинета - запрет записи без auth.
- **Последствие:** журнал решений - главный дифференциатор по §16 - несёт неатрибутируемого автора, и доказательная ценность петли обнуляется; для второго (клиентского) tenant это ещё и дефект контроля доступа.

## §4.E. Отложенные требования v1

### A-22 · high · «Условия возврата» названы, но у двух из них нет владельца и даты, а обе уже на критическом пути октября

- **Место:** `prd.md:544-554` (таблица §4.E: FR-10/FR-11 ждут AD об остатках, FR-13 ждёт путь ручного ввода), `prd.md:758`/`prd.md:764` (OQ-13 и OQ-8 отнесены в блок «M-06+ и источники», то есть после сентября), `prd.md:971` (§13, M-06+).
- **Триггер:** октябрь: Story 5.3 требует нового AD записи из webapp (G-9/OQ-8), а CM-9/FR-37 - AD об остатках (OQ-13, разведка уже выполнена 02.09). Ни у одного нет автора-архитектора с датой; в очереди §8.0 они стоят в самом дальнем блоке.
- **Guard / правка:** поднять OQ-8 и OQ-13 в блок «блокируют ближайшие единицы» с владельцем и датой до планирования октябрьских единиц; в §13 пометить их как блокеры конкретных историй, а не как фоновые вопросы.
- **Последствие:** два октябрьских эпика стартуют заблокированными на архитектурную работу, которую никто не запланировал; лидер матрицы отбора (CM-9, 89 баллов) стоит из-за отсутствующей записи AD, а не из-за данных.

## §4.F. Модули решений и прогнозов (M-06+)

### A-23 · medium · У `[PROPOSED]`-модулей есть testable consequences, построенные на непроверенных числах спецификации

- **Место:** `prd.md:556-563` (общая форма требования модуля), `prd.md:587-596` (FR-38), `prd.md:598-605` (FR-39: 3 запроса/мин, 30/100 запросов, обновление раз в час - «живой проверки не было»), `prd.md:607-615` (FR-40), `prd.md:794-795` (§9).
- **Триггер:** Mike утверждает модуль → AC пишутся по зеркалу OpenAPI (R2), которое ни разу не проверено живым вызовом, при том что §4.0 сам объявляет приоритет замера.
- **Guard / правка:** внести в шаблон §4.F обязательное предусловие: «живая разведка по образцу API-FACTS до написания AC; числа спецификации в AC не используются, только в обосновании» - зеркально правилу §4.0.
- **Последствие:** единицы M-06+ планируются на числах, которые по собственному правилу проекта проигрывают замеру; повторяется история `reportDetailByPeriod` и лимита остатков.

### A-24 · medium · Не сказано, какие события FR-36 восстанавливаются задним числом, а какие копятся только вперёд

- **Место:** `prd.md:566-573` (FR-36: цена и скидка из строк заказа, доля склада из поля отгрузки, появление/исчезновение в воронке, отзывы, реклама).
- **Триггер:** модуль принят и от него ждут объяснения провалов прошлого месяца; часть ряда (цена, склад) восстановима из уже собранных строк заказов, часть (воронка, отзывы, реклама) начинается с нуля в день запуска.
- **Guard / правка:** разделить перечень событий на «восстанавливается из истории заказов» и «копится только вперёд», и из второго вывести дату, с которой модуль начинает давать диагноз.
- **Последствие:** модуль запускается и месяцами не даёт диагноза - повтор ошибки с окном воронки (D20), которую проект уже один раз оплатил.

## §4.G. Карта FR

### A-25 · low · В карте нет колонок «носитель истории» и «блокер», поэтому требования без носителя неотличимы от остальных

- **Место:** `prd.md:617-659` (карта FR → ступень → статус → раздел); FR-20/FR-21 без истории (`prd.md:421`, `prd.md:431`), FR-38/FR-40 заблокированы токенами (`prd.md:655-657`).
- **Триггер:** читатель планирует октябрь по §4.G и не видит, что у двух требований нет носителя, а у двух нет доступа к источнику.
- **Guard / правка:** добавить колонки «история/носитель» и «блокер (токен, AD, решение)».
- **Последствие:** требования без владельца остаются невидимыми до момента, когда спринт уже собран.

## §5. Non-Goals

### A-26 · high · Гарантия «WB WRITE - никогда» держится на правиле, сформулированном только внутри октябрьского `[PROPOSED]`-требования

- **Место:** `prd.md:686` (§5: WRITE - никогда), `prd.md:248` (§4.0: гейт запрещает URL вне реестра и write-эндпоинты), `prd.md:594` (FR-38: «методы остановки и паузы кампаний вызываются через GET, поэтому запрещаются явно и по полному пути, а не по HTTP-методу»), `prd.md:868` (§11.1), `prd.md:915` (CM-4).
- **Триггер:** в реестр AD-4 добавляется новый источник (остатки по OQ-13, финансы по OQ-14), а детектор write-эндпоинтов в гейте различает мутации по HTTP-методу.
- **Guard / правка:** поднять правило на уровень guardrail §11.1: allowlist и запрет - по полному пути, не по методу; потребовать в `tools/verify_wb_client.py` тест-канарейку с GET-мутацией; в §5 сослаться на этот механизм, а не на общее «гейт запрещает write».
- **Последствие:** самое жёсткое обещание продукта опирается на деталь, спрятанную в октябрьском предложении; одна GET-мутация в реестре - и запрет обойдён молча.

## §6. Объём

### A-27 · high · Ветка отказа гейта выбрасывает runbook, от которого зависит сам релиз

- **Место:** `prd.md:962` (ветка отказа, пункт 3: «первыми выпадают 1.13 (runbook) и 1.8 (сандбокс)»), `prd.md:700` (§6.1, порядок), `prd.md:1008-1009` (§15: релиз только с записанным планом отката; `AGENTS.md` Policy - то же).
- **Триггер:** 15.09 срабатывает триггер темпа → 1.13 снимается → Story 1.14 (первый релиз на сервер, `[Claude]`) выполняется без runbook и без записанного плана отката, нарушая правило §15 и D7 в самый нагруженный момент.
- **Guard / правка:** пометить 1.13 как неснимаемую (или свести её к минимальному плану отката внутри 1.14) и выбрать другую жертву; проверить, что 1.7 (`delete_run`) остаётся, иначе откат по прогонам невозможен физически.
- **Последствие:** ветка отказа заставляет проект нарушить собственное правило деплоя ровно тогда, когда цена ошибки максимальна.

## §7. Метрики успеха

### A-28 · high · Нет операционной метрики надёжности и нет наблюдения за самим наблюдателем

- **Место:** `prd.md:719-735` (§7 целиком), `prd.md:726` (SM-3: исходный замер `UNKNOWN`, «замер до и после релиза 2.6», при том что 2.6 - последняя единица сентября и владелец замера не назван), `prd.md:1002-1005` (§15: алерты есть, целевого значения нет), `prd.md:297` (dead-man = `stale` на `/brief`).
- **Триггер:** умирает канал алертов (токен бота, удалённый канал) или падает сам юнит `proxima-alert@`; единственный dead-man для человека - `stale` на экране, который в выходные никто не открывает. Отдельно: SM-3 без «до» замера нечем измерить после релиза.
- **Guard / правка:** добавить метрику надёжности (доля утр со SUCCEEDED-сводкой до 06:30: 7/7 в неделю гейта, ≥ 95 % далее) и требование heartbeat - положительный ежедневный сигнал «цикл прошёл», чтобы тишина сама была тревогой; проверку канала алертов включить в еженедельный restore-check; SM-3 - назначить владельца и дату замера «до» (до релиза 2.6) или снять метрику.
- **Последствие:** сбои молчат; разрыв серии из семи утр выясняется постфактум, когда гейт уже провален; SM-3 остаётся неизмеримой навсегда.

## §9. Индекс допущений

### A-29 · medium · У допущений нет владельца, срока проверки и последствия фальсификации

- **Место:** `prd.md:786-796` (перечень `[ASSUMPTION]` v2).
- **Триггер:** допущение о дневной квоте async-отчётов (FR-30) оказывается ложным - вместе с ним умирает SM-7, но по индексу этого не видно; допущение про 10/мин для `orders` перестаёт действовать «до повышения частоты сбора» - кто и когда это проверит, не сказано.
- **Guard / правка:** превратить перечень в таблицу с колонками «владелец / дата проверки / что ломается, если ложно / где проверяется» и связать каждое допущение с метрикой или единицей, которую оно убивает.
- **Последствие:** допущения живут бессрочно; момент, когда реальность их опровергла, никем не отслеживается.

## §10. Jira Sync

### A-30 · medium · Флаги «Готово-без-кода» и потерянный скоуп PA-48 остались без владельца и даты

- **Место:** `prd.md:852-854` (строки PMM-8, PA-29, PA-48: «закрыть честно или вернуть в работу», «скоуп потерян»), `prd.md:810` (в этой ревизии записей в Jira нет).
- **Триггер:** приходит approve на запись в Jira - помеченные задачи закрываются вместе с остальными, а потерянный скоуп PA-48 (заказы, маржа, реклама) второй раз никем не заводится.
- **Guard / правка:** каждому флагу - действие и дата («PA-29: вернуть в работу либо закрыть со ссылкой на ветку-артефакт, до approve»); для PA-48 - явное состояние «заведено в CM-4/CM-13/CM-5: да/нет» с датой.
- **Последствие:** история закрывается без артефактов - ровно тот дефект, который зафиксировал BACKLOG-REVIEW; потерянный скоуп теряется повторно.

## §11. Ограничения и guardrails

### A-31 · critical · Read-write analytics-токен боевого кабинета находится у неизвестного потребителя, а трактуется как вопрос квоты

- **Место:** `prd.md:265` (§4.0: «analytics-токен read-write используется только для read-эндпоинтов (PA-13 - ротация в бэклоге)»; «квота делится с внешним потребителем токена»), `prd.md:775` (OQ-10 - в статусе вопроса о квоте), `prd.md:872`/`prd.md:997` (копии токенов вне VPS - «в инвентарь и ротацию», без даты), `prd.md:991` (риск - только про квоту). Факт: `API-FACTS.md:45` - на сервере нет ни таймера, ни cron, ни процесса, создающего эти отчёты.
- **Триггер:** ничего делать не нужно - по состоянию на 30.08 кто-то вне VPS ежедневно использует read-write токен «Аналитика» боевого кабинета; ротация лежит в бэклоге без даты.
- **Guard / правка:** переклассифицировать OQ-10 из «квоты» в инцидент с учётными данными: ротация analytics-токена на read-only (PA-13) с датой до старта Story 3.1/3.0, отзыв старого, установление потребителя; до ротации - запрет планировать новые работы на квоту этого токена. Синхронно - дата инвентаризации копий токенов вне VPS.
- **Последствие:** третья сторона располагает правом записи в кабинет от имени проекта; обещание §5 «любая запись в WB - никогда» не обеспечивается за пределами собственного кода, хотя PRD даёт его как безусловное.

## §12 и §12.1. Реестр кандидатов

### A-32 · medium · Константы реестра без источника и отсутствие правила, как закрывается PENDING разбора конкурента

- **Место:** `prd.md:913` (CM-9: ABC 80/95, `(orders − cancels)/30`, `Z×σ×√L` - формулы приведены как рабочие), `prd.md:920` (CM-13: пороги 18 %/30 %/20 %, «источник UNKNOWN»), `prd.md:941` («Закрывается HAR авторизованной зоны, не догадками»), `prd.md:433` (FR-21: статусы «внешний ориентир / гипотеза»).
- **Триггер:** модуль принимается - константы реестра переезжают в паспорт клиента как значения по умолчанию без источника, в обход FR-20/FR-21. Отдельно: снятие HAR авторизованной зоны чужого продукта предложено без указания законной рамки и без исполнителя, при том что §5 запрещает браузерную автоматизацию и скрейпинг.
- **Guard / правка:** пометить все числовые константы §12 статусом «внешний ориентир, источник UNKNOWN» и запретить их использование как значений по умолчанию без D-записи; явно записать, что закрытие PENDING допустимо только с легального оплаченного аккаунта, вручную, без автоматического сбора.
- **Последствие:** непроверенные чужие пороги становятся продуктовыми умолчаниями; исследовательский шаг соскальзывает в ту самую автоматизацию, которую проект запретил.

## §14 и §15. Риски и операционные требования

### A-33 · high · Нет RPO/RTO, нет требований к ёмкости и росту диска, а три верхних риска в таблицу не попали

- **Место:** `prd.md:1006` (бэкап и восстановление), `prd.md:1010` (ретеншн: ничего не удаляется автоматически - Deferred), `prd.md:993` (риск «один VPS, один Postgres» - только про ML и потерю сервера), `prd.md:870` (§11.1), `prd.md:985-1000` (таблица рисков).
- **Триггер:** CAS растёт на десятки МБ в сутки (полная выгрузка 30.08: `orders` 10.85 МБ + `sales` 8.77 МБ) плюс ночные дампы плюс воронка; удаление отключено; VPS один. Никакого порога свободного места, оценки роста и цели восстановления в документе нет. В таблице рисков отсутствуют: цепочка «стоп на первой ошибке» (A-09), незащищённые артефакты бэкфилла (A-10), исчерпание диска.
- **Guard / правка:** записать RPO/RTO для данных кабинета и для утреннего цикла; оценку роста на кабинет в месяц и порог алерта по свободному месту; дату решения по ретеншну (привязать к OQ-15); добавить три недостающие строки риска с владельцами.
- **Последствие:** исчерпание диска одновременно останавливает утренний цикл и бэкапы - единственный отказ, который ломает и гейт, и путь восстановления; после потери VPS никто не знает, какой объём потерь считается допустимым.

## §16. Международные best practices

### A-34 · low · У строк §16 нет статуса решения, поэтому раздел не меняет объём и не проверяется

- **Место:** `prd.md:1018-1036` (таблица паттернов: колонка «Отражение в PRD» указывает на FR и CM, часть которых не принята).
- **Триггер:** паттерн отображён на кандидата CM-n, который не принят; читатель не может отличить «взято», «отклонено» и «отложено», а аудитор не может проверить, что принцип §0.2.3 действительно применён.
- **Guard / правка:** добавить колонку «решение» (принято → FR/D-запись / отклонено + причина / к рассмотрению) и короткий перечень «что сознательно не берём».
- **Последствие:** доказательный раздел не влияет на требования и не поддаётся проверке; принцип §0.2.3 объявлен, но не операционализирован.
