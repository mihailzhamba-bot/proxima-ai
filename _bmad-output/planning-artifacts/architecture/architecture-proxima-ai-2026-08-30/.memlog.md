---
scope: Лестница M-00..M-05: сбор дневного ряда WB, норма, утренняя сводка на /brief, воронка фоном; кабинет ИП Амировой, один VPS
purpose: Build substrate для OpenHands: инварианты, чтобы независимые единицы работы не разошлись
altitude: initiative→features (ступени M-01..M-05)
updated: 2026-09-09T10:43
---

- (direction) Режим Fast path (Mike, 30.08); единственный артефакт - ARCHITECTURE-SPINE.md с mermaid внутри; папка названа architecture-proxima-ai-2026-08-30 (slug без пробела вместо project_name).
- (direction) Входы: SPEC.md spec-wb-morning-brief (CAP-1..8) + companions; DECISIONS D1-D21 (унаследованы как settled); код-свип brownfield запущен (collector, db, control-plane, ветки pa41/pmm-20/pmm29/ai-pa-50, infra).
- (decision) Парадигма: pipes-and-filters над неизменяемым реестром доказательств (evidence → staging → versioned facts → norm/signals → materialized brief). Ратифицирует существующий raw-store CAS + staging + attempt/lineage. Prevents: слои, пишущие влево.
- (decision) AD-1 [ADOPTED] доказательство раньше факта: любой ответ WB - артефакт CAS + реестр с run_id, парсинг только после. Prevents: факты без источника.
- (decision) AD-2 fact_cabinet_daily (tenant, calendar_day МСК по WB date) append-only с версиями по run_id + view _current; норма и сводка читают только view. [ASSUMPTION] версии вместо upsert-in-place - ради обратимости; альтернатива upsert+audit отклонена.
- (decision) AD-3 прогон обратим целиком: collector_runs, NOT NULL run_id + ON DELETE CASCADE во всех новых таблицах, make delete-run с --dry-run под ролью proxima_run_janitor; существующие attempt_id-таблицы получают run_id в 011. Prevents: невозвратные строки после отката кода.
- (decision) AD-4 один WB-клиент с реестром эндпоинтов и бюджетами (sales 1/мин, orders 10, reportDetail 1, v3 3, downloads 3), RecordedHttpClient, FixtureTransport в тестах, малые обезличенные фикстуры в git ≤ 200 КБ, полные на VPS+S3; сеть в тестах запрещена. [ASSUMPTION] коммит малых фикстур закрывает open question спеки №2.
- (decision) AD-5 расписание = systemd-таймеры на хосте (collect 05:30 → funnel 05:45 → norm 06:00 → brief 06:10 МСК), one-shot процессы, Persistent, OnFailure в Telegram монитора; бэкфилл = collect --from 2026-03-01 вручную при первом деплое. [ASSUMPTION] хост-таймеры, не ofelia/cron в compose - monitor уже так живёт, in-process таймеры запрещены верификатором.
- (decision) AD-6 норма считается один раз модулем control-plane/norm (медиана 14 полных дней, D21) и материализуется в norm_daily; сводка и детекторы не считают сами; scn001 (среднее 7/14/28 + weekday) остаётся внутри сценария M-04 через адаптер loader → fact_*_current.
- (decision) AD-7 сводка материализована в brief_daily.payload по contracts/brief.schema.json; webapp postgres-provider = два SELECT под webapp_readonly на view brief_current/data_status_current, без бизнес-логики; stale/blocked → предупреждение вместо цифр (CAP-3).
- (decision) AD-8 [ADOPTED] контракты = JSON Schema в contracts/ (принять signal/diagnosis/decision-record из pmm29, добавить cabinet-daily, norm, brief), TS через make codegen, Python через pydantic + jsonschema; ручные types.ts из ai/pa-50 удаляются после codegen; schema_version.
- (decision) AD-9 среды: proxima + proxima_test на одном Postgres, make test-db-refresh = pg_dump|psql; роли source_publisher / norm_publisher / webapp_readonly / run_janitor / sandbox (только proxima_test); OpenHands .env.task = роль proxima_sandbox; пустые proxima_dev выводятся.
- (decision) AD-10 один канонический чекаут на сервере ~/proxima-ai = origin/main + тег vYYYY.MM.DD-N; /srv/proxima-ai/repo и webapp-staging выводятся первым релизом; деплой только по «деплой»: checkout tag → apply-migrations → compose up → restart timers → CHANGELOG; откат = prev tag + delete-run; миграции не откатываются. [ASSUMPTION] канон = ~/proxima-ai.
- (decision) AD-11 [ADOPTED] tenant_id + RLS по образцу 009 в каждой новой таблице; job делает set_config proxima.tenant_id; второй tenant = строка + секреты без изменения схемы.
- (decision) AD-12 [ADOPTED] миграции additive-only self-checksum; номера 011 run ledger, 012 stg orders/sales, 013 fact_cabinet_daily+view, 014 norm_daily, 015 brief_daily+роли; ветки pa41/PA-03-02 перенумеровываются при мерже.
- (decision) AD-13 направление зависимостей: WB → collector → PG → control-plane → PG → webapp; общий код только через contracts/ и БД; никаких импортов между сервисами (mermaid в спайне).
- (constraint) Из спеки унаследовано без переоткрытия: WB только READ; канал /brief; без auth до октября; тесты на фикстурах; run_id; multi-tenant; норма D21; переиспользование веток через ревью; сервер только чтение; единица работы = сеанс+PR+результат; первая единица M-01 до 08.09.
- (version) Стек ратифицирован из репо (не с веба): Node 22.23 (engines >=22 <23), TS 5.8.3, pg 8.16.3, ajv 8.20.0, tsx 4.20.3, Next 16.3.3, React 19.2.8, drizzle 0.45.2, better-auth 1.7.1, vitest 4.1.11, Python 3.14 / uv 0.11.7, psycopg 3.3.4, pydantic 2.13.4, jsonschema 4.25.1, pytest 8.4.2, postgres:16.10-alpine, json-schema-to-typescript 15.0.4. Новых технологий спайн не вводит; systemd - хостовый.
- (assumption) Инкремент сбора: dateFrom = today-3 с flag=0 (WB отдаёт изменённые по lastChangeDate строки, отмены/возвраты доезжают позже) + ON CONFLICT по srid/saleID; пересборка версии дня для затронутых дней.
- (question) Порог тревоги M-04 - Deferred (Mike после первых сводок). LLM-провайдер diagnosis - Deferred M-05.
- (event) Черновик ARCHITECTURE-SPINE.md записан (Fast path, 13 AD, 4 [ASSUMPTION]); далее reconcile inputs + reviewer gate.
- (event) Reviewer gate 1: lint 0; reconcile-inputs 3 high/6 med; rubric 2 critical + 6 high; adversarial 16 дыр (5 critical); verified-current 85 проверено, 10 расхождений. Отчёты в reviews/.
- (decision) AD-1 расширен: единый реестр wb_raw_artifacts(run_id → collector_runs CASCADE), locator artifact://wb/sha256; business_signal_raw_artifacts не трогаем (закрывает rubric C2, adversarial H5).
- (decision) AD-2 переписан: staging = наблюдения с PK (tenant, srid|sale_id, last_change_at), view _latest как единственный вход агрегатора, пересчёт всех затронутых дней из всех наблюдений; текущий день не версионируется; fact_order_counts (CSV nmId) не подменяет кабинетный ряд (закрывает reconcile 1/8, adversarial H1, rubric H3).
- (decision) AD-3 переписан: _current = max finished_at среди SUCCEEDED, версии+статус в одной транзакции; collector_run_inputs и транзитивный delete_run.py; существующие таблицы получают collector_run_id NULL без CASCADE (закрывает adversarial H2/H4, verified 4).
- (decision) AD-4 переписан: реестр без reportDetailByPeriod/stocks; новый гейт tools/verify_wb_client.py вместо правки verify_business_signal.py; [ASSUMPTION] flag=0 + dateFrom=run_day-3 проверяется двумя read-вызовами в первой единице M-01 (закрывает reconcile 3, rubric H1, verified 1).
- (decision) AD-5 новый: воронка - два источника (v3 окно 7 дней, csv раз в неделю через wb_async_report.py как грандфазер-исключение с collector_run_id), один словарь COLUMN_MAP, fact_funnel_daily_current предпочитает csv; не более 1 async-отчёта в сутки (закрывает reconcile 7/8, rubric H2, adversarial H8).
- (decision) AD-6 переписан: один утренний юнит proxima-morning@ с последовательными шагами, OnCalendar с Europe/Moscow, OnFailure → proxima-alert@; шаги = docker compose run one-shot образов collector/control-plane (снимает отсутствие uv/psql на хосте); бэкфилл с пагинацией по lastChangeDate при 80k (закрывает reconcile 2, rubric H4/H6, adversarial H6/H10).
- (decision) AD-7 новый: единый helper mskDay/msk_day; полный день = < run_day и есть версия в _current; stale считается только view data_status_current (закрывает reconcile 6/9, adversarial H2/H6/H10).
- (decision) AD-8 норма: окно = 14 полных дней перед evaluation_day, оцениваемый день исключён [ASSUMPTION]; sample_days<14 → insufficient (закрывает adversarial H9, reconcile 10).
- (decision) AD-9 сводка: WEBAPP_TENANT_ID + set_config на connect; статусы ok|stale|insufficient|blocked; WEBAPP_DATA_MODE=postgres только после первого SUCCEEDED brief (закрывает adversarial H3/H7, rubric H5).
- (decision) AD-10 контракты: владелец brief/norm = control-plane; codegen расширяется на webapp; Python = dataclass + jsonschema (существующий паттерн, без pydantic); деньги строкой в JSON (закрывает verified 3, adversarial H7).
- (decision) AD-11 новый: ledger создаёт только proxima_* NOLOGIN-роли и SELECT/INSERT-гранты; LOGIN-пользователи, GRANT DELETE janitor, база proxima_test и proxima_sandbox - в infra/bootstrap/provision-runtime-roles.sh вне ledger; job никогда не superuser; RLS-тест под реальной ролью в make verify (закрывает rubric C1/H5, verified 6).
- (decision) AD-12 среды: Prevents переформулирован - сандбокс видит копию данных кабинета сознательно (D8), но не боевую базу (закрывает reconcile 5, adversarial H11).
- (decision) AD-13 RLS: политики FOR ALL USING + WITH CHECK, view WITH (security_invoker=true), set_config(…, true) внутри одной транзакции в TS; tenant сентября = существующий amirova-test (закрывает adversarial H3, rubric medium).
- (decision) AD-14 миграции: план 011-016; pa41 перенумеровывает после 016, PA-03-02 списывается как старая версия того же кода (D14 уточнён).
- (decision) AD-15 канон = /srv/proxima-ai/repo (vps-contract.json, initdb-mount, ProtectHome), ~/proxima-ai - зеркало администратора; деплой через образы + overlay webapp.staging.compose.yaml (auth off) [ASSUMPTION] (закрывает reconcile 4, rubric H6, verified 5).
- (decision) AD-16 граф зависимостей включает tools/wb_async_report.py и CSV-путь; AD-17 наблюдаемость/бэкап/ретеншн (закрывает rubric измерение «операции»); AD-18 заморозки на сентябрь.
- (event) Спайн v2 распилен: 18 AD, 4 [ASSUMPTION] (flag=0 семантика, окно нормы без оцениваемого дня, хост-таймеры + контейнерные one-shot, канон /srv/proxima-ai/repo). Далее: повторный враждебный проход по v2.
- (event) Reviewer gate 2 (adversarial-v2): 16/32 старых закрыто, 15 частично, 1 нет (async CSV не вызывался); 14 новых дыр (N1 модель транзакций, N2 janitor под RLS, N3 гранты на базовые таблицы, N4 дни без строк/floor, N5 статус цифр на /brief, N6-N7 funnel_csv, N8-N9 образы/секреты/.env, N10-N14). Отчёт reviews/review-adversarial-v2.md.
- (decision) v3 AD-1: locator остаётся artifact://business-signal/sha256 (raw-store.ts заморожен), wb_raw_artifacts с tenant_id и source; строка артефакта autocommit по получении (N1, N11, N13).
- (decision) v3 AD-2: canonical_sha256 в наблюдениях, конфликт PK с другим payload = WB_SCHEMA_DRIFT; версии для каждого дня [floor, run_day-1] включая нулевые; floor = mskDay(dateFrom) / --from+1; dateFrom collect = min(run_day-3, last_full_day+1); агрегатор пишет distinct run_id наблюдений в run_inputs; проверка уникальности srid в первой единице (N4, мелочи).
- (decision) v3 AD-3: три вида транзакций (RUNNING autocommit; артефакты autocommit; obs+versions+inputs+SUCCEEDED одна транзакция; FAILED отдельный autocommit); частичных данных не существует; delete_run --tenant --run обязательны; janitor NOLOGIN + FOR ALL политики в ledger 011, LOGIN + GRANT DELETE в bootstrap; удаление бэкфилла = замыкание на все collect (N1, N2).
- (decision) v3 AD-5: funnel_csv = фаза 1 wb_async_report.py под owner-URI (исключение из AD-11 до M-04, автосоздание tenants удаляется) + фаза 2 промоушен stg_wb_nm_report_rows → stg_wb_funnel_obs(csv) → fact_funnel_daily под proxima_job_collector в TS; таймер понедельник 06:30; [ASSUMPTION] async CSV create/status/file и глубина startDate не вызывались - проверка ≤ 3 вызовами в единице CAP-6 (N6, N7, V-10).
- (decision) v3 AD-6: kind с _, юниты с -; юниты User=root, ProtectHome=true, без ProtectSystem/PrivateDevices; образы collector/control-plane Dockerfile + корневой .dockerignore; USER 1010 proxima-jobs; секреты 1010:1010 0600 через compose secrets:; PROXIMA_GIT_SHA/PROXIMA_IMAGE_ID через env; OnFailure на .service (N8, мелочи).
- (decision) v3 AD-7: SQL-форма дня (now() AT TIME ZONE Europe/Moscow)::date, CURRENT_DATE в db/ запрещён; stale = COALESCE(…, true); collected_at по kind IN (collect, backfill) (N5).
- (decision) v3 AD-8: norm_daily UNIQUE(tenant, evaluation_day, metric, run_id) + norm_daily_current; окно календарное [eval-14, eval-1], sample_days = дни с версией, <14 → insufficient; psycopg в dependencies (N12, V-6).
- (decision) v3 AD-9: brief читает norm_daily_current за brief_day и пишет run_inputs; brief_current = одна строка на tenant; webapp владеет своим Pool, set_config на connect с catch, WEBAPP_TENANT_ID валидируется; цифры только при status=ok AND stale IS FALSE AND brief_day = last_full_day (N5, N12, вопрос 8).
- (decision) v3 AD-11: гранты и политики только на базовые таблицы под security_invoker (перечень по ролям); UPDATE на collector_runs целиком + конвенция + RLS-тест; owner-URI ровно в двух местах (apply-migrations, funnel_csv фаза 1); provision после миграций, REVOKE CONNECT в bootstrap; существующие роли 009 не трогаются (N3, N13, V-7).
- (decision) v3 AD-12: test_db_refresh.sh = terminate → DROP WITH FORCE → CREATE → pg_dump|psql ON_ERROR_STOP → GRANT SELECT sandbox → ALTER ROLE sandbox SET proxima.tenant_id; порядок миграции → provision → refresh; гейт для сандбокс-единиц = CI (N10, мелочи).
- (decision) v3 AD-13: tenant_id во всех новых таблицах включая run_inputs и raw_artifacts; единая схема имён токенов <tenant>_wb_<category>_token через mv (не symlink, O_NOFOLLOW), канон analytics в /etc; .env WB_*_TOKEN_FILE только для probe (N13, N14).
- (decision) v3 AD-14: 011 включает janitor-роль и политики; 015 UNIQUE + view; 016 роли, гранты и политики на базовые таблицы. AD-15: infra/jobs.env + secrets: для control-plane, apply-migrations ENV_FILE=infra/jobs.env, git под proxima-admin, docker через sudo (N9, N8). AD-16 граф: promote CSV в TS, чтение stg_wb_nm_report_rows коллектором. AD-18: tools/ открыт, raw-store.ts заморожен.
- (event) Спайн v3 записан: 18 AD, 5 [ASSUMPTION] (flag=0; async CSV; окно нормы; хост-таймеры + контейнеры; канон /srv). Далее: lint + рендер + финализация.
- (event) spine finalized 2026-08-30: lint 0, 3 диаграммы рендерятся, 18 AD, 5 [ASSUMPTION] на ревью Mike (flag=0 семантика; async CSV глубина; окно нормы без оцениваемого дня; хост-таймеры + контейнерные one-shot; канон /srv/proxima-ai/repo).
- (event) Mike принял все 5 [ASSUMPTION] 30.08.2026; проверки flag=0 (2 вызова) и async CSV (≤3 вызова) разрешены заранее внутри единиц работы.
- (decision) v3.1 (после валидации нарезки): AD-9 режим postgres до первого SUCCEEDED brief = «только статус»; AD-11 все 4 NOLOGIN-роли создаются в 011, каждая миграция с run_id выдаёт гранты/политики (janitor FOR ALL) в том же файле; proxima_collector - член proxima_source_publisher (чтение 003/005 без миграции); AD-14 номера целевые, вторая мержащаяся ветка перенумеровывает. Живые вызовы WB и record_fixture - только Claude (шаги 0), не OpenHands.
- (event) Spine v3.1 amended in place, AD-ID стабильны.
- (decision) v3.2 (после readiness gate CONCERNS): AD-2 импорт двух артефактов 30.08 в CAS + backfill --source artifact, run_day = mskDay(retrieved_at); AD-3 GUC на сессию при подключении, kinds funnel_csv_download/promote; AD-4 ArtifactSink/WbArtifactSink; AD-5 PK funnel obs без run_id (с canonical_sha256, observed_at), два прогона funnel_csv; AD-12 TS↔PG harness в pg_local_roundtrip (тег db, SET ROLE до LOGIN), sandbox BYPASSRLS + GRANT ALL только в proxima_test; AD-17 restore-check = pg_restore дампа в proxima_test; AD-15 control-plane-admin для owner-секретов; Conventions - таблица имён секретов/env и формат JSON-лога.
- (decision) v3.3 (гейт, проход 2): AD-4 своя обёртка recording-client.ts поверх raw-store/fetchTransport (RecordedHttpClient и SignalRepository не трогаем); AD-12 harness = миграции → provision-runtime-roles.sh на одноразовом PG16 → *.db.test.ts с DSN каждой роли (PROXIMA_TEST_DSN_<ROLE>), без SET ROLE; RLS-матрица по грантам; AD-2 backfill --retrieved-at + tools/cas_import.ts; AD-17 restore-check = age -d | gunzip | psql.
- (event) Readiness gate (bmad-sprint-planning) проход 3: PASS. Спайн v3.3 финальный: устаревшие фразы AD-6/11/13 и Seed выровнены с v3.2-v3.3.
- (event) Story 1.0 факты: flag=0 = фильтр по lastChangeDate (подтверждено 31.08, orders 69 строк с date<dateFrom); srid/saleID уникальны; локальные дампы plain gzip (age только S3) - AD-17 уточнён; sha256 двух артефактов 30.08 записаны в API-FACTS.
- (event) Update intent 08.09.2026 (Story 4.0, разрез nmId и предмета): resume по memlog; входы - epics.md Story 4.0 (Given дословно: грейн (tenant_id, calendar_day, nm_id), категория subjectName через справочник, AD решает reuse 007 vs новая таблица), D21/D25(6а)/D29, миграции 007-017, ветка pa41-full-w2-phase3 (promote-order-counts.ts на attempt_id, 010 = column-level GRANT), фикстуры orders/sales (payload: subject, category, brand, supplierArticle, nmId - не subjectName). AD-18 уже занят («Заморозки»), новое решение = AD-19.
- (constraint) Унаследовано без переоткрытия: D21 (норма - медиана 14 полных дней, метрика - заказы без отмен), 6а (порог 30 % - Story 4.4, не здесь), AD-2 (сумма по nmId никогда не подменяет кабинетный ряд), AD-3 (версии по run_id, _current по SUCCEEDED, CASCADE от collector_runs, транзакция (3)), AD-9 (webapp - ровно два SELECT), AD-11 (шаблон грантов/политик), AD-14 additive-only: NOT NULL/CHECK 007 и CHECK kind 011 не ослабляются (verify_migrations.py: только ADD COLUMN/ADD CONSTRAINT/ENABLE RLS).
- (decision) AD-19 (предложение к D32): fact_nm_daily(tenant, calendar_day, nm_id, run_id CASCADE, шесть колонок как у fact_cabinet_daily, evidence[]) PK (tenant, day, nm, run) + fact_nm_daily_current по AD-3; пишет тот же collect/backfill в транзакции (3) сразу после aggregateCabinetDaily из тех же строк _latest, интервал [floor, run_day-1], строка на каждый день × каждый nmId версии справочника (нули включительно, evidence нулевой строки = артефакты прогона); справочник dim_nm_subject(tenant, nm_id, run_id CASCADE, subject_name, category_name, brand, supplier_article, last_change_at, evidence) - версия на прогон для всех nmId из orders_latest ∪ sales_latest по наблюдению с max last_change_at, dim_nm_subject_current по AD-3; категория аномалии = subject (предмет WB, = product.subjectName в v3) как атрибут SKU на момент последнего SUCCEEDED прогона, не дня; fact_order_counts 007 - legacy без писателя; миграция 018_nm_daily; check per_nm_sums_vs_cabinet тем же run_id в той же транзакции, JSON-лог, не блокирует, порог UNKNOWN (OQ-7); гейт order-counts = node:test на фикстурах + db-тест по _current; роли: collector SELECT/INSERT + FOR ALL, norm SELECT + FOR SELECT, janitor FOR ALL, webapp - нет; 4.1 читает fact_nm_daily_current + dim_nm_subject_current + fact_funnel_daily_current.open_card, соединяет адаптер, детектор - шаг brief, норма SKU в detection_data (носитель - Deferred). Prevents: второй писатель в 007 с ложным source_family; два определения заказов; предмет колонкой факта; детектор на payload; ноль ≠ отсутствие; подмена кабинетного ряда; двойная выручка; строки вне CASCADE.
- (decision) Отклонено в AD-19: (1) reuse fact_order_counts 007 - attempt_id NOT NULL RESTRICT и CHECK source_family неизменяемы под additive-only, писатель лгал бы в fact_attempt_runs, janitor резал бы пять RESTRICT-таблиц, order_count там = CSV ordersCount (второе определение заказов); (2) принять писатель pa41 - lineage на attempt_id, миграция 010 с column-level GRANT (запрет AD-11); (3) reuse dim_product 004 - ручной паспорт с NOT NULL cogs/lead_time и effective_from по решению человека; (4) категория колонкой факта - переименование предмета переписывает историю; (5) справочник как view над stg_wb_orders_latest - norm/webapp получили бы SELECT на сырые payload, JSONB-скан на каждое чтение; (6) справочник upsert-in-place - мутация строки против парадигмы и CASCADE-семантики; (7) строки только по наблюдённым парам - отсутствие неотличимо от нуля, детектор считал бы sample_days по-разному; (8) грант webapp на факты - против AD-9, аномалии доезжают через brief_daily.payload.signals[]; (9) детектор отдельным прогоном - kind CHECK 011 заморожен.
- (assumption) A1 AD-19: subject/category/brand/supplierArticle заполнены и постоянны в пределах nmId (фикстура sample.json: 301/295 строк, 86 nmId - пустых нет, второго предмета нет) - проверить на полных фикстурах 30.08 (13 325 / 10 611 строк) до кодирования справочника.
- (assumption) A2 AD-19: finishedPrice/forPay не более двух знаков (на обезличенной фикстуре 0-2 знака, но деньги там умножены на коэффициент, D26) - иначе сумма округлённых по nmId расходится с кабинетом на копейки; допуск задаёт Mike вместе с OQ-7.
- (assumption) A3 AD-19: объём ~200 nmId × 183 дня бэкфилла ≈ 37 тыс. строк, ежедневно ≈ 800; индексов (run_id), (tenant, day), (tenant, nm, day) достаточно - проверить count(*) и время collect в harness после бэкфилла.
- (question) Q1 (Mike/оркестратор): epics.md 4.0/4.1 называют fact_order_counts_current; AD-19 вводит fact_nm_daily_current (конвенция fact_<grain>_daily, legacy 007 носит старое имя) - править истории при записи D32. Q2: Given 4.0 просит грант webapp - AD-19 не даёт (AD-9). Q3: kind CHECK 011 заморожен additive-only - Story 5.0 kind=decision и Epic 6 требуют исключения в AD-14 или отдельной таблицы прогонов. Q4: допуск check по деньгам, если A2 не подтвердится.
- (event) Reviewer gate 3 (update v3.5, без субагентов, враждебный проход автора): lint 0; 10 находок - 6 autofix (evidence нулевой строки, гейт без PG16, атрибуты в detection_data, COALESCE в check, индекс run_id справочника, фраза про AD-2), 1 в Deferred (заморозка kind), 3 - вопросы Mike (имя таблицы в epics.md, грант webapp, допуск денег). Отчёт reviews/review-adversarial-v3-ad19.md.
- (event) Спайн v3.5 записан 08.09.2026: AD-19 после AD-18, строки CAP-1/CAP-7 в trace-таблице, Deferred без SKU-грейна (+ норма SKU, run-keyed check, заморозка kind), паразитный Facts-ряд парадигмы и erDiagram/seed дополнены; 3 новых [ASSUMPTION]. Статус: предложение, ждёт D32 (Mike); до принятия Story 4.0 не стартует.
- (note) Поправка к предыдущей записи: читать «Facts-ряд таблицы парадигмы дополнен», не «паразитный».
- 2026-09-08 (Mike, D32 day run, PR #95): AD-19 уточнён при приёмке Story 4.0 - (1) ключи словаря `dim_nm_subject` (subject/category/brand/supplierArticle) проверяются на дрейф только у выигравшего наблюдения nmId, `nmId` - у каждой строки; (2) MISMATCH quality-check `per_nm_sums_vs_cabinet` пишется на уровне `warn` до порога OQ-7; (3) смена предмета у товара: предмет = последнее наблюдение, `_current` переключается для всей истории SKU (SCD2 отклонён).
- 2026-09-08 (Mike, D32 day run, PR #104): AD-9 уточнён при приёмке Story 4.2 - payload `brief_daily` получает обязательный ключ `threshold {value, source, date}` (сегодня все `null` = порог не применяется; значение задаёт Story 4.4 в `detector/threshold.toml`); контракт `brief.schema.json` v1 расширен на месте без смены `schema_version` (боевых строк нет); порог применяется к любой упавшей метрике (заказы или выручка, D32) с границей `<=`; `signals[]` отсортированы по `rub_assessment.value_rub` по убыванию (CAP-7).
- 2026-09-08 (Mike, D35 addendum, репетиция на VPS): AD-6/AD-11/AD-15 уточнены по владельцу секретов - файл `/etc/proxima-ai/secrets/proxima_webapp_uri` (и `proxima_webapp_password`) - `1001:1001 0600` (uid образа webapp, `services/webapp/Dockerfile: useradd --uid 1001 webapp`), остальные `*_uri` и токены - `1010:1010 0600` как в спайне. Причина - репетиция 08.09: витрина в `WEBAPP_DATA_MODE=postgres` не читала URI-файл `1010:1010` («файл WEBAPP_DATA_DATABASE_URI_FILE не читается»). Решение Mike: uid образа webapp остаётся 1001 (свой uid у публичного процесса), владельца выставляют `infra/bootstrap/provision-runtime-roles.sh` (переустанавливает на каждом прогоне) и `tools/rehearsal_run.sh init`; compose-overlay без изменений (файл биндится как есть). Спайн на месте не правился - запись здесь; гейт `tools/tests/test_provision_runtime_roles.py` связывает uid образов с правилом chown.
- (decision) AD-9 уточнение (D36 C3): полоса шелла - отдельный читатель `proxima_webapp_readonly`, ≤ 2 SELECT, только `data_status_current` и `fact_cabinet_daily_current`; сводка `/brief` по-прежнему 2 SELECT.
- (event) Update intent 09.09.2026 (Story 5.0, AD записи из webapp): resume по memlog. Входы дословно - epics.md Story 5.0 (Given: роль proxima_webapp_writer LOGIN только INSERT, RLS WITH CHECK, атомарность «запись до закрытия экрана», orphaned при откате, синтетический прогон kind = decision) и Story 5.3; PRD FR-16 + G-9 + OQ-8; contracts/decision-record.schema.json v1 и сгенерированный services/webapp/src/lib/contracts/decision-record.ts; миграции 011/015/018; tools/delete_run.py и tools/verify_migrations.py; infra/bootstrap/provision-runtime-roles.sh; services/webapp/src/lib/data/postgres-provider.ts; detector/signals.py (форма signal_id); D32/D35/D36; docs/state/DATA-DICTIONARY.md (строка decision_records в «Планируемых»). AD-19 занят - новое решение = AD-20.
- (constraint) Унаследовано без переоткрытия: AD-3 (версии по run_id, _current по SUCCEEDED, CASCADE от collector_runs, транзитивный delete_run по collector_run_inputs), AD-9 (webapp - читатель; бюджет SELECT считается на поверхность, уточнение D36-C3), AD-10 (decision-record.schema.json v1 менять нельзя; деньги строкой), AD-11 (ledger создаёт NOLOGIN-роли, гранты и политики; LOGIN-пользователи - bootstrap), AD-12 (RLS-матрица в make verify), AD-13 (tenant_id в каждой таблице, set_config(…, false) первым statement, view security_invoker), AD-14 (additive-only; CHECK kind из 011 не ослабляется), AD-15 + D35 (владелец секретов webapp 1001:1001), AD-18 (auth-зона webapp заморожена до октября).
- (decision) AD-20 (предложение к D-записи Mike): decision_records(record_id uuid PK, tenant_id, signal_id, brief_day, source_run_id uuid БЕЗ FK, run_id uuid NULL → collector_runs ON DELETE CASCADE, actor, recorded_at, git_sha, image_id, payload jsonb по decision-record v1), зерно - строка на запись, логический ключ (tenant_id, signal_id), append-only, UPDATE не выдаётся никому; decision_records_current = DISTINCT ON (tenant_id, signal_id) ORDER BY recorded_at DESC, record_id DESC (правило AD-3, роль finished_at играет recorded_at) + LEFT JOIN collector_runs по source_run_id даёт вычисляемый orphaned. Синтетического прогона kind = decision НЕТ: CHECK из 011 заморожен additive-only, а у записи из одной строки нет фазы RUNNING - разделитель run_id IS NULL ⇔ написал человек (строка сама себе прогон, git_sha/image_id - сборка образа webapp), run_id IS NOT NULL ⇔ написал прогон brief (исход сверки 5.3, обычная версия AD-3, исчезает по CASCADE). Роли: NOLOGIN proxima_ui_writer только GRANT INSERT + политика шаблона FOR ALL … USING/WITH CHECK (FOR INSERT в PG принимает только WITH CHECK, а verify_migrations.py требует USING - «только INSERT» задаёт грант, не форма политики; current_setting обязателен двухаргументный); LOGIN proxima_webapp_writer в bootstrap, URI /etc/proxima-ai/secrets/proxima_webapp_writer_uri 1001:1001 0600, env WEBAPP_WRITE_DATABASE_URI_FILE; webapp_readonly - SELECT на таблицу и view; job_norm - SELECT, INSERT (исходы); janitor - FOR ALL (DELETE доберёт provision по колонке run_id). Два пула в витрине: читатель postgres-provider.ts не меняется, писатель - postgres-writer.ts со своим Pool только в route handler src/app/api/decisions/route.ts; ответ 200 после COMMIT, record_id генерирует handler (INSERT … RETURNING требует SELECT-гранта, которого нет). Миграция 019_decision_records.sql. Гейты: строки RLS-матрицы AD-12, db-тест «orphaned after delete_run», tools/verify_webapp_write_boundary.py, расширение test_provision_runtime_roles.py.
- (decision) Отклонено в AD-20: (1) синтетический прогон collector_runs kind = decision дословно по истории - CHECK 011 неизменяем под additive-only (DROP CONSTRAINT запрещён, ADD CONSTRAINT только сужает), исключение для CHECK-списков в AD-14 сняло бы весь смысл гейта; (2) переиспользовать существующий kind (brief) для записи человека - ложь в реестре, и delete_run прогона brief унёс бы решения по CASCADE, ровно то, что история запрещает; (3) отдельный реестр operator_runs/ui_runs сейчас - правило трёх: сегодня один вызывающий, а цена - вторая CASCADE-сеть, вторая RLS-матрица и второй delete_run; вернуться, когда появится второй писатель (CM-19/CM-1) или прогон Epic 6; (4) run_id решения → прогон brief: CASCADE стирает решение, RESTRICT блокирует откат; (5) orphaned колонкой, которую проставляет delete_run или джоб - мутация append-only строки, писателю пришлось бы дать UPDATE, а delete_run.py - знать таблицу; (6) source_run_id с FK ON DELETE SET NULL - мутирует строку и теряет провенанс (какую сводку видел человек); (7) исход сверки правкой строки решения - тот же UPDATE, история решений исчезает, SM-9 «доля решений, изменённых после сверки» становится неисчислимой; (8) один пул на чтение и запись - ломает бюджет AD-9 и least privilege (баг в читателе смог бы писать); (9) грант INSERT существующей роли proxima_webapp_readonly - имя роли перестаёт описывать права; (10) политика FOR INSERT … WITH CHECK без USING - синтаксис PG верный, но verify_migrations.py её не пропускает; (11) исходы сверки в brief_daily.payload вместо своей таблицы - счётчики SM-4/SM-9 пришлось бы считать разбором JSON по истории сводок.
- (assumption) A1 AD-20: actor до октябрьской auth берётся из env WEBAPP_ACTOR (WEBAPP_REQUIRE_AUTH=false, AD-18, сессии нет), после включения auth - из сессии. A2 AD-20: write-эндпоинт живёт только внутри конверта AD-15 (loopback :3000 + ssh-туннель); переход на Caddy/домен без auth его открывать не должен - блокирующее условие октябрьского релиза. A3 AD-20: PROXIMA_GIT_SHA/PROXIMA_IMAGE_ID добавляются в environment: сервиса webapp так же, как C1 добавил их коллектору и control-plane; без них git_sha/image_id строки решения остаются NULL (колонки nullable, как в collector_runs).
- (question) Q1 (Mike, главный): история 5.0 просит синтетический прогон collector_runs kind = decision; CHECK из 011 под AD-14 неизменяем, поэтому AD-20 предлагает разделитель run_id IS NULL вместо нового вида прогона. Принять AD-20 в этой форме - или оговорить в AD-14 исключение для CHECK-списков и всё-таки завести kind = decision? Q2: AD-20 сужает первый consequence FR-16 («запись обратима по run_id») - обратимы исходы сверки, решения человека нет; правка PRD/epics при записи D. Q3: прогон теневого пересчёта Epic 6 своего вида в реестре по-прежнему не имеет - AD-20 закрывает только случай решения. Q4: Epic 5 заморожен D35 до трёх утр SUCCEEDED; AD-20 - документ, кода не открывает; подтвердить, что заморозка не нарушена. Q5: имя LOGIN-роли proxima_webapp_writer занято историей, поэтому NOLOGIN-группа названа proxima_ui_writer (в PostgreSQL общее пространство имён) - принять пару имён или переименовать.
- (event) Reviewer gate 4 (update v3.6, AD-20): lint_spine 0 до и после; две линзы finalize_reviewers как параллельные субагенты против ARCHITECTURE-SPINE.md - adversarial (две единицы: Story 5.3 и CM-19) и verified-current + rubric walker. Первая редакция получила 4 critical + 5 high + 8 medium/low и 13 находок второй линзы; отчёты - reviews/review-adversarial-v3.6-ad20.md и reviews/review-rubric-v3.6-ad20.md (линзы read-only, файлы записал автор). Автор применил autofix ко всем, кроме правок в чужих AD.
- (decision) AD-20 переписан по гейту: (1) decision_records_current - одна строка на сигнал, решение берётся WHERE run_id IS NULL, исход подклеивается LEFT JOIN только если новее решения, status = open при его отсутствии; SM-4/SM-9 считаются по истории таблицы (закрывает critical 1 и 2 - исход прятал решение, «открыто» было немонотонным); (2) форма обоих payload записана точно - решение с actual: null, delta: null и без outcome, horizon_days внутри expected (корневого поля в v1 нет), snapshot_id из правила убран, исход = копия payload решения с тремя заполненными полями и человеческим actor (закрывает critical 3 и 4); (3) валидация без новых зависимостей: структурная по схеме в webapp по образцу brief-fixtures.test.ts (Ajv в webapp нет, зависимости запрещены, contracts/*.json в образ не копируются), полная jsonschema - в control-plane (закрывает critical Н-2); (4) политик четыре, а не две - гранта без политики не бывает; (5) названы все ручные списки provision-runtime-roles.sh, включая жёсткий IN (…) EXISTING_GROUPS, GRANT CONNECT в TEST_SQL и закреплённую тестом строку-отчёт; (6) гейт приземлён: таргет decision-records, tools/verify_webapp_write_boundary.py без PG, services/collector/tests/decision-records.db.test.ts, DSN PROXIMA_TEST_DSN_WEBAPP_WRITER из pg_local_roundtrip.sh, в CI PG16 есть; (7) писатель переехал в src/app/api/decisions/writer.ts - барель src/lib/data/index.ts реэкспортировал бы его в серверные компоненты мимо текстового гейта; (8) таблица сузилась до решений по сигналам, ручной ввод CM-19/CM-1 - своя таблица в той же группе и пуле; (9) целевой номер миграции по AD-14, FK REFERENCES tenants(tenant_id) ON DELETE RESTRICT, CHECK на signal_id, clock_timestamp() вместо CURRENT_TIMESTAMP, ON CONFLICT (record_id) DO NOTHING как идемпотентность, WEBAPP_ACTOR валидируется при старте, POST 403 вне WEBAPP_DATA_MODE=postgres, staging-overlay и /run/secrets вместо infra/compose.yaml, append-only janitor - конвенция плюс RLS-тест, оговорка про --dry-run.
- (question) Q6 (следствие гейта): принятие AD-20 требует трёх однострочных правок в соседних AD, до D-записи они не внесены - AD-3 (исключение из «все новые таблицы run_id NOT NULL … CASCADE»), AD-9 (бюджет SELECT на поверхность вместо «ровно два», как уже уточнил D36-C3 для полосы метрик), AD-16 (ребро webapp → Postgres на запись). Q7: скобка AD-19 «в CI pg-roundtrip = SKIP» устарела - .github/workflows/verify.yml ставит postgresql-16; правится вместе со следующей ревизией AD-19. Q8: словарь metric.name и unit для решений (RUB против rub против ₽) - вместе с OQ-3.
- (event) Спайн v3.6 записан 09.09.2026: AD-20 после AD-19, строка CAP-8 в trace-таблице, две строки erDiagram, Presentation-ряд парадигмы, Conventions (роли proxima_ui_*, секрет proxima_webapp_writer_uri, append-only decision_records), Structural Seed, четыре пункта Deferred. Статус: предложение, ждёт D-записи Mike; до принятия Story 5.0 закрыта, Story 5.3 не стартует (Epic 5 заморожен D35).
