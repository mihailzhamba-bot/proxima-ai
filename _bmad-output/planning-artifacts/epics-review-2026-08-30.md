# Ревью нарезки epics.md - 30.08.2026

Объект: `_bmad-output/planning-artifacts/epics.md` (5 эпиков, 22 истории, 16 сентябрьских). Эталоны: SPEC.md (CAP-1..8, constraints), ARCHITECTURE-SPINE.md (AD-1..18), DECISIONS.md (D7, D14, D19, D21, D22), правило единицы работы из промта. Режим: враждебный, read-only. Проверено по коду репо: `tools/verify_migrations.py`, `tools/pg_local_roundtrip.sh`, `.github/workflows/verify.yml`, `.openhands/`, `db/migrations/003,005,009`, `infra/`, ветка `origin/ai/pa-50`, `docs/state/{API-FACTS,INVENTORY,MIGRATION-GAPS,WEB-STATE}.md`.

## TL;DR

**Вердикт: не готово к Воротам 3. 6 критических правок, 9 существенных, 12 средних; 5 историй делить (1.2, 1.4, 1.6, 2.1, 3.2) - сентябрь становится 21 единицей вместо 16.**

Критические (ломают релиз или гейт, не «стиль»):

| # | Где | Что | Правка |
|---|---|---|---|
| C1 | 1.4, 1.6, 1.8, 2.2 ← 2.3 | Роли `proxima_job_collector` / `proxima_job_norm` / `proxima_webapp_readonly` создаются в 016 (Story 2.3, Epic 2), а нужны уже в Epic 1: 1.4 делает LOGIN-пользователей «членами ролей», 1.6 читает view под `proxima_webapp_readonly`, 1.8 гонит `collect` под `proxima_collector`, 2.2 - `norm` под `proxima_norm`. Скрытая зависимость E1 → E2 и 2.2 → 2.3. Без правки релиз M-01 запускает job под owner-URI (нарушение AD-11 / NFR4) или падает на `GRANT proxima_job_collector TO proxima_collector` | Три NOLOGIN-роли создать в 011 (1.2) вместе с janitor; гранты + политики на таблицы - в той миграции, которая создаёт таблицу (образец 009); 016 оставляет только `brief_daily`. Поправка AD-11/AD-14 в memlog спайна |
| C2 | 2.2, 2.3, 3.1 | `tools/verify_migrations.py:263-266` не допускает пропусков в нумерации (`migration order gap`). E2 (015/016) физически не мержится раньше E3 (014). Утверждение «E2 и E3 независимы» ложно; AC 3.1 «ledger 14/14 или 16/16» и 2.2 «15/15» противоречат гейту | Правило в 2.2/2.3/3.1: «номер присваивается при мерже; ветка, мержащаяся второй, перенумеровывает файл с пересчётом checksum» (тот же приём, что для pa41 в AD-14); литералы «N/N» в AC заменить на `pg-roundtrip: ledger complete`. Либо жёстко: 3.1 раньше 2.2 |
| C3 | 1.3, 2.2, 2.3, 3.1 → FR10 | Политики janitor `FOR ALL` создаются только для таблиц 011/012 (1.2). Для `fact_cabinet_daily` (013), `stg_wb_funnel_obs`/`fact_funnel_daily` (014), `norm_daily` (015), `brief_daily` (016) - ни в одном AC. Политику нельзя навесить из 011 на таблицу, которой ещё нет. `delete_run.py` под janitor увидит 0 строк в этих таблицах и «удалит» прогон без версий - FR10 закрыт на 2 таблицы из 6 | В каждую миграцию с `run_id` - грант + политика janitor; тест `delete_run: closure` (1.4) сверяет `pg_policies` со списком таблиц с колонкой `run_id` и падает при пропуске |
| C4 | 1.2 / 1.3 → FR3, CAP-2 | `jobs/backfill.ts` (пагинация 80 000 строк по `lastChangeDate`, 1/мин, `--from`, resume) не принадлежит ни одной истории. 1.2 делает только `collect --date-from`; 1.3 использует `backfill --from 2026-03-01` как данность в Given | Разделить 1.2: 1.2a `collect` + 011/012; 1.2b `backfill` режим + пагинация + тест «resume после 80k на фикстуре из двух страниц» |
| C5 | 1.6 ↔ 1.8 ↔ 2.5 ↔ AD-9 | 1.6 задаёт postgres-режим «статус из БД, сводка из фикстур»; 1.8 требует строку статуса на `/brief` (значит режим включён в M-01); 2.5 включает `WEBAPP_DATA_MODE=postgres` только в M-03; AD-9: «включается после первого SUCCEEDED brief». Три места противоречат - одно действие Mike в 1.8 («видит строку статуса») не выполнимо по построению | 1.8 включает `WEBAPP_DATA_MODE=postgres` в семантике 1.6; 2.5 режим не переключает; memlog AD-9: «postgres-режим с M-01 показывает статус; цифры сводки - только по правилу показа» |
| C6 | 1.1, 3.2 | Живые вызовы WB (flag=0 - 2 вызова; async CSV - ≤ 3) внутри сеанса OpenHands невозможны: агент не видит токены (D5: `~/signal-inputs` 0700 `proxima-admin`, `/etc/proxima-ai/secrets` 0600) и не имеет ssh на VPS. `record_fixture.ts` на хосте не запустить (INVENTORY §11: `node_modules`/`uv` нет), в зоне - нет доступа к полным ответам | В 1.1 и 3.2 выделить «Шаг 0 (Claude в чате, вне OpenHands, по D22)»: вызовы → раздел API-FACTS → полные фикстуры на VPS → обезличенные выжимки ≤ 200 КБ на маке (scp в gitignored `fixtures/wb-api/`, `record_fixture.ts` запускается там) → коммит-заготовка в ветку единицы. Сеанс OpenHands начинается с готовых фикстур |

Существенные (H) и средние (M) - в разделах ниже.

## 1. Покрытие

### FR1-FR12

| FR | История | AC закрывают полностью? |
|---|---|---|
| FR1 сбор orders/sales в дневной ряд | 1.2 (наблюдения) + 1.3 (ряд) + 1.7/1.8 (ежедневно) | Да, кроме C1 (роль на релизе) и NFR11 (JSON-лог не в AC) |
| FR2 идемпотентность | 1.2 (`DO NOTHING` при том же `canonical_sha256`, вторая строка при новом `lastChangeDate`, `WB_SCHEMA_DRIFT`) + 1.3 (версия по прогону) | Да |
| FR3 бэкфилл с 01.03 | 1.3 (агрегат, W10/W35) + 1.8 (запуск) | **Нет**: код `backfill` без хозяина (C4); срок - см. H9; тест W10/W35 в git - см. H2 |
| FR4 статус данных | 1.3 (`data_status_current`) + 1.6 (строка) | Да, при C5 |
| FR5 предупреждение stale | 1.6 | **Частично**: 1.3 тестирует только `collected_at > 24 ч`; условие `last_full_day < вчера` не тестируется ни в 1.3, ни в 1.6 |
| FR6 норма | 2.2 | Да (34.5 / 34 595, окно [15.08, 28.08], insufficient при 9/14) |
| FR7 вчера против нормы | 2.3 + 2.4 | Да; арифметика проверена: (27−34.5)/34.5 = −21.7 %, (41 141−34 595)/34 595 = +18.9 % |
| FR8 воронка v3 | 3.1 | Да |
| FR9 воронка CSV | 3.2 | **Частично**: «не более одного созданного отчёта в сутки» есть только в живой проверке (Given), как поведение job и тест - нет; кто создаёт строку `collector_runs kind=funnel_csv` при двух фазах (python под owner / TS под collector) - не определено |
| FR10 delete_run | 1.4 | **Частично** - C3 |
| FR11 аномалии | 4.1-4.3 | Контуры, детализация после M-03 - как и заявлено |
| FR12 план действий | 5.1-5.3 | Контуры; 5.3 честно требует отдельного AD |

### CAP-1..8

| CAP | История | Success-критерий спеки закрыт? |
|---|---|---|
| CAP-1 | 1.2, 1.3, 1.8 | «Три дня подряд без вмешательства» - 1.8 наблюдает одно утро. Нигде нет 3-дневной приёмки (M8) |
| CAP-2 | 1.3, 1.8 | Суммы W10/W35 - только если бэкфилл пройдёт, пока март в окне WB (H9) |
| CAP-3 | 1.3, 1.6, 1.8 | Да, при C5 |
| CAP-4 | 2.2 | Да |
| CAP-5 | 2.3, 2.4, 2.5 | «7 предыдущих утр без вмешательства» - 2.5 наблюдает одно утро (M8) |
| CAP-6 | 3.1, 3.2, 3.3 | «8 недель к 27.10» - наблюдение, не единица; ок |
| CAP-7 | Epic 4 | Контуры |
| CAP-8 | Epic 5 | Контуры |

### NFR1-13

| NFR | Где реализуется | Статус |
|---|---|---|
| NFR1 READ-only, реестр, 429 | 1.1 (реестр 4 эндпоинта, гейт, тест 429/бюджет) | Приземлился |
| NFR2 фикстуры, без сети | 1.1 (`FixtureTransport`, ≤ 200 КБ) | Приземлился; конфликт «обезличено vs реальные суммы» - H2 |
| NFR3 три вида транзакций | 1.2 (TS, дословно AD-3) | TS - да. Python-jobs (2.2, 2.3) модель прогона в AC не описана (RUNNING autocommit / SUCCEEDED в одной транзакции / FAILED отдельно) - пропуск |
| NFR4 multi-tenant, без superuser | 1.2, 1.3, 1.4, 2.3 | Нарушен на релизе 1.8 (C1) |
| NFR5 секреты-файлы | 1.5 (`secrets:`), 1.7 (`mv`), 1.8 | Приземлился; секрет Telegram для `proxima-alert@` не назван (M2) |
| NFR6 сервер только чтение, релиз = тег | 1.7, 1.8, 2.5, 3.3 | Приземлился; откат первого релиза - H3 |
| NFR7 единица = сеанс + PR + одно действие | все | Нарушен размером в 1.2, 1.4, 1.6, 2.1, 3.2 (§2) и живыми вызовами (C6) |
| NFR8 заморозка auth | 1.6, 2.4 (`git diff --stat` guard) | Приземлился |
| NFR9 сроки | 1.1 (до 08.09), 1.8 (бэкфилл) | D19 говорит «первая единица M-01 = расписание + бэкфилл до 08.09»; в нарезке расписание + бэкфилл = 1.7/1.8, восьмая единица. Март тает по дню в сутки - H9 |
| NFR10 WORKS-TODAY, make verify, RLS-тест | 1.8/2.5/3.3, 1.4 | Приземлился (при C1) |
| NFR11 наблюдаемость | 1.7 (`OnFailure`), 1.3/1.6 (`stale`) | JSON-строка на событие с `run_id`/`kind`/`step` - ни в одном AC (M4) |
| NFR12 время | 1.1 (`mskDay`), 1.3 (SQL-форма), 1.7 (`OnCalendar` с TZ) | Python `msk_day.py` (AR8) не создаётся нигде - H6 |
| NFR13 контракты | 2.1, 2.3 | Приземлился |

### AR1-16

| AR | Где | Статус |
|---|---|---|
| AR1 миграции 011-016 | 1.2 (011, 012), 1.3 (013), 3.1 (014), 2.2 (015), 2.3 (016) | C1, C2, C3 |
| AR2 образы, compose, overlay | 1.5 | Да |
| AR3 планировщик | 1.7 | Да; `proxima-funnel-csv@` как no-op - M1; секрет алерта - M2 |
| AR4 роли вне ledger | 1.4 | H8 (Docker на маке нет - скрипт не выполним в roundtrip без `PSQL=` override) |
| AR5 WB-клиент, `record_fixture.ts`, гейт | 1.1 | C6 (где запускать `record_fixture.ts`) |
| AR6 живые проверки | 1.1 (flag=0, srid), 3.2 (CSV) | C6; 1.1 не ссылается на API-FACTS:42, где уже есть свидетельство против гипотезы (M9) |
| AR7 контракты | 2.1 | H7 (размер) |
| AR8 helper дня | TS 1.1, SQL 1.3 | Python - **нет истории** (H6) |
| AR9 delete_run, RLS-тест | 1.4 | C3 |
| AR10 тестовая база, `.env.task`, еженедельная проверка | 1.4 (скрипт) | `.env.task` в зоне и еженедельный таймер - **нет истории** (H5) |
| AR11 первый релиз | 1.7 (runbook), 1.8 | H3 (откат), H4 (скрипт бэкапа вне git), H9 |
| AR12 psycopg, norm/, brief/, tenants | 1.5, 2.2, 2.3, 3.2 | Да. `psycopg` в 1.5 обоснован: `tools/apply_migrations.py:11` импортирует `wb_async_report` → psycopg нужен образу для `apply-migrations` |
| AR13 postgres-provider | 1.6, 2.4 | C5 |
| AR14 ветки | pa-50 → 1.6, pmm29 → 2.1, pmm-20 → 4.1 | pa41 (перенумерация 010, писатель `fact_order_counts`, нужен 4.1 для сверки) - **нет истории**; списание PA-03-02/pa-9/stash - нет (чеклист Ворот 3) |
| AR15 старый бэклог | «после Ворот 3» | Не единица OpenHands - должен быть пунктом чеклиста Ворот 3, а не строкой в Coverage Map |
| AR16 наблюдение сутки | 1.8, 2.5, 3.3 | Одно утро вместо 3/7 по CAP (M8) |

## 2. Размер (ориентир: ≤ ~5 областей, ≤ ~600 строк, один связный PR)

| История | Области / оценка строк | Влезает? | Как делить |
|---|---|---|---|
| 1.1 | `src/wb/` (4 файла), тесты + 4 фикстуры, `tools/verify_wb_client.py` + Makefile, API-FACTS - ~550-650 | Да, при выносе шага 0 (C6) | - |
| 1.2 | 011 + 012 (~200 SQL с политиками), `jobs/collect.ts`, писатель наблюдений/реестра/артефактов, CLI, 3 сценария тестов - ~700-900 без backfill | **Нет** | 1.2a `collect` + миграции + idempotent replay; 1.2b `backfill` + пагинация 80k + resume-тест (C4) |
| 1.3 | 013, `facts/cabinet-daily.ts`, self-heal `dateFrom` в collect, тесты W10/W35, гейт `CURRENT_DATE` - ~500-600 | На грани | Не делить, но убрать backfill (C4) и назвать файл гейта для `CURRENT_DATE` (M5) |
| 1.4 | `delete_run.py` (замыкание, dry-run), `provision-runtime-roles.sh`, `test_db_refresh.sh`, Makefile, RLS-тест, интеграция в roundtrip - 6 областей, ~600-750; cross-язычный harness (bash + TS collect + Python) не определён | **Нет** | 1.4a `delete_run.py` + RLS-тест (pytest в `tools/tests`, БД из roundtrip); 1.4b bootstrap + refresh + Makefile |
| 1.5 | 2 Dockerfile, `.dockerignore`, compose + overlay + `jobs.env`, CI (2 job'а), pyproject + uv.lock, Makefile `ENV_FILE` - 9 файлов, ~350-400, одна тема | Да | - |
| 1.6 | принять `ai/pa-50` (19 файлов, +694/−68: `signal-detail.tsx` 120, `types.ts` 111, 6 компонентов, 2 теста) + реализация провайдера + строка статуса + vitest - > 5 областей, ~900 | **Нет** | 1.6a «принять ai/pa-50 через ревью: шов провайдера, `WEBAPP_DATA_MODE`, fixtures-provider; `/brief` визуально не меняется» (проверка: `make verify` + `/brief` как был); 1.6b «postgres-provider: статус» |
| 1.7 | 5 юнитов, `morning_run.sh`, скрипт алерта, runbook (~200 строк), CI shellcheck/systemd-analyze - ~450-550 | Да | Убрать no-op `funnel-csv` юниты в 3.2 (M1) |
| 1.8 | Не сеанс OpenHands - операция Claude на сервере по D7 | Другой тип единицы | Пометить как «релиз-единица (Claude, сервер)», не считать сеансом OpenHands |
| 2.1 | принять pmm29 (26 файлов, +1039/−242: 3 схемы, 7 примеров, TS-типы, `verify_contracts.py` +68) + 3 новые схемы + примеры + codegen на webapp + удаление `types.ts` + правка импортов webapp + `diagnosis/validator.py` + удаление копии схемы + pytest - 5 областей, > 1000 строк | **Нет** | 2.1a принять pmm29 + 3 схемы + примеры (`make contracts`); 2.1b codegen → webapp + удалить `types.ts` + импорты; перенос diagnosis-валидатора на `contracts/` - в 5.1 (решение не нужно в сентябре) |
| 2.2 | 015, `norm/` (4 файла), `common/msk_day.py`, тесты - ~450-550 | Да | Добавить `msk_day.py` явно (H6) |
| 2.3 | 016, `brief/` (2-3 файла), тесты, RLS-тест - ~400-500 | Да | - |
| 2.4 | `getBrief()`, компонент сводки, `page.tsx`, vitest - ~350-450 | Да | Убрать правку `tools/morning_run.sh` (несвязанная область) в 2.5 или в `jobs.env` (M1) |
| 2.5 | релиз | Другой тип | - |
| 3.1 | 014, `jobs/funnel-v3.ts`, `facts/funnel-daily.ts`, тесты, шаг в morning_run - ~500-600 | На грани | - |
| 3.2 | правки `wb_async_report.py`, `jobs/funnel-csv-promote.ts`, 2 юнита + обёртка двух фаз, миграция грантов (H1), тесты, API-FACTS - 6 областей, ~650-800 | **Нет** | Шаг 0 (Claude, ≤ 3 вызова) отдельно (C6); 3.2a промоушен TS + миграция грантов + тест replay-after-delete; 3.2b `wb_async_report.py` (tenants, `--collector-run-id`, `--period`) + юниты + обёртка двух фаз |
| 3.3 | релиз | Другой тип | - |

Итог: сентябрь = 16 → 21 единица (1.2a/b, 1.4a/b, 1.6a/b, 2.1a/b, 3.2a/b) плюс 3 «шага 0» силами Claude.

## 3. Зависимости

Внутри эпика (N.M → N.(M+k)) - нарушения:
- **2.2 → 2.3**: `proxima_job_norm` (016) нужен loader'у 2.2 и `INSERT collector_runs kind=norm` (C1).
- 1.3 правит `collect.ts` из 1.2 (`dateFrom = min(run_day-3, last_full_day+1)`) - допустимо, направление верное.

Между эпиками:
- **E1 → E2 (обратная, критическая)**: 1.4 (члены ролей), 1.6 (`proxima_webapp_readonly`), 1.8 (`proxima_collector`) требуют 016 из 2.3 (C1).
- **E2 ↔ E3 (взаимная через гейт)**: нумерация 014/015/016 - C2. «E2 и E3 независимы после E1» верно только для кода, ложно для ledger.
- E2 ← E1: 2.1 удаляет `services/webapp/src/lib/data/types.ts`, которого в `main` нет - он появляется только после принятия `ai/pa-50` в 1.6. Зависимость 2.1 → 1.6 есть, в тексте 2.1 не названа.
- E3 ← E1: 3.1 читает `stg_wb_orders_latest` (1.2), шаг в `morning_run.sh` (1.7) - названо, ок.
- **3.2 → миграция, которой нет в плане (H1)**: промоушен читает `stg_wb_nm_report_rows` и `wb_analytics_report_tasks` под `proxima_job_collector`; обе таблицы под RLS (`009_runtime_roles.sql:58-80`) с политиками только для `proxima_source_publisher`; AD-11 не даёт `proxima_job_collector` SELECT на них. Нужен грант + `FOR SELECT`-политика в 014 (3.1) или в новой 017.
- 4.1 → pa41 (писатель `fact_order_counts` для сверки) - истории на мерж/перенумерацию pa41 нет (M10).

**Story 1.2: janitor и `run_inputs` в 011 - допустимо, и это правильный выбор.** Аргументы: (а) политика RLS создаётся только вместе с таблицей, миграции additive-only - отложить janitor-политики на 011-таблицы некуда, кроме новой миграции ради ничего; (б) `collector_run_inputs` - часть модели прогона AD-3, не фича 1.3; пустая таблица стоит ноль, а тест 1.2 уже может проверить её RLS; (в) правило «создавать только когда нужно» - про код и единицы, не про колонки ратифицированной схемы. Реальные нарушения этого правила в нарезке другие: `fact_attempt_runs.collector_run_id` (писатель - pa41, M-04+; `wb_analytics_report_tasks.collector_run_id` нужен 3.2 - оставить), no-op юниты `proxima-funnel-csv@` в 1.7 и флаги `norm`/`brief`/`funnel_v3` в `morning_run.sh` до своих эпиков (M1).

## 4. Соответствие спайну

| Проверка | Результат |
|---|---|
| AD-3 в 1.2 | Порядок трёх транзакций воспроизведён дословно (RUNNING autocommit → артефакты autocommit → наблюдения + SUCCEEDED одна транзакция с `set_config(…, true)` первым statement → FAILED отдельно). Пропуски: `kind`/`status` - гейт не пропускает `CREATE TYPE` (`verify_migrations.py:41-44`), нужен `text + CHECK` - в AC не сказано; `git_sha`/`image_id` из `PROXIMA_GIT_SHA`/`PROXIMA_IMAGE_ID` (1.7) с fallback - не сказано |
| AD-2 в 1.3 | Полностью: версия каждого дня `[floor, run_day-1]`, нули + артефакты прогона для дней без строк, `floor = mskDay(dateFrom)` / `--from + 1`, `= run_day` не версионируется, distinct `run_id` в `run_inputs`, `dateFrom = min(run_day-3, last_full_day+1)` |
| AD-11 в 2.3 | «Гранты и политики на базовые таблицы» упомянуты, но не перечислены; RLS-тест только `brief_current`. Нужно: `data_status_current` под webapp-ролью (читает `collector_runs` + `fact_cabinet_daily`), `norm_daily_current` под norm-ролью, INSERT/UPDATE `collector_runs` + ALL `collector_run_inputs` для `proxima_job_norm`. И C1: роли нужны раньше |
| AD-9 в 2.4 | Правило показа цифр дословно: `status='ok' AND stale IS FALSE AND brief_day = last_full_day`; при `insufficient` - «норма копится 9/14» плюс строка статуса из 1.6 = предупреждение с `last_full_day`/`collected_at` - соответствует |
| Owner-URI в 3.2 | Соответствует AD-5/AD-11: фаза 1 под owner-URI, комментарий с датой пересмотра M-04, фаза 2 под `proxima_collector` |
| AD-6 в 1.7 | `kind` с `_` (`funnel_v3`), юниты с `-` (`proxima-funnel-csv@`), `OnCalendar` с `Europe/Moscow`, `Persistent`, `User=root`, `ProtectHome`, `OnFailure=proxima-alert@%n.service`, шаги = `docker compose --profile jobs run --rm`, `PROXIMA_GIT_SHA`/`IMAGE_ID` - соответствует |
| AD-4 в 1.1 | Реестр из 4 эндпоинтов с бюджетами, 429 по `X-Ratelimit-Retry` ≤ 3, запрет `setInterval`/`node-cron`, `FixtureTransport` - соответствует. Граничные значения `mskDay` верны (23:30Z → 30.08; 20:59:59Z → 29.08) |
| AD-8 в 2.2 | Окно `[eval-14, eval-1]`, `UNIQUE`, `insufficient` при `< 14`, `_current` = последний SUCCEEDED - соответствует |
| AD-7 в 1.3 | `COALESCE(…, true)`, SQL-форма `(now() AT TIME ZONE 'Europe/Moscow')::date` - соответствует; тест на `last_full_day < msk_today-1` отсутствует (M5) |
| AD-5 в 3.1 | PK, `_current` csv над v3, пакеты ≤ 20, окно 7 - соответствует. «Активные nmId = distinct за 30 дней из `stg_wb_orders_latest`» - решение принято внутри истории, в спайне определения нет: зафиксировать в memlog |
| AD-15/AD-13 в 1.7/1.8 | Канон `/srv/proxima-ai/repo`, тег, overlay, `mv` токенов с chown - соответствует |
| AD-17 | `OnFailure` есть; еженедельная проверка восстановления и `PROXIMA_RAW_DIR` в бэкапе - без истории (H4, H5) |

Все истории ссылаются на AD; противоречий буквы AD в AC не найдено, кроме C5 (AD-9 vs 1.8) - и оно в самом спайне тоже (AD-9 «после первого SUCCEEDED brief» несовместимо с CAP-3 в M-01).

## 5. Проверяемость («одно действие Mike»)

| История | Действие в AC | Оценка | Лучше |
|---|---|---|---|
| 1.1 | `make verify` → `verify_wb_client: PASS` + «строка из API-FACTS» | `make verify` не печатает строк из API-FACTS - второе действие спрятано | `verify_wb_client.py` проверяет наличие раздела «flag=0 семантика» в API-FACTS и печатает его заголовок; тогда одно действие честное |
| 1.2, 1.3, 2.1, 2.2, 2.3, 3.1 | `make verify` с именованным тестом | Одно действие, ок | Имена тестов закрепить в AC как строки вывода (уже сделано) |
| 1.4 | `make verify` → `delete_run: closure`, `rls: roles` | Ок, если harness определён (H8) | Назвать файл теста (`tools/tests/test_delete_run_pg.py`) |
| 1.5 | Открыть зелёный CI PR | Ок | - |
| 1.6, 2.4 | `npm run dev` с `WEBAPP_DATA_MODE=postgres` и локальной БД «или `ssh -N proxima-db`» | Слабо: серверная БД до релиза пуста; локальная требует roundtrip-кластер + `collect` на фикстурах (+ `norm`, `brief` для 2.4) = 3-4 действия | `make webapp-demo-postgres`: поднять эфемерный PG16 (как `pg_local_roundtrip.sh`), прогнать `collect`/`norm`/`brief` на фикстурах, запустить dev - одно действие; либо приёмка = `make verify` + скриншот из vitest-снапшота |
| 1.7 | «Mike читает runbook» | Слабо | `bash tools/morning_run.sh amirova-test --dry-run` печатает последовательность команд (в AC есть) - сделать это действием Mike; runbook читается на приёмке 1.8 |
| 1.8 | Открыть `/brief` через туннель | Ок при C5 | Добавить `make runs-status` (последний SUCCEEDED по `kind` из `collector_runs` через `DATABASE_URI`) - пригодится 2.5/3.3 |
| 2.5 | Открыть `/brief`, сверить с кабинетом WB | Приемлемо (сверка - содержание проверки) | - |
| 3.2 | `make verify` + «раздел API-FACTS обновлён» | Как 1.1 | Как 1.1 |
| 3.3 | «Читает релиз-заметку с выводом SELECT» | Слабо: чтение чужого вывода - не проверка | `make runs-status` через `ssh -N proxima-db` - одна команда показывает `funnel_v3`/`funnel_csv` SUCCEEDED с датами; либо `funnel_status_current` view и вторая строка статуса на `/brief` (небольшое UI-изменение, но воронка на `/brief` - non-goal сентября, поэтому лучше make-цель) |

## 6. Пропуски (что из спайна/спеки не имеет истории)

| Элемент | Где живёт | Статус |
|---|---|---|
| `tools/record_fixture.ts` | 1.1 (Given) | Есть, но кто/где запускает - не определено (C6) |
| `tools/verify_wb_client.py` в `make verify` | 1.1 | Есть |
| `.dockerignore` | 1.5 | Есть |
| `PROXIMA_RAW_DIR` в бэкап | 1.7 (текст runbook) + 1.8 (исполнение) | **Пропуск по существу**: `proxima-pg-backup.sh` лежит только на сервере (`/usr/local/bin`, root, 1599 Б - INVENTORY §11/№126), в git его нет. Правка = редактирование невидимого кода на боевом. Нужно: импорт в `infra/backup/` + установка в runbook (в 1.7) |
| Вывод `proxima_dev` (хост и зона) | 1.7 runbook, 1.8 (неявно) | Есть; в Then 1.8 явно не перечислено |
| `mv` токенов | 1.7 runbook, 1.8 | Есть |
| `CHANGELOG.md` | 1.7 (упоминание), 1.8 (неявно) | Файла нет в репо; кто создаёт - не сказано. Создать в 1.1 при обкатке релиза (§7) |
| Наблюдение сутки | 1.8, 2.5, 3.3 | Есть; одно утро вместо 3/7 (M8) |
| Старый бэклог AR15 | «после Ворот 3» | Нет единицы; не задача OpenHands - в чеклист Ворот 3 |
| RLS-тест в `make verify` | 1.4 | Есть (C1, H8) |
| `psycopg` в dependencies | 1.5 | Есть, обосновано (`apply_migrations.py` → `wb_async_report` → psycopg) |
| Удаление автосоздания `tenants` (`wb_async_report.py:296`) | 3.2 | Есть |
| `infra/webapp.staging.compose.yaml` | 1.5 | Есть |
| `proxima_control_plane/common/msk_day.py` (AR8) | - | **Пропуск** (H6): нужен 2.2/2.3 для `evaluation_day`/`brief_day` |
| `.env.task` с URI `proxima_sandbox` в зоне OpenHands (AR10, AD-12) | - | **Пропуск** (H5): серверная операция, ни в 1.4, ни в 1.8. Плюс `.openhands/hooks/verify-gate.sh:4-7` отказывает без `DATABASE_URI` - сеанс 1.1 закончится deny (известный pitfall в AGENTS.md) |
| Еженедельная проверка восстановления (AD-17: `make test-db-refresh` в Пн до `funnel_csv`) | - | **Пропуск** (H5): таймера нет ни в 1.7, ни в 3.2 |
| Секрет Telegram для `proxima-alert@` | - | Пропуск (M2): на сервере уже есть `/etc/proxima-ai/secrets/telegram_bot_token` и `telegram_chat_id` (`infra/monitoring/monitor.env.example`) - указать их и скрипт отправки |
| JSON-строка на событие (NFR11, AD-17) | - | Пропуск (M4): ни в 1.2, ни в 2.2/2.3 |
| Перенумерация/мерж pa41 (AR14, D14) | - | Пропуск (M10): нужен 4.1; хотя бы контурная история в Epic 4 |
| Списание веток PA-03-02/pa-9/stash (AR14) | - | Не единица; чеклист Ворот 3 |
| `contracts/cabinet-daily.schema.json` | 2.1 | Есть; потребитель схемы в сентябре не назван (fact_cabinet_daily - таблица, не JSON через границу) - оставить, но указать, кто читает |
| Грант `proxima_job_collector` на `stg_wb_nm_report_rows`/`wb_analytics_report_tasks` | - | **Пропуск** (H1) |
| `infra/jobs.env` | 1.5 | Есть |
| Модель прогона для Python-jobs (AD-3, «set_config на соединении») | - | Пропуск в 2.2/2.3 (NFR3) |

## 7. Первая единица (Story 1.1) против 10 шагов конвейера

Что 1.1 обкатывает сейчас: ветка → сеанс OpenHands → автотесты (`make verify`/CI) → приёмка Mike (`make verify`). Четыре шага из десяти. Не обкатывает: враждебное ревью (кто, где фиксируется), регрессию WORKS-TODAY, тест-запуск на `proxima_test`, релиз/деплой с планом отката, наблюдение, шаг 10 (статус задачи по D17). Кроме того, сеанс OpenHands для 1.1 сейчас не стартует чисто: `verify-gate.sh` требует `DATABASE_URI`; `.openhands/setup.sh` не ставит `PUPPETEER_SKIP_DOWNLOAD=1` (Chromium в сандбоксе); живые вызовы и `record_fixture.ts` агенту недоступны (C6).

Что добавить, чтобы 1.1 прошла все 10 шагов на безопасной нагрузке:

0. **Шаг 0 (Claude, до сеанса, по D22)**: 2 вызова `flag=0` с VPS → раздел «flag=0 семантика» в API-FACTS с точной формулировкой теста (доля строк с `date < dateFrom` в ответе; API-FACTS:42 уже даёт свидетельство против гипотезы «только `lastChangeDate`» на границе окна - сослаться) → полные ответы в `~/signal-inputs/fixtures/wb-api/` → `count(*) = count(distinct srid)` → выжимки ≤ 200 КБ на маке в ветку-заготовку.
1. **Ветка** от `main`, имя по конвенции (`feat/u01-wb-client`), заготовка со «Шагом 0» уже в ней.
2. **Сеанс OpenHands**: в скоуп 1.1 добавить чинку конвейера - `verify-gate.sh` не требует `DATABASE_URI` (make verify его не читает), `PUPPETEER_SKIP_DOWNLOAD=1` в `setup.sh`, `runs-on: ubuntu-24.04` в CI (там `/usr/lib/postgresql/16/bin` - `pg-roundtrip` реальный, а не SKIP; `ubuntu-latest` может уехать).
3. **Ревью**: `reviewer` (read-only, другая модель) по `.claude/agents/reviewer.md`; отчёт - комментарий в PR; гейт 0 blocker. Сейчас в AC 1.1 ревью не упомянуто.
4. **Автотесты**: CI зелёный = гейт для сандбокс-единиц (AD-12).
5. **Регрессия**: подмножество WORKS-TODAY, относящееся к репо/маку (`make verify`, `pg-roundtrip: PASS`) - прогнать, записать в PR.
6. **Тест-запуск**: для 1.1 нечего запускать в `proxima_test` - записать «н/п» явно, чтобы шаг не пропал из шаблона.
7. **Приёмка Mike**: `make verify` на маке.
8. **Релиз**: тег `v2026.09.NN-1`, `CHANGELOG.md` (создать), релиз-заметка `docs/operations/releases/…` с планом отката; «деплой» = единственная безопасная серверная операция из AR11: восстановить `origin` у `/srv/proxima-ai/repo` и `git checkout <tag>` (сервисы не трогаются, откат = `git checkout <prev sha>`). Это обкатывает «явное деплой + план отката + релиз-заметка» с нулевым риском и снимает первый пункт AR11 с 1.8.
9. **Наблюдение**: «сервер не изменился кроме чекаута» (WT-13-подобная проверка через сутки).
10. **Статус задачи**: после Ворот 3 - задача единицы в Jira → Done со ссылкой на PR/тег (D17).

Так 1.1 остаётся маленькой (код ~550 строк) и при этом проходит конвейер целиком, а не «код + verify».

## Существенные (H)

- **H1** 3.2: гранты и `FOR SELECT`-политики для `proxima_job_collector` на `stg_wb_nm_report_rows`, `wb_analytics_report_tasks` (обе под RLS с 009) - миграция нужна, в плане AD-14 её нет. Плюс: кто вставляет `collector_runs kind=funnel_csv` при двух фазах.
- **H2** 1.3 (и 2.2): тест «W10 = 649 / 700 860 ₽» на «обезличенной выжимке ≤ 200 КБ» - противоречие. Точные суммы требуют реальных `finishedPrice` по строкам (≈ 1 600 строк × 820 Б ≈ 1.3 МБ полного ответа; выжимка до 6 полей ≈ 190 КБ, на грани). D15 отклонил git-lfs именно из-за «реальных продаж кабинета в GitHub». Правка: в git - синтетическая фикстура с синтетическими контрольными суммами; сверка W10/W35 = AC релиза 1.8 (уже есть) + локальный тест на маке против gitignored полной копии (`PROXIMA_FULL_FIXTURES=1`). Решение Mike: допустимы ли реальные дневные суммы кабинета в репо (для 2.2 - 28 значений окна нормы).
- **H3** 1.7/1.8: откат «prev tag» для первого релиза не существует; runbook выводит `~/proxima-webapp-staging` и контейнер в том же релизе - путь отката веб-морды уничтожен до наблюдения суток. Правка: уборка (старый контейнер, `proxima_dev`, старые дампы) - отдельный шаг после суток; откат первого релиза = `systemctl disable --now proxima-morning@…timer` + `delete_run.py` по прогонам релиза + старый контейнер жив.
- **H4** Скрипт бэкапа вне git (см. §6).
- **H5** `.env.task` для зоны, еженедельный таймер восстановления, `verify-gate.sh` (см. §6, §7).
- **H6** `msk_day.py` (см. §6).
- **H7** Размер 1.6 и 2.1 (см. §2).
- **H8** 1.4: `provision-runtime-roles.sh` по AD-11 идёт через `docker compose exec postgres`; на маке Docker нет (AGENTS.md) - в `pg_local_roundtrip` скрипт не выполним без `PSQL=` override. Тест «под sandbox `count(*) > 0`» требует данных из `collect` на фикстурах - harness bash + TS + Python не определён, файл теста не назван.
- **H9** Срок бэкфилла: окно WB на 30.08 начинается 01.03 и сдвигается на день в сутки; бэкфилл запланирован в 1.8 (восьмая единица). При ~1-2 дня на единицу бэкфилл уходит на 15-20.09 - W10 (2-8 марта) к тому моменту выпадет, и AC 1.8 «W10 совпадает с API-FACTS» невыполним по построению. Правка: бэкфилл 01.03-30.08 делать **replay'ем сохранённого полного ответа 30.08** (`~/signal-inputs/fixtures/wb-api/statistics/supplier-{sales,orders}/20260830T…flag-0.json`, 8.8 + 10.9 МБ - это уже доказательство по AD-1), а живой `collect` закрывает хвост с 27.08. Тогда март не зависит от календаря единиц; в 1.2b - режим `--replay <artifact>`.

## Средние (M)

- **M1** 1.7: no-op `proxima-funnel-csv@` и флаги шагов - создание до надобности. Список шагов в `infra/jobs.env` (`MORNING_STEPS=collect`), каждый эпик добавляет шаг одной строкой; `funnel-csv` юниты - в 3.2; 2.4 перестаёт трогать `tools/`.
- **M2** Секрет Telegram и скрипт для `proxima-alert@` (см. §6).
- **M3** 1.5 CI: `docker compose config` для staging-overlay с `secrets: file:` - указать, что файлы секретов в CI подменяются пустышками (иначе `config` может упасть на отсутствующем файле).
- **M4** JSON-логи NFR11 - ни в одном AC.
- **M5** 1.3: `stale` при `last_full_day < вчера` не тестируется; гейт на `CURRENT_DATE` в `db/` - не назван файл (правка `verify_migrations.py`).
- **M6** 3.1: фикстура «`sales-funnel-v3-history` 30.08, 3 nmId, 7 дней» - D16/D20 делали 2 вызова (неделя назад + 12 мес назад); состав nmId не подтверждён - проверить до сеанса. 3.2: «≤ 1 отчёт в сутки» как поведение job и тест.
- **M7** Проверяемость 1.6/2.4/3.3 (см. §5).
- **M8** CAP-1 «3 дня», CAP-5 «7 утр» - добавить в 1.8/2.5 как условие закрытия эпика (не единицы): «эпик закрыт после N утр подряд».
- **M9** 1.1: «при опровержении `dateFrom = run_day-14` записью в memlog» - изменение AD решает Mike (D7); формулировать «предложение в PR, решение Mike». Сослаться на API-FACTS:42.
- **M10** Epic 4: нет истории на мерж/перенумерацию pa41 (AR14).
- **M11** 1.8 «ledger 13/13», 2.5 «16/16», 1.2 «12/12» - литералы ломаются при любом порядке мержа с E3; писать «ledger complete».
- **M12** 1.2: `kind`/`status` как `text + CHECK` (гейт запрещает `CREATE TYPE`), `git_sha`/`image_id` из env с fallback.

## Что в нарезке сделано верно (для калибровки, не похвала)

Порядок 1.1 → 1.2 → 1.3 внутри E1 соблюдает «наблюдения раньше фактов» (AD-1/AD-2); AC 1.2 и 1.3 воспроизводят AD-3 и AD-2 дословно и проверяемо; арифметика нормы/отклонения в 2.2/2.3 сходится; заморозка auth закреплена гейтом `git diff --stat`; owner-URI-исключение в 3.2 - с датой пересмотра; 5.3 честно остановлена до отдельного AD. Проблемы нарезки - в основном унаследованные дефекты спайна (роли в 016, AD-9 vs CAP-3, нумерация 014-016 при параллельных эпиках) и в логистике живых вызовов, которые спайн не рассматривал.
