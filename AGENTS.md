# PROXIMA AI — Claude Code adapter

@AGENTS.md

## Claude-specific

- MCP (user-level, not project): context7, postgres restricted — see ~/.codex/config.toml; project scope keeps jira-atlassian + node_repl only (agent-toolset contract).
- Russian-first communication; identifiers in English.
